# UX-C「站① 卡片減肥＋語氣＋退回＋警告皮」工人開場指令

> 用法：開**新**工人對話，把下方 ```text``` 框**整段貼上**。  
> 工人先交「修法確認清單」→ 老闆轉 UIUX 指揮放行 → 再動 code。  
> 畫面核帳：Claude；通過後 UIUX 指揮再開 UX-D（或依指揮排程）。  
> **前包**：UX-A ✅、UX-B ✅（2026-07-18 核畫面）。

---

```text
你是 Nestory【UX-C｜站① 卡片與審稿回饋】工人。
UI/UX 總指揮透過老闆轉達；系統總指揮是 Fable（避撞）。

════════════════════════════════════
必讀（只讀這些）
════════════════════════════════════
1. docs/交接指南-給接手的AI模型.md
2. docs/UIUX總指揮計劃包-2026-07-18.md
   ——§1.4 避撞、§2.2 命名、§2.5 各站卡片資訊、§6 UX-C（T7 T8 T9 T26 T31）
3. AGENTS.md UI 鐵則
4. 現況精讀（做到再打開）：
   - src/components/listing/ResultCard.tsx（站別 isCopyStation／isImageStation／isReadyStation）
   - src/lib/drafts/warningTiers.ts、resultCardTabs.ts
   - src/types/domain.ts → generation_tone?（migration 034；未跑 DB 時可能 undefined）
   - VersionNav ↺ 按鈕（同檔 ResultCard）
   - requestRevision / returnFromReady（window.prompt／confirm）

════════════════════════════════════
避撞
════════════════════════════════════
禁止：src/lib/providers/**、src/lib/csv/**、src/app/api/**、migrations、prompt／標題引擎
禁止：push／deploy；自稱實機通過
可以：components/listing/**、globals.css（警告／chip 皮）、lib/drafts 純顯示 helper
不寫 generate 寫入邏輯；generation_tone 若 DB 無值 UI 誠實降級（見 T8）

════════════════════════════════════
本包只做：T7、T8、T9、T26、T31
════════════════════════════════════

────────
【T7】站① 卡片資訊減肥（去圖標記／未標記；展開無圖片分頁）
────────
老闆／規格 §2.5：站①（copy_review）收合與展開**只要文案審核需要的**。

禁止在 isCopyStation 出現：
- 「圖片未標記（N）」chip／status（現 rc-status-chips 約 1454 行對所有站顯示 → 站① 隱藏）
- process_intent／標記摘要
- 展開後「圖片」分頁（RESULT_CARD_TABS 的 images）
- 核准按鈕 title 勿再寫「→ 圖片審核」；改「核准文案 → 標圖」或「核准文案」

允許／保留站①：
- 縮圖、標題、價區間、IP／角色／類型 chip
- 文案狀態、警告（見 T31）、語氣（T8）
- 動作：✓ 核准、↻ 重生、移出
- 展開分頁：文案／定價／Tags／SEO（定價若規格曾說站①可留售價編輯則保留現狀；**不要**為減肥刪定價能力）

站②／③：圖片相關 UI **保留**（T7 只動站①條件分支，勿誤刪 isImageStation 圖標記）

驗：站①收合列掃描無「未標記」；展開無「圖片」tab。

────────
【T8】卡面顯示文案語氣（generation_tone）
────────
資料：domain 已有 generation_tone?（P1-66／migration 034 註解）。
UI 定案：
- 站①（建議站②／③ 也顯示若有值）收合列或 meta 小 chip：
  「語氣：{generation_tone}」或短標「🎙 {tone}」
- 有值才顯示；null／undefined／空字串 → **不顯示假資料**、不寫「預設」唬人
  （可選 title 提示「尚無語氣紀錄／請確認 migration 034」僅 dev 不強求）
- **禁止**為顯示語氣去改 providers 或 generate API（禁區）；若欄位全站皆空，UI 接線仍算完成，核帳註「DB 無值／待 Fable／migration」

動檔：ResultCard.tsx（＋可選小 class 在 globals，用既有 chip tokens）

────────
【T9】②→① 退回：去掉理由 prompt，二次確認即可
────────
現況：requestRevision() 用 window.prompt("請輸入退回原因：") 再 POST request-revision。
定案：
1. **刪除 prompt**；改為二次確認（window.confirm 可暫用，或 inline「再按一次退回」；
   本包不强制抽 useDoubleConfirm，但禁止再要使用者打字理由）
2. body.comment 送空字串或省略（API 已 optional comment／null）
3. 成功／失敗：toast（UX-A showToast）＋必要時原地 message
4. returnFromReady 已有 confirm → 維持不要理由；文案「圖片審核」改「標圖」若有出現
5. **不改** api/request-revision route（禁區）。若 403（僅 admin/reviewer）屬既有權限，
   不在本包擴 operator——失敗 toast 誠實顯示即可

驗：點「↩ 退回」無輸入框；確認後可退回（有權限時）。

────────
【T26】文案區排版減空；↺ →「重生」文字
────────
1. VersionNav（ResultCard 內）：↺ 改為可見文字「重生」（busy 時「…」／「重生中」）；
   保留 title 說明會花 AI
2. 文案分頁：描述／FAQ 預覽預設不要佔整屏牆——
   - 預設預覽區 max-height + 展開（若尚無：加 .rc-copy-preview 限制約 8～12 行，可「展開全文」）
   - 不要大砍欄位；只減空白與預設高度
3. 勿動 HTML 對帳／formatPlainText 邏輯（那是 Fable／內容）

────────
【T31】警告分級視覺：建議🔍／待確認⚠／必修⛔
────────
邏輯已有 gradeDraftWarnings（block／confirm／suggest）。本包只做**皮**：

1. 收合列：
   - ⛔ block → 維持 is-block，用 --danger，夠醒目
   - ⚠ confirm → warn 樣式（用 --warn-text 可讀，UX-A 已修）
   - 🔍 suggest → **不要**跟 ⚠ 搶「待確認」文案；維持小標 🔍 N，muted／次要
2. 禁止把 suggest 算進「N 項待確認」字樣（若現有 confirm 文案已分開則核對即可）
3. 必修擋核准時按鈕 disabled title 清楚（已有可微調文案）
4. 可選：展開 Tags／警告列表時三級分組標題（建議／待確認／必修）——有就做，時間不夠可只做收合列

不改 warningTiers 業務分級規則（除非純 bug）；不改 API。

════════════════════════════════════
本包明確不做
════════════════════════════════════
- UX-D：Ctrl+V、上傳排序、語氣六卡緊湊
- UX-F：工廠橋接、站② 主圖｜規格｜詳情拖排序
- UX-E：批次列收合、Undo 大包
- api／providers／csv／migration
- 站② 圖三分頁大改（那是 UX-F）

════════════════════════════════════
流程
════════════════════════════════════
1. 先貼「修法確認清單」（每 T# 2–3 句＋擬改檔＋T26 預覽高度策略）。
   等「指揮放行」再動 code。
2. Commit 建議：
   feat(UX-C-T7): copy station card slim
   feat(UX-C-T8): show generation_tone chip
   feat(UX-C-T9): return without reason prompt
   feat(UX-C-T26): regen label + copy preview density
   feat(UX-C-T31): warning tier visuals
3. 每步 pnpm typecheck；CSS 跑 b15-style-probe
4. 收工：計劃包 §9 一行；施工清單進度筆記一行
5. 回報：檔案、generation_tone 有無實值、給 Claude 核帳步驟、手動？Y/N

鐵則：只加不減；站②③ 圖相關能力不可因 T7 誤刪；命名「標圖」不是「圖片審核」。
```

---

## 給 Claude 核帳用（UX-C 完工後）

| # | 步驟 | 通過標準 |
|---|---|---|
| 1 | 站①（審文案）收合卡 | 無「圖片未標記」；無圖標記摘要 |
| 2 | 站① 展開 | 無「圖片」分頁；有文案／定價／Tags／SEO（依實作） |
| 3 | 核准按鈕 | 文案不說「圖片審核」；有 generation_tone 時看得到語氣 chip |
| 4 | 站② 卡 | 仍有標記摘要／審核／圖相關（沒被 T7 砍掉） |
| 5 | 站②「↩ 退回」 | **無**「請輸入退回原因」；確認後可退（權限足夠時） |
| 6 | 單欄「重生」 | 按鈕有「重生」文字，不是只有 ↺ |
| 7 | 描述預覽 | 預設不佔整屏；可展開 |
| 8 | 警告 | ⛔／⚠／🔍 可分；suggest 不叫「待確認」 |
| 9 | 三主題＋手機 375 | 卡面無爆版 |

核完回 UIUX 指揮 → 下一包預設 **UX-D**。
