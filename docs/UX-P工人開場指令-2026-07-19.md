# UX-P「手機款式＋二手打磨」工人開場指令

> 用法：開**新**工人對話，整段貼下方 ```text```。  
> **前包**：A～N ✅；**UX-O 程式完成待 Claude 核**（老闆將批次核帳）。  
> **包二 B**：Fable **尚未**做多軸表單升級——本包**只做版面／手感**，**禁止**實作 1–3 軸交叉引擎（留給包二 B）。

---

```text
你是 Nestory【UX-P｜手機款式＋二手打磨】工人。
UI/UX 總指揮透過老闆轉達；Fable 包二 B 未到——本包不做多軸資料模型。

════════════════════════════════════
必讀
════════════════════════════════════
1. docs/交接指南-給接手的AI模型.md
2. docs/UIUX總指揮計劃包-2026-07-18.md ——§1.4、§6 UX-P（T69）、§9
3. AGENTS.md（tokens；.sel／.schip；三主題；手機 ≥44px）
4. 現況：
   - src/components/listing/VariantEditor.tsx（輸入區＋站①規格 tab 共用——**禁 fork**）
   - src/app/globals.css（.variant-box／.vgrid／.variant-*）
   - WorkspaceInputPanel 二手 pill／等級（UX-J T52 已過）
   - ResultCard 規格 tab（UX-M 嵌同一 VE）
5. 回饋 72 手機款式版面；R59 二手（基底已過，本包打磨）

════════════════════════════════════
避撞
════════════════════════════════════
禁止：api/、providers/、csv/、migration、prompt
禁止：**重寫** 多軸交叉展開／改 MAX 維度語意／fork 第二套 VariantEditor
禁止：push／deploy；自稱實機通過
可以：VariantEditor **只加** className／layout 結構、globals tokens、Workspace 二手區微調

════════════════════════════════════
本包只做：T69（拆三刀可合併）
════════════════════════════════════

────────
【T69-A】手機款式規格版面（R72）
────────
痛點：多欄款式表在手機橫向擠／難點。

指揮定案：
1. **&lt;960px**（對齊 C1 斷點）：
   - 款式表格改**可捲橫向**（overflow-x:auto）＋明顯 min-width，**或**
   - 每列改**卡片式堆疊**（軸值／成本／售價／庫存上下排）——二選一寫進修法清單，優先「不丟欄、可操作」
2. 觸控：input／btn-mini 區 min-height ≥44px（必要時只補 media query）
3. **同一套**改動必須同時惠及：輸入區 VariantEditor ＋ 結果卡 `.rc-specs-wrap` 內嵌（因共用元件）
4. 桌機 ≥960 維持可用網格，不故意弄壞
5. **不做** 新多軸 UX 流程、不改 formRowsToDb 語意

驗：375 寬可改成本／售價／庫存；無整頁橫向爆版；三主題。

────────
【T69-B】二手模式 UI 打磨（R59 餘）
────────
基底 T52 已有：一般｜二手 pill、等級、品況、備註。

指揮定案（小打磨，非重做）：
1. 手機：二手區塊在手風琴「基本資料」內是否好點、pill ≥44px、展開後不擠爆
2. 等級 pill 一排在窄屏可 wrap，不要擠成不可點
3. 文案：必要時一句 muted「二手會帶等級進文案／Tags」（短，非工程註記牆）
4. **不**改 is_secondhand 寫入語意、sale 連動規則（J 已定案）

驗：375 切二手→選級→可填；桌機仍順。

────────
【T69-C】自檢＋避撞包二 B
────────
1. 確認未 fork VE；未改 buildMatrixify／api
2. 修法清單註明：「多軸交叉留給包二 B；本包僅 layout」
3. 若發現現有 VE 在 2 軸以上桌機已可用——只優化手機，不重做維度 UI

════════════════════════════════════
本包不做
════════════════════════════════════
- 包二 B 多軸交叉編輯引擎
- UX-O 重做（已待核）
- 重做 T52 連動規則、BX-P

════════════════════════════════════
流程
════════════════════════════════════
1. 修法清單（T69-A 選堆疊 or 橫捲；會動哪些 class）→ 指揮放行
2. commits 建議：
   style(UX-P-T69a): mobile variant editor layout
   style(UX-P-T69b): secondhand mobile polish
3. typecheck；b15-probe
4. §9＋施工清單；不自稱通過；不 push
5. 老闆可能稍後才 Claude 核 O／P——你仍不自稱實機通過
```

---

## 核帳表（UX-P｜老闆批次核時用）

| # | 通過標準 |
|---|---|
| 1 | 375 款式可編、不整頁橫爆 |
| 2 | 輸入區＋站①規格 tab 同一手感 |
| 3 | 二手 pill／等級手機可點 |
| 4 | 桌機 1280 未壞 |
| 5 | 未 fork VE、未動 api/csv |
```
