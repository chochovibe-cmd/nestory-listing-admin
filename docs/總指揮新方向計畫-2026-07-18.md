# 總指揮新方向計畫（2026-07-18，Fable）

> 下一個 Fable session 的開工藍本。搭配讀：`docs/實機測試回饋-2026-07-16.md`（回饋 1–78）、
> `docs/UIUX總指揮計劃包-2026-07-18.md`（前台體驗歸 UIUX 指揮）、`docs/流程重構規格書-2026-07-16.md`。
> 分工鐵則：Fable＝系統/文案/管線/SQL 的**指揮**（找問題、定方案、發包、核帳，不親自寫 code；
> 修到一半的可收尾）；UIUX 指揮＝前台 IA/卡片/通知/命名；衝突時系統邏輯以 Fable 為準、畫面以 UIUX 為準。

## 1. 第三輪測報分診（防止下一手重修沒壞的東西）

### 1.1 其實已生效、被舊資料誤判成「沒修」的
| 項 | 證據 |
|---|---|
| ◈ 描述 HTML 排版（h3/ul） | 老闆自己匯出的 Matrixify CSV 第 10 列（TOYUKI 小八）就是 `<h3><strong>◈ 商品亮點</strong></h3><ul>…`；第 2 列是舊草稿存檔（舊格式），**重新生成才會換新格式** |
| 標題重複詞（吊飾吊飾） | 修復已 push（titleGenerator dedupe 層）；佇列裡看到的是修復前存檔的舊卡 |
| 發布紀錄「已發布／封存」分頁 | R4 已做（四分頁第 4 格），Fable 實測截過畫面；老闆回報沒看到，待確認是否 Vercel 版本落差或漏看 |
| 描述欄「原始碼」顯示純文字 | 刻意設計（A23）：存純文字、出 Shopify 才轉 HTML；FAQ 存真 HTML 所以兩欄長不一樣。是**標籤誤導**（77，歸 UX 改字），不是壞 |

### 1.2 真 bug（P0 修復包，最優先發工人）
| # | 問題 | 指揮診斷 |
|---|---|---|
| 61 | 佇列卡「↻ 重生」按了沒反應 | R2 RegenCopyModal 接線疑未觸發（開窗事件或 modal 條件），工人從 ResultCard 的 onClick → modal state 查起 |
| 62 | 失敗草稿重生成功（進度卡打勾）但佇列卡仍顯示失敗 | generate 成功後 status/pipeline_stage 未從 failed 拉回，或前端佇列未 reload；查 generate route 對 failed 草稿的狀態覆寫＋DraftResultsPanel 刷新 |
| 73 | **handle 撞名（嚴重）**：兩件不同商品同 handle `chiikawa-hachiware`，Matrixify 匯入會把兩件合併互蓋 | 已診斷：handleGenerator 產 `{ip}-{character}-{type}` 決定性 slug、無唯一後綴。修法：slug 尾掛 draft id 短碼（如 `-6bb298`）或特色詞＋撞名時強制後綴；補 verify |
| 63 | 站②→① 退回 API 必填 reason | 放寬為選填（UI 的二次確認歸 UX-C，本包只鬆 API） |
| 74 | 庫存「無上限」商品 Matrixify 匯出 Qty 0 + policy deny＝到店買不到 | 對映 018 庫存語意：無上限應出 continue（或 qty 不追蹤）；查 matrixify.ts/payload 的 inventory 對映並修 |
| 78 | 按生成後輸入區欄位疑未清（設計是保留來源/狀態/語氣、清標題/圖/價格） | 工人查 light-reset 實際行為；壞就修，沒壞就在收工報告寫明（老闆的困惑另由 UX-A toast 解） |

### 1.3 文案品質（P1 包，P0 後接著發）
| # | 問題 | 指揮診斷 |
|---|---|---|
| 75a | **品牌斷鏈**：LLM 明明認出 TOYUKI（寫進商品資訊）卻沒進標題 | 根因：`product_drafts.product_brand`（031）沒有任何寫入來源。修法三件套：LLM 偵測輸出加 brand 欄位（[[key]] 段）→ generate route 寫回 product_brand → 標題/SEO 引擎已就緒（夜工完成）會自動吃到；表單手填欄位待 UX 包 |
| 75b | LLM 標題沒套骨架：多角色「・」沒出現（只有小八）、品牌×IP 沒出現 | 夜工只補了 rule engine＋seo_title 指引；`title_zh/enriched_title` 的 prompt 規則沒補。修法：標題欄位規則加「品牌 × IP（中文在前英文在後）｜多角色・列出（≤3，款式含的角色也算）｜特色」，並把 variantSummary 的角色明確餵進 prompt 事實區 |
| 76 | 小編聊天口吻不像小編、說好可含 Emoji 也沒出現 | prompt 的 emoji 規則是「允許」→ 改「此語氣描述與 FAQ **必須**自然使用 1–2 個」；語氣示範句每語氣補到 2–3 句、拉開語感差異；用老闆點讚的 HelloKitty 環保杯那篇當語感錨點（在 DB 裡撈出來引用） |
| 66 | 卡片要顯示本次生成語氣 | 需 tone 入庫：migration 產檔（product_drafts.generation_tone 或併入既有 generation 欄位群）＋ generate 寫入；卡片顯示歸 UX-C |
| 69 | SM/Matrixify 匯出也要進發布紀錄（可篩選） | 覆寫 D7 A-lite「CSV 不進批次帳」：exports route 建 publish_batches（kind=showmore/matrixify）；紀錄頁篩選歸 UX |
| 68 | Matrixify 預覽表格欄位太少 | 覆寫 Fable R3 Q5-A 裁決：表格模式改「該 kind 完整欄位＋橫向捲動」；資料面我方支援、畫面歸 UX |

### 1.4 歸 UIUX 指揮（已同步，不重複發）
64 站①卡瘦身、65 站籤常駐＋失敗獨立籤、67 站②三分頁＋拖排序＋補圖、70 桌機三塊/手機 IA、
71 toast＋辨識完成回報、72 手機款式規格版面、77「原始碼」改字。
**我方共管支援**（UX 開對應包前提供）：63 reason 放寬（P0 做）、66 tone 入庫（P1 做）、
失敗籤資料來源（countPipelineStations 已有 fail 計數，直接用）、工廠橋接 query（image_batches 現有欄位夠，UX-F 前確認）。

## 2. 發包順序（定案）

```
P0 修復包（工人，立刻）
   ↓ 核帳
P1 文案品質包（工人）      ∥ UX-A Toast（UIUX 指揮發，可並行——UX-A 動 components/通知，
   ↓ 核帳                       P0/P1 動 lib/api/prompt，檔案不重疊；有撞先停）
UX-B 起（UIUX 指揮排程）    ∥ Fable 後續：AI 合成圖包／擷取小工具／爬蟲選型已定 D 路線
```

老闆晨測未完成項（簡轉繁送圖、送圖全鏈、三主題）等 P0 修完一起補測。

## 3. P0 修復包工人開場指令（貼給工人）

```text
請依序讀這三份，其他文件等做到相關項目再讀：
1. docs/交接指南-給接手的AI模型.md
2. docs/施工清單.md（進度筆記頂部）
3. docs/總指揮新方向計畫-2026-07-18.md（本包依據：§1.2 六項，引用編號）

你是本包工人，總指揮是 Fable（透過老闆轉達）。本包代號【P0｜功能修復】，只做 §1.2 的六項：
61 重生鈕無反應、62 失敗重生狀態不同步、73 handle 撞名（最優先，含唯一後綴＋verify）、
63 退回 reason 改選填（只鬆 API）、74 無上限庫存的 Matrixify inventory 對映、78 light-reset 查證。
每項先讀對應診斷再動手；78 若查證沒壞，收工報告寫明即可不硬改。
鐵則：不動 UI 版面與樣式（UIUX 指揮轄區）；不碰 titleGenerator/seoGenerator/systemPrompt（P1 範圍）；
零 migration 預設；每步 typecheck＋verify-r2/r3/d8/matrixify 相關；新增 verify-p0-fixes 腳本；
不 push；收工必 git commit 並列 hash；收工報告對照編號念完成度；照 AGENTS.md 收工儀式收尾。
```

## 4. P1 文案品質包（P0 核帳後由 Fable 出開場指令）
範圍：75a 品牌三件套、75b LLM 標題骨架規則、76 語氣/Emoji 強化、66 tone 入庫（migration 產檔）、
69 CSV 匯出入批次帳。裁決點預告：brand 偵測的誠實邊界（沒把握不填）、tone 欄位設計。

## 5. 下一個 Fable session 開場指令（老闆貼）

```text
請依序讀這四份：
1. docs/總指揮手冊.md
2. docs/總指揮新方向計畫-2026-07-18.md（現況與發包藍本，§1 分診表最重要）
3. docs/實機測試回饋-2026-07-16.md（回饋編號唯一真相）
4. docs/UIUX總指揮計劃包-2026-07-18.md（分工邊界）
再看 git log --oneline -15 對帳。
你是 Fable 總指揮：只指揮不寫 code（半成品可收尾）。P0 修復包（計畫 §3）可能已發包，
先收工人回報照慣例核帳（typecheck＋verify＋內建瀏覽器實測）；然後照 §2 順序推進。
鐵則：SQL 只產檔；push 要老闆同意；每包收工後給老闆下一步的貼上即用指令。
```

## 6. 對 UIUX 指揮的回覆（老闆轉達）
1. §2 架構命名（審文案／標圖／待發布／工廠）**OK**——「標圖」比先前定的「圖片審核」更準，
   視為覆寫 2026-07-17 命名定案，請在你方文件註記供工人對齊。
2. 順序同意「Fable 先收功能/文案」，但 **UX-A（Toast）可立刻並行**：它動 components/通知層，
   P0/P1 動 lib/api/prompt，檔案不重疊；請在 UX-A 開工指令加避撞條款（不碰 lib/providers、lib/csv、api/）。
3. 共管四項我方時程：reason 放寬＝P0 內；tone 入庫＝P1 內；失敗籤計數 pipelineStage.countPipelineStations
   已提供直接用；工廠橋接 query 於 UX-F 開工前由 Fable 確認欄位。
```
