# UX-S「包二 B 多軸編輯 ＋ 成本一鍵套用 ＋ P4 改字」工人開場指令

> 用法：開**新**工人對話，整段貼下方 ```text```。  
> **Fable 2026-07-19**：包二 B 前置就緒，**不用再等 Fable 開工**；資料行為方案貼 Fable 裁決，畫面歸 UIUX。  
> 規格：`docs/包二規格書-多維規格模型-2026-07-18.md` §3B＋§4。

---

```text
你是 Nestory【UX-S｜包二 B 多軸 ＋ T72 一鍵成本 ＋ T73 P4 改字】工人。
UI/UX 總指揮管畫面；系統總指揮 Fable 管資料行為。

════════════════════════════════════
必讀
════════════════════════════════════
1. docs/交接指南-給接手的AI模型.md
2. docs/UIUX總指揮計劃包-2026-07-18.md ——§1.4、§6 UX-S（T72 T73 ＋包二 B UI）、§9
3. docs/包二規格書-多維規格模型-2026-07-18.md ——§3B 表單多軸、§4 裁決點
4. AGENTS.md UI 鐵則
5. 現況：
   - src/components/listing/VariantEditor.tsx（唯一殼；站① 規格 tab 共用——**禁 fork**）
   - src/lib/variants/**（dimensions、rows、mapping、persist）
   - WorkspaceInputPanel 如何 seed／save variants
   - WorkspaceInputPanel Web Search 說明（約「查來的內容會標來源」——P4 後已不成立）
6. UX-M／UX-P：卡片嵌同一 VE；手機堆疊 layout 保留

════════════════════════════════════
避撞
════════════════════════════════════
禁止：api/（除非 Fable 白名單）、providers/、csv 匯出語意、migration（規格說零／極少 migration——**不要自開 migration**）
禁止：fork 第二套 VariantEditor
禁止：push／deploy；自稱實機通過
可以：VariantEditor、lib/variants 純前端 helper、WorkspaceInputPanel、globals、ResultCard 僅若嵌 props 需要

════════════════════════════════════
本包做：包二 B UI ＋ T72 ＋ T73
════════════════════════════════════

────────
【包二 B】VariantEditor 1–3 軸交叉編輯
────────
目標（規格 §3B）：
- 可定義 1–3 軸（variant_dimensions，既有 MAX）
- 軸值交叉 → 列（product_variants 對映 option1/2/3）
- 可編每列成本／售價／庫存／圖（沿用既有欄）
- 舊單維（option1＝款式）草稿 **零回歸**

指揮定案（畫面）：
1. **直接改共用 VariantEditor**——輸入區＋站① 規格 tab 自動吃到
2. 軸 UI：新增／刪軸、軸名、軸值 chips 或輸入（對齊現有 tokens；手機沿用 P 堆疊）
3. 「依軸值重新展開列」若會覆蓋手填價——要二次確認或保留已填列策略（**寫進修法清單給 Fable 裁**）
4. 不做 Shopify／CSV 後端對映（包二 A 已做）

**修法清單必答（貼指揮 → 指揮轉 Fable）**：
- 交叉展開演算法（笛卡爾？上限列數？）
- 刪軸／改軸值時既有列如何 merge
- 舊單維相容
- 與 persistVariantsSafe／formRowsToDbInserts 是否需擴

────────
【T72】成本一鍵套用到全部款式（R87）
────────
- 商品級成本（productCost）一鍵寫入所有列 cny／成本欄（僅未鎖定價？或全寫——**修法清單二選一，預設：只填空白列，已填不覆蓋**；若要全覆蓋需 double-confirm）
- 按鈕文案例：「套用成本到全部款式」；mini／secondary，在 VE 頭列
- 不改定價公式引擎，只改列上成本欄；若有 reprice 解鎖列再沿用既有 recalculate

────────
【T73】P4 後 Web Search 說明改字
────────
現況：WorkspaceInputPanel 仍有「查來的內容會標來源提醒核實」
P4 後顧客文案不再標「來源：網路」。
改為誠實短句，例如：「預設開啟（冷門 IP／規格更準）；趕時間可關。搜尋結果只當內部核實，不會寫進上架文案。」
全站搜同類誤導字一併改。

════════════════════════════════════
流程（重要）
════════════════════════════════════
1. **先交修法確認清單**（含包二 B 資料行為答題）→ UIUX 指揮放行畫面部分
2. 指揮把資料行為段轉 Fable 裁決（或老闆代轉）→ 收到 Fable OK 再動交叉展開 code
   （T72／T73 純 UI 可在 Fable 回覆前先做，若時間緊）
3. commits 建議：
   feat(UX-S-T73): fix web search copy after P4
   feat(UX-S-T72): apply product cost to all variants
   feat(UX-S-pkg2b): multi-axis VariantEditor cross edit
4. typecheck；b15-probe；若有 verify-b7／variants 腳本一併跑
5. §9＋施工清單；不自稱通過；不 push

════════════════════════════════════
本包不做
════════════════════════════════════
- 改 Matrixify／Shopify server 展開（包二 A）
- 擷取 CAP 語意
- BX-P
```

---

## 核帳重點

| # | 通過標準 |
|---|---|
| 1 | 可建 2–3 軸並產出交叉列 |
| 2 | 舊單維草稿仍可用 |
| 3 | 一鍵套成本行為符合定案 |
| 4 | Web Search 說明無「標來源」誤導 |
| 5 | 站① 規格 tab 同一 VE；375 可用 |
```
