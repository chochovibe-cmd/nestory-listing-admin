# UX-B4-P06「失敗卡 — 需修改同大＋紅字上標題列」工人開場指令

> **用法**：開**新的 Grok 工人對話**，把下方 ```text``` 框**整段貼上**。  
> **工人**：**Grok**（只實作＋commit feat；**不 push**）。  
> **來源**：老闆 2026-07-21 補充截圖 #1（失敗／需修改卡）。  
> **同步**：`docs/UIUX本輪改動同步-2026-07-21.md` §2.8。  
> **分支**：`codex/nestory-v0.1-safety-skeleton`。  
> **建議**：可先於 P04 做（小）；與 P04 同檔 `ResultCard.tsx` 時避免並行互蓋。

---

```text
你是 Nestory【UX-B4-P06｜失敗／需修改標籤與紅字位置】工人（Grok）。
臨時代理指揮規格已定案，照做即可。

════════════════════════════════════
必讀
════════════════════════════════════
1. AGENTS.md
2. docs/UIUX本輪改動同步-2026-07-21.md §2.8
3. 動檔：
   - src/components/listing/ResultCard.tsx
     · failReasonSummary（formatDraftFailSummary）
     · .rc-fail-reason 現在在 header **下方**獨立 <p>（約 L2421）
     · secondary StatusBadge（needs_revision →「需修改」）
     · primary station chip（失敗 → schip--error）
   - src/components/listing/StatusBadge.tsx（.status.action vs .status.failed 字級）
   - src/app/globals.css
     · .status / .status.action / .status.failed
     · .rc-station-chip / .schip--error
     · .rc-fail-reason

════════════════════════════════════
避撞
════════════════════════════════════
禁止：push；改失敗判定 API／formatDraftFailSummary 語意（只搬 UI）；新 !important；自創色。
可以：CSS 對齊尺寸；把 failReason 挪到標題列。

════════════════════════════════════
現況根因（已對照）
════════════════════════════════════
1. 「失敗」多半是 .schip.schip--error.rc-station-chip（較醒目）
2. 「需修改」走 <StatusBadge status="needs_revision" /> → .status.action
   （字級／padding 可能與 station chip 不一致 → 看起來比較小）
3. 紅字原因字串：failReasonSummary 渲染在 header 外的 .rc-fail-reason 整行，
   老闆要改成跟**標題同一排後方**（可 wrap，但語意是 title 旁附註，不是卡片底橫幅）

════════════════════════════════════
本包只做
════════════════════════════════════

【1】「需修改」與「失敗」同大、同權重視覺
- 對齊 font-size、padding、line-height、min-height（建議兩者都吃同一套「狀態主標」尺寸）
- 色相可維持：失敗＝error／accent2 系；需修改＝warn／action 系（不要兩個都變紅）
- 做法擇一（註明於報告）：
  A. StatusBadge 在 result-card 內改 class 對齊 .rc-station-chip 尺寸
  B. globals 把 .status.action／.status.failed 與 .rc-station-chip 字級 padding 拉齊
- 桌機＋手機都適用（不限 isNarrow）

【2】紅字「缺少…」移到標題後同一排
- 來源仍是 failReasonSummary（或同等 block 級摘要字串）
- 從 header 下方獨立 <p className="rc-fail-reason"> **移出**
- 放到 .rc-title **後方**（同一 flex 容器可 wrap）：
  例：【標題文字】【銷售小標】【紅字原因精簡】
- 樣式：延續 error／warn 文字色（可用 .rc-fail-reason 改成 inline／inline-flex）
- 長文：允許換行；可用 title 屬性放全文；列表勿把卡片撐到不可掃
- 無 failReason 時不佔位

【3】不要做
- 不改 warnings 分級邏輯
- 不把整份 Tags 必修清單全塞標題列（只搬現有 failReasonSummary 那一條摘要）
- 不做 P04 三排（除非同工人已接 P04）

════════════════════════════════════
驗收
════════════════════════════════════
[ ] 失敗篩選下列卡：「需修改」與「失敗」視覺高度／字級接近
[ ] 紅字「缺少有效售價…」等在標題後，不在卡底獨立大條（摺疊態）
[ ] 無 failReason 的卡不殘留空位
[ ] 桌機＋手機都正確
[ ] typecheck 綠；不 push

════════════════════════════════════
收工
════════════════════════════════════
commit：feat(UX-B4-P06): match revision badge size; fail reason after title
回報：尺寸對齊策略 A/B、failReason DOM 新位置。

開工先回 4 行：失敗／需修改各自 class、failReason 現況節點、移動目標、是否動 StatusBadge 全域。
```

---

## 總指揮備註

| 項 | 內容 |
|---|---|
| 體量 | 小～中 |
| 雷 | 改 .status 全域會影響其他頁 StatusBadge → 優先 scope 在 .result-card 內 |
