# UX-B4-P02「預覽卡片標題後 — 銷售狀態小標」工人開場指令

> **用法**：開**新的 Grok 工人對話**，把下方 ```text``` 框**整段貼上**。  
> **工人**：**Grok**（只實作＋commit feat；**不 push**）。  
> **來源**：老闆截圖 #2；同步 `docs/UIUX本輪改動同步-2026-07-21.md` §2.2。  
> **分支**：`codex/nestory-v0.1-safety-skeleton`。

---

```text
你是 Nestory【UX-B4-P02｜卡片標題銷售狀態小標】工人（Grok）。
臨時代理指揮規格已定案，照做即可。

════════════════════════════════════
必讀
════════════════════════════════════
1. AGENTS.md
2. docs/UIUX本輪改動同步-2026-07-21.md §2.2
3. 動檔：
   - src/components/listing/ResultCard.tsx（rc-title 一帶）
   - src/lib/saleStatus.ts（顯示短標 helper，可擴）
   - 可對照 globals.css .rc-title／.rc-detect-chip

════════════════════════════════════
避撞
════════════════════════════════════
禁止：push；改定價／核准流程；改 sale_status 寫入語意；新 !important；自創色。
可以：ResultCard 標題列 JSX + 小 CSS class + saleStatus 純顯示函式。

════════════════════════════════════
本包只做
════════════════════════════════════
1. 在預覽卡片標題（.rc-title）**同一視覺標題列後方**加小標，顯示商品銷售狀態。
2. 資料：draft.sale_status
3. 顯示短標（對齊同步文件）：
   - 海外代購（約14天）→ 海外現貨
   - 台灣現貨 → 台灣現貨
   - 二手現貨 → 二手現貨
   - 預購中 → 預購中
   建議新增 pure helper 如 formatSaleStatusBadge(sale_status) 放 saleStatus.ts，避免 UI 散落 magic string。
4. 樣式：小 chip 或 muted badge；重用 .rc-detect-chip 或新增 .rc-sale-badge（tokens only）。
5. 空值／未知：不顯示（未知原文若 normalize 後仍非四類，可顯示 normalize 後原文或隱藏——建議隱藏避免髒字）。
6. 不要跟站別「文案已生成・待審核」搶同一 schip 列。

════════════════════════════════════
驗收
════════════════════════════════════
[ ] 有 sale_status 的卡標題後可見小標
[ ] 四類對照正確
[ ] 無值不顯示
[ ] 桌機＋手機標題不爆版（可 wrap）
[ ] typecheck 綠；不 push

════════════════════════════════════
收工
════════════════════════════════════
commit：feat(UX-B4-P02): sale status badge after result card title
回報：helper 名稱、class 名、四類對照表。

開工先回 3 行：資料欄位、短標映射、掛在 rc-title 哪裡。
```

---

## 總指揮備註

| 項 | 內容 |
|---|---|
| 體量 | 小；適合先做暖身 |
| 依賴 | 與 P04 同檔 ResultCard → **P02 先於 P04** 或同一工人連做 |
