# 總指揮代理紀錄

> 用途：記錄代理總指揮期間的裁決、發包指令、風險與交接點。下一任指揮先讀
> `docs/總指揮手冊.md`，再讀本檔即可接續。

---

## 2026-07-13｜Codex 暫代 Fable 指揮

### 接手狀態

- 使用者指定 Codex 暫時代理 Fable，擔任專案總指揮。
- 已讀：`docs/交接指南-給接手的AI模型.md`、`docs/施工清單.md`、
  `docs/Mockup差異備忘.md`、`docs/總指揮手冊.md`。
- `git log --oneline -15` 顯示最新進度到 C1 App Shell：
  `a067d9c docs(C1): mark done, add diff 16 and acceptance checklist`。
- `git status --short` 顯示未追蹤：
  `.claude/settings.local.json`、`docs/總指揮手冊.md`。
- 注意：`docs/總指揮手冊.md` 寫「014-025 migration 已全部執行」，
  但 `docs/施工清單.md` 待確認區仍列多個 migration 待執行。使用者回覆：
  「Supabase 那個因為我現在外面先不碰」，因此本代理期間不以外部 Supabase
  對帳作為阻塞項，不要求使用者現在貼 SQL。

### C2 工人方案裁決

工人 Grok 提出 C2「設定分類收合」設計方案，代理指揮裁決如下：

- Q1：選 C。設定入口放桌機側欄底部＋手機更多抽屜；頂欄不新增設定齒輪。
- Q2：選 A。admin＋operator 都看得到入口，頁內依權限鎖寫入。
- Q3：選 A。獨立頁 `/settings`。
- Q4：選 A-lite。做 Admin System Prompt UI＋版本骨架，但本包不得讓功能依賴
  使用者立刻執行新 SQL；若需要新表，只產 migration 並讓 UI 有待接線/待 SQL 的誠實提示。
  `generate` 不在本包強制改讀 DB prompt。
- Q5：選 A。定價設定先延續現有 localStorage＋事件同步，避免新增 Supabase 依賴。
- Q6：選 A。工作檯底部定價規則設定保留可編，與設定頁雙向同步。
- Q7：選 A。頂欄 Bar 只顯示套用中匯率；抓今日/套用只在設定頁。
- Q8：選 A-restricted。自動化區可做 UI 和非敏感偏好；Make Webhook URL 不得存
  client localStorage，若沒有 server-side 安全儲存就先 disabled/標「Phase D 接通」。
- Q9：選 A。Showmore +5% 暫跟定價 localStorage；D8 或正式發布前再轉 team_settings。

### 補充要求

- 本包不碰外部 Supabase、不要求使用者現在執行 SQL。
- 可以產 `.sql` 檔，但 C2 驗收不得依賴未執行的 migration。
- 不要讀 `.env.local` 內容；不要把 webhook URL、token、secret 放進前端儲存。
- C2 若產生新的「與 Mockup 不同但已定案」差異，必須補 `docs/Mockup差異備忘.md`。
- 收工報告必須列：changed files、commits、typecheck、`scripts/b15-style-probe.mjs`、
  375px/960px 斷點自查、C2 驗收步驟。

### C2 收工遺失回覆後的復原核帳

使用者不小心關掉 Grok 視窗，未能複製 C2 收工報告。代理指揮從 repo 復原如下：

- 最新 C2 commits：
  - `24bc559 feat(C2): settings page with collapsible sections and nav entry`
  - `5f1b127 feat(C2): optional migration skeleton for system_prompt_versions`
  - `011d007 docs(C2): mark done, diff 17, acceptance checklist`
- 施工清單已將 C2 打勾，並新增 C2 驗收步驟清單。
- Mockup 差異備忘已新增差異 17，記錄 C2 設定頁定案：入口側欄底/更多抽屜、獨立頁、
  頂欄匯率只顯示、Prompt 骨架不依賴 SQL、Showmore % 本機、Webhook 禁用。
- 產生 migration `supabase/migrations/026_system_prompt_versions.sql`，但標為可選，
  C2 驗收不依賴執行；符合使用者「Supabase 外面先不碰」的邊界。
- 代理指揮補跑 `npm.cmd run typecheck`：通過。
- 代理指揮補跑 `node scripts/b15-style-probe.mjs`：失敗於 Chrome headless 啟動
  `theme dark failed code=2147483651`，未取得樣式報告。此為本視窗重現限制或 Chrome profile/環境問題，
  不可當成 C2 CSS 通過證據；後續若需要正式核收，請工人重跑或改用可重現的瀏覽器驗收截圖。
- `git status --short` 仍有未追蹤：
  `.claude/settings.local.json`、`docs/總指揮手冊.md`、`docs/總指揮代理紀錄.md`。

### C2 實機驗收延期

- 使用者在外部 Wi-Fi 環境用 Supabase 帳密登入失敗，畫面顯示 `couldn't fetch`。
- 判斷：高度可能是目前 Wi-Fi/網路環境擋 Supabase 請求，不先視為 C2 功能缺陷。
- C2 需要登入後才能驗的項目延後回家測：
  - 側欄/手機更多入口進 `/settings`
  - 定價設定與工作檯雙向同步
  - admin/operator 權限差異
  - 連線狀態區重新檢查
  - 三主題與 375px/800px/960px 肉眼驗收
- 因 Supabase 登入暫不可用，下一包不建議 C3 成員管理；優先改做較不依賴登入與外部 DB 的 C6 匯率。

---

## 2026-07-13｜Grok 接續暫代指揮（Fable 額度恢復後交回）

### 接手狀態

- 使用者指定：Fable 目前無額度；Codex 曾代理一個段落；**現由 Grok 暫代總指揮**，
  等 Fable 恢復額度再交回正式總指揮。
- 已讀：`docs/總指揮手冊.md`、`docs/總指揮代理紀錄.md`（含 Codex 段）、
  `docs/施工清單.md`、`docs/Mockup差異備忘.md`（差異 17／C2）、
  Mockup 頂欄匯率＋設定定價區、現有 `ExchangeRateWidget`／`SettingsPanel` 匯率邏輯。
- `git log --oneline` 最新：`011d007 docs(C2)…`／`5f1b127`／`24bc559`（C2 已落地）。
- 工作區：施工清單有 Codex 進度筆記 4 行未 commit（C2 實機登入延期＋建議 C6）；
  未追蹤：`docs/總指揮手冊.md`、`docs/總指揮代理紀錄.md`、`.claude/settings.local.json`。
- 邊界延續 Codex 裁定：不碰外部 Supabase／不逼貼 SQL；C3 暫緩；本包唯一工項 **C6**。

### 現況一句話

C1／C2 程式完成；C2 實機登入驗收因外部 Wi‑Fi／Supabase 擋連線延期；
頂欄匯率仍只有「套用中」，尚無「今日參考」、server FX API、Cron 骨架——**C6 待開工**。

### C6 工人方案裁決（Grok 代理指揮）

工人（另一 Grok 視窗）提出 C6 設計方案。對照：
- Mockup 頂欄：`¥→NT$ **套用中**　今日 x.xx`（僅顯示，套用在設定）
- 計畫／交接：開工具自動抓當日**僅顯示**；套用由使用者按
- C2 差異 17 Q7-A：頂欄不 ↻ 改套用中；抓取／套用在設定頁；Cron 屬 C6

**整體方案：OK，放行實作。**

| 題 | 裁決 | 理由（一句） |
|---|---|---|
| Q1 開頁自動抓今日參考 | **A** | 對齊計畫／Mockup「開工具自動抓僅顯示」；同日快取；失敗「今日 —」不 toast |
| Q2 Cron 做到哪 | **A** | vercel.json + `/api/cron/fx` 完整骨架；本包不寫 DB；留 1 支 cron 額度給日後 D6 |
| Q3 今日數字來源 | **A** | 本機 last successful `/api/fx`；無 DB 不做假 server 快取；全隊共用延後 |
| Q4 誰可套用 | **A** | 維持 C2：admin + operator；C6 不收緊以免與定價本機權限打架 |

### 補充要求（工人必守）

1. **兩數字語意不可混**：套用中＝`nestory_pricing_settings.rate`；今日參考＝新 key（如 `nestory_fx_reference`）。
   開頁／Cron／抓取**絕不**自動改套用中。
2. **抓取統一走 server**：`src/lib/fx/fetchCnyTwdRate.ts` + `GET /api/fx/cny-twd`；設定頁拿掉瀏覽器直連 open.er-api.com。
   誠實失敗，不塞假匯率、不沿用昨日期冒充今日。
3. **Cron**：`GET /api/cron/fx` + Bearer `CRON_SECRET`（prod 無 secret 拒絕；dev 可放寬並註明）；
   `vercel.json` 加 1 支 cron（建議 `0 16 * * *` UTC，註解寫清約台灣時間）；
   `.env.example` 加 `CRON_SECRET` 說明；**不讀、不印 `.env.local`**。
4. **預設套用中 rate 維持 4.5**，不為對齊 Mockup 示意 4.70 而改。
5. **頂欄**：套用中粗／大 + 今日較小 muted；無「↻ 一鍵改套用中」；點頂欄不強制開設定。
6. **樣式**：只用現有 tokens／`.rate-val`／muted；**BX-P 不動**其它版面。
7. **文件**：收工時清單打勾 C6＋驗收步驟；`docs/Mockup差異備忘.md` 補差異（建議差異 18）：
   今日參考本機 store、全隊 DB 共用延後、Cron 僅骨架不寫 DB、預設 4.5 等。
8. **驗證**：每步 typecheck；能跑 `b15-style-probe` 就跑，Chrome headless 失敗誠實回報；
   可選 `scripts/verify-c6-fx.mjs`。不 push、不 deploy、無 SQL。
9. **commit**：`feat(C6): …`／`docs(C6): …` 前綴；收工報告含 changed files、commits、
   typecheck、probe、驗收步驟。

### 本回合發包

- 狀態：裁決完成，**工人可動工 C6**（見老闆對話中「貼給工人」指令）。
- 下一指揮（Fable 或後續代理）接續時：先核 C6 收工報告＋抽查 `fetchCnyTwdRate`／兩 store 是否分離／頂欄無 ↻。

### C6 收工核帳（Grok 代理指揮；程式通過，實機畫面未測）

**Git 對帳**
- commits 齊：`2ffe1fa` → `4a5b8f9` → `f63460c`（與工人報告一致）。
- 工作區乾淨（就 C6 相關檔）；僅未追蹤：`docs/總指揮手冊.md`、`docs/總指揮代理紀錄.md`
  （指揮文件，未要求 commit）。

**指揮複跑**
- `npm run typecheck`：綠燈
- `node scripts/verify-c6-fx.mjs`：ALL passed

**程式碼抽查（通過）**
- `fetchCnyTwdRate`：誠實失敗、不塞假匯率；parse／round 合理。
- 兩 store 分離：`nestory_pricing_settings` vs `nestory_fx_reference`；事件分離。
- `GET /api/fx/cny-twd`、`GET /api/cron/fx`：後者 Bearer secret、prod 無 secret 拒、
  `persisted: false`、不寫 DB。
- `vercel.json`：1 支 cron `0 16 * * *`；`.env.example` 有 `CRON_SECRET`。
- 頂欄：套用中＋`今日`／`—`；開頁 auto-fetch 只寫 reference；**無 ↻ 套用**。
- 設定：抓取改 `/api/fx/cny-twd`，已無瀏覽器直連 open.er-api；套用才改 pricing rate。
- `.fx-ref` 僅用 `--text-muted` token；預設 rate 4.5 保留。
- 清單 C6 已勾、差異 **18**、驗收步驟齊。

**裁定**：C6 **程式核帳通過**。指揮／本環境無法代做登入後實機畫面，下列列入「待老闆實機累積清單」，
**不阻擋** C6 程式收工；回家／網路可登入後一次測完即可。

### 待老闆實機驗收累積清單（環境恢復後一次測）

> 用途：指揮無法實機、或老闆在外部 Wi‑Fi 登入失敗時，把未完成畫面測試集中在此，
> 避免散落在對話。測完一項就在清單／此處打勾或刪除。

#### 延期原因摘要
- 外部 Wi‑Fi 下 Supabase 登入曾出現 `couldn't fetch`（偏網路環境，不先當功能缺陷）。
- 代理指揮視窗不代跑完整瀏覽器登入流程；只做 git／typecheck／腳本／程式抽查。

#### C2 設定頁（需登入；約 12–15 分）— 見施工清單「C2 驗收步驟清單」
- [ ] 桌機側欄底 ⚙ → `/settings`；頂欄**無**設定齒輪
- [ ] 手機 &lt;960「更多」抽屜含設定
- [ ] 五區收合可用；定價改後與工作檯底部定價規則雙向同步
- [ ] 抓取／套用匯率（C2 基底；C6 後再以 C6 清單為準）
- [ ] admin vs operator 寫入權限差（Prompt／自動化僅 admin）
- [ ] 連線狀態區重新檢查
- [ ] 三主題 dark／nordic／kitty＋375／800／960 肉眼

#### C6 匯率（可與 C2 合併測；約 8–10 分）— 見施工清單「C6 驗收步驟清單」
- [ ] **A** 頂欄：套用中粗體＋`今日 x.xx` 或 `今日 —`；**無** ↻ 一鍵改套用中
- [ ] **A** 開頁幾秒：網路通則今日出現；**套用中不因開頁自動變**（先記數字再重整）
- [ ] **B** 設定→定價：抓取→只更新今日；套用→套用中＋頂欄粗體＋公式／工作檯同步
- [ ] **B** 手改 CNY 匯率欄仍可；未改過預設可為 4.5
- [ ] **C**（可選）斷網抓取→黃字、無假今日、套用中不變
- [ ] **D**（部署後）Vercel 設 `CRON_SECRET`；排程 `0 16 * * *`；log 見成功／失敗且 `persisted: false`
- [ ] 工程已代核可略：`verify-c6-fx`、typecheck（指揮 2026-07-13 已綠）

#### 部署時才做（非本包阻塞）
- [ ] Vercel 環境變數加 `CRON_SECRET`（deploy 前）
- [ ] push／deploy 需老闆明確同意後再做
- [ ] 全隊共用今日匯率（team_settings／Supabase）— **未做，延後**；C6 僅本機 reference

#### 其他仍擱置
- [ ] C3 成員管理：需穩定 Supabase 登入；本代理期不開
- [ ] B18 真人密度驗收：老闆約同事 3 分鐘掃視（另排）
- [ ] migration 026（system_prompt_versions）：可選、C2 不依賴；回家可貼 SQL 或繼續延後

### 下一包建議（給老闆／下一指揮）
1. **優先**：環境恢復後一次做完上表 C2＋C6 實機勾選（約 20 分）。
2. 功能線：C3（需登入）或 C4 商品庫彈窗；或 B18。
3. Fable 額度恢復 → 交回正式總指揮；先讀本檔本節即可接。

---

## 2026-07-13｜Grok 代理指揮：下一包裁決 **C4 商品庫**

### 決策（老闆授權代理總指揮自決；Fable 恢復前繼續代理）

**現況一句話**：C1／C2／C6 程式完成；實機畫面（C2＋C6）累積待測；Phase C 剩 C3／C4／C5。

**下一包唯一工項：C4 商品庫改上方 Bar 彈窗。**

| 候選 | 裁決 | 理由 |
|---|---|---|
| C3 成員管理 | **暫緩** | 需穩定 Supabase 登入＋Admin 邀請/停權；單人作業現階段價值低；外部網路曾擋登入 |
| C4 商品庫 | **開工** | 日常找商品／修文案價值高；資料已有 `product_drafts`；計畫＝頂欄彈窗非獨立頁；可沿用佇列查詢模式；**本包目標不新增 SQL** |
| C5 發布紀錄 | **暫緩** | `/records` 仍死佔位；尚無完整「批次發布日誌」表／重跑管線，硬做易空殼或逼 SQL；宜在發布軌跡更清楚後再做 |
| Phase D | 不做 | 圖片管線／Make／Shopify Files 體量大，不插隊 |
| B18／BX-P | 不做 | B18＝老闆約人肉眼；BX-P 老闆定案延後 |

**對老闆白話**：先做「頂欄一鍵開商品庫、搜尋、進去改文案／看誰上架」；成員管理跟發布紀錄頁先放一旁，等登入穩定與資料更齊再做。

### 發包狀態

- 工人：先交 **C4 設計方案**（含現況盤點＋裁決點），**確認前不寫 code**。
- 實機驗收：仍沿用「待老闆實機累積清單」；C4 完成後核帳同樣只做 git／typecheck／抽查，畫面測登記累積。
- 邊界：不 push／不 deploy；不碰 C3／C5／BX-P；SQL 僅在工人證明「沒表就做不了」時才產檔，預設零 SQL。
### C4 設計方案裁決（Grok 代理指揮；可動工）

工人提交 C4「商品庫 Bar 彈窗」設計。貼文在 Q2 中段被截斷，但 Q1 全文＋Q2 建議方向＋「建議預設全 A」＋功能／保留現狀已足夠裁決。

**整體：OK，放行實作。**

| 題 | 裁決 | 理由 |
|---|---|---|
| Q1 列表範圍 | **A** | 使用者 session＋現有 RLS；與佇列一致；零 SQL。不做 B（service 放寬）。不做 C（未發布也要找得到才能修文案） |
| Q2 上架人名字 | **A** | 有名字顯示；解析不到「成員」或短 ID；不假造人名。admin 本可讀 profiles；operator 在 Q1-A 下多半只見自己。B（為顯示名改 RLS／service）本包不做 |
| Q3 佇列 vs 商品庫 | **A（採工人定案表）** | 佇列＝流水線批次；商品庫＝頂欄快找＋深連結；本包無批次；佇列頁不刪不降級 |
| Q4～Q6 | **全 A（工人建議）** | 貼文截斷未見選項全文；工人自標「建議全 A」。若某條 A 會逼 SQL 或 BX-P，停工回報，不得擅自改 B/C |

**補充硬性要求**
1. 入口：登入後頂欄 `.hdr-btn`「商品庫」；手機走既有頂欄工具收合；非獨立頁、非 tabbar 第五格；設定入口差異 17 不動。
2. Modal：B11 `.modal-overlay`／`.modal-box`；<960 底部抽屜；Esc／遮罩關閉。
3. 搜尋：title_zh／原標／淘寶標／ip／character／可含 shopify id；誠實空狀態；無假商品。
4. 操作：編輯文案→`/drafts/[id]`；編輯圖片→`/drafts/[id]?focus=images` 捲到圖片區；不重做 Phase D。
5. meta：狀態用 `.schip`／既有 StatusBadge；時間 published_at 優先否則 created_at 並標語意；縮圖有則顯示。
6. 預設列表排除 archived；本包可不做「含封存」開關。
7. 零 migration；不 push／deploy；BX-P 不順手美化；每步 typecheck＋b15-style-probe。
8. 文件：清單打勾 C4＋驗收步驟；差異備忘補定案差（RLS 範圍、無批次、深連結等）。
9. 收工後畫面測進「待老闆實機累積清單」；指揮只核帳抽查。

**發包**：工人可動工 C4。

### C4 全文補裁（Q2–Q6 原文已從工人 session 對齊）

貼文再次被通道截斷，但工人視窗 chat_history 有完整方案。補齊選項定義如下，**仍全 A**：

| 題 | 裁決 | 選項定義（工人原文） |
|---|---|---|
| Q1 | A | session＋RLS；operator 自己／admin 全部 |
| Q2 | A | 有名顯示；否則「成員」／短 ID；不做 service 對名 API（B）、不 migration 放寬 profiles（C） |
| Q3 | **A** | 預設已發布側：`status in (active_published, draft_created, csv_ready)`，非 archived；**不是**「全部未封存」（那是 B） |
| Q4 | A | 深連結 `?focus=images` 捲動；不嵌圖列表、不做 D 管線 |
| Q5 | A | 開窗載約 100～200 筆 → **前端過濾**；本包不做 `/api/library?q=` |
| Q6 | A | 手機與桌機同一顆，只在頂欄工具選單；**不**加「更多」抽屜列 |

對前次發包指令的修正：先前寫「預設排除 archived」過寬，應改為 **Q3-A 三狀態白名單**。可選「含進行中」開關本包可不做（有空再加；不做不擋驗收）。

### C4 收工核帳（Grok 代理指揮；程式通過，實機畫面未測）

**Git 對帳**
- commits 齊：`443dc7b` feat(C4) → `548728a` docs(C4)
- 工作區無 C4 未提交改動；僅未追蹤指揮文件 `總指揮手冊.md`／`總指揮代理紀錄.md`

**指揮複跑**
- `npm run typecheck`：綠燈
- nav／MobileTabbar：無「商品庫」（Q6-A 不進更多／不進四格）— 抽查 OK

**程式碼抽查（通過）**
- Q1-A：browser Supabase session + `.in(status, LIBRARY_STATUSES)`；無 service role 放寬
- Q2-A：`libraryCreatorLabel` 有名／「成員 · 短ID」；profiles 可讀才解析
- Q3-A：僅 `active_published|draft_created|csv_ready`；limit 150（落在 100–200）
- Q4-A：`?focus=images` + `#draft-images` + `DraftFocusScroll`；無嵌圖編輯／無 D 管線
- Q5-A：`filterLibraryRows` 前端 token 過濾；無 `/api/library`
- Q6-A：入口僅 `HeaderControls` 登入後 `.hdr-btn`（手機 ☰ 同一顆）
- 無批次操作；誠實空狀態／登入錯誤；CSS layout-only + tokens
- 文件：C4 打勾、差異 **19**、驗收步驟齊

**裁定**：C4 **程式核帳通過**。畫面測進累積清單。

### 待老闆實機累積清單 — 追加 C4

#### C4 商品庫（需登入；約 8–10 分）— 見施工清單「C4 驗收步驟清單」
- [ ] 登入後頂欄「🔍 商品庫」開 Modal；×／遮罩／Esc 可關；未登入不出現
- [ ] 手機：在 ☰ 工具選單；**更多抽屜沒有**商品庫；彈窗底部抽屜
- [ ] 列表僅三狀態；搜尋可用；0 筆／斷線誠實；**無批次**
- [ ] 編輯文案 → 草稿頁；編輯圖片 → `?focus=images` 捲到圖片上傳
- [ ] 佇列仍在；頂欄無設定齒輪
- [ ] 三主題＋375／960 肉眼（可與 C2 同趟）

### 下一包建議（核帳後）
1. **優先**：登入穩定後一次勾 **C2＋C4＋C6** 實機（約 25–30 分）。
2. 功能線：仍暫緩 C3（要穩定登入＋邀請流）；C5 等發布軌跡更清楚再做。
3. 若老闆要立刻再開工人包：先跟指揮說；預設不自動開 C3。

---

## 2026-07-13｜Grok 代理總指揮：全專案路線裁決（代 Fable）

### 現況一句話
A＋B 主幹完成；Phase C 殼 **單人可用版已齊**（C1／C2／C4／C6）；真缺口在 **Phase D 圖片第二段產線** 與 **老闆實機／部署**，不是成員管理。

### 進度粗估（給老闆）
| 階段 | 狀態 | 白話 |
|---|---|---|
| A 後端引擎 | ~95% | 真發布／文案／Vision 主幹在；剩 A19/A20/A25 等效能與 CSV 邊角 |
| B 前台主流程 | ~95% | 工作檯可用；B18 真人驗收、BX 加分未做 |
| C App 殼 | ~85% | 導覽／設定／商品庫／匯率好了；C3 成員、C5 完整紀錄未做 |
| D 圖片自動化 | 0% | **下一座大山；日常產能瓶頸在這** |
| E 儀表板 | 0% | 等有穩定量產與成本資料再划算 |
| F/G 選品爬蟲 | 0% | 最後甜點，現在開＝分心 |

### 戰略原則（拍板）
1. **單人店主優先**：能每天多上幾件、少手忙，比「邀請同事帳號」重要。
2. **殼夠用就關 Phase C**：不再為湊齊 C3／完整 C5 拖延進 D。
3. **不空殼充數**：C5 完整版依賴 D7 批次發布日誌；硬做會空。
4. **實機債要還，但不無限擋 coding**：能登入後 30 分一次勾完；登入不穩時可寫 D 的純程式骨架。
5. **BX-P／風格打磨繼續凍**到約 80%（老闆原定）。
6. **不 push／不 deploy** 除非老闆明確同意；分支已 ahead 很多，deploy 前要過金鑰輪換關卡（手冊既有）。

### 明確暫緩（直到條件成立）
| 項目 | 條件 |
|---|---|
| **C3 成員管理** | 第二位上架人手要進場，**或** 登入長期穩定且你明確要多人 |
| **C5 完整發布紀錄**（批次卡＋失敗重跑） | Phase D 有真實批次寫入後（約 D7 前後）再做完整版 |
| **BX-P 打磨** | 約 80% 功能後，你累積截圖一次交 |
| **B18 真人 3 分鐘** | 你約同事；非 coding 包 |
| **E 儀表板** | D 主幹能跑、有一定上架量後 |
| **F/G 爬蟲** | E 之後；要 VPS 錢與維護心智 |

### 建議路徑（有序）

**第 0 步｜老闆 25–40 分（最高優先，不需工人）**  
回家／穩定網路一次勾：
- 登入是否正常  
- **C2 設定**＋**C4 商品庫**＋**C6 匯率**（見累積清單）  
- 順便：開一件草稿確認工作檯沒掛  

**第 1 步｜下一包 coding（指揮預設開工方向）＝ Phase D 開場，不是 C3**  
建議包名：**D0／D3 先行**（名稱可調，精神如下）  
- **D3**：sharp 單圖處理端點（裁切／方形／WebP）——可單測、價值高  
- **D1 骨架**：圖床流向註解＋必要 API 殼（Supabase 暫存 → 處理 → 未來 Shopify Files）；本包可不逼完整 Files 上線  
- **明確不做**：Make 全 Scenario、Email/LINE、完整圖審 UX（那是下一～兩包）、C3、BX-P  
- 若 sharp／圖床環境在本機難測：允許誠實降級，但程式與驗收腳本要在  

**第 2 步｜D 中段**  
D5 圖審頁 UI（可先吃既有 image_status／批次）→ D2 Make 接線 → D6 通知 → D7 發布批次 → 回頭補 **C5** 吃真實資料  

**第 3 步｜收尾殼與加分**  
C5 完整、E1 今日待辦、A25 CSV、A19 縮圖策略；BX 與 B18 穿插  

### 可選支線（僅當第 1 步被擋）
若 sharp／OpenAI Image／Storage 權限短期弄不起來，備案 **C5-lite**（一包）：  
`/records` 用 `product_drafts` 已發布列當「個人發布軌跡」，深連結商品庫／草稿；**無**失敗重跑、無批次卡謊造。標差異備忘「lite 非 Mockup 完整版」。  
**預設不走這條**，除非 D 開場卡死。

### 對老闆白話決策
- **不要**現在做成員管理。  
- **不要**先做漂亮儀表板或爬蟲。  
- **要**先確認家裡登入與 C2／C4／C6 畫面。  
- **接著**工人開 **圖片自動處理（Phase D 開頭）**——這才是「貼圖→審圖→上架」承諾裡最大的洞。  

### 執行狀態
- 本裁決即時生效；Fable 恢復後以本節為預設路線，可覆核不可無故打散。  
- 老闆若回「繼續開工」且未指定項目 → 代理指揮發 **D3＋D1 骨架** 工人開場指令。  
- 老闆若回「我先只測驗收」→ 不開工人，只維護累積清單。

### 發包｜Phase D 開場（D3 sharp + D1 骨架）— 2026-07-13

老闆：「繼續開工，能實機會再說。」  
→ 不開 C3；發 **D-open：D3＋D1 骨架** 工人包。  
狀態：工人先交設計方案，確認後才動 code。

### D-open 設計方案裁決（Grok 代理指揮；可動工）

工人方案完整（自 session 對齊 Q1–Q6）。**整體 OK。**

| 題 | 裁決 | 理由 |
|---|---|---|
| Q1 方形化 | **A** | 預設等比長邊≤2048；不強制 1:1 裁主體；`square:true` 可選參數可留 |
| Q2 寫回哪 | **A** | WebP 回存 Supabase processed 路徑；`storage: supabase_temp`；不做假 CDN、本包不做真 Files |
| Q3 image_status | **A** | 成功用既有 `done`；零 SQL；註解標 D5 語意後續 |
| Q4 de_text/regen | **A** | skip＋理由；不跑 sharp、不改 URL |
| Q5 B14 自動 sharp | **A** | 不改送圖；手動／腳本／未來 Make 才打 API |
| Q6 單圖路由 | **A** | 只做 `POST /api/images/sharp-batch`；單圖用 imageIds 一張 |

**硬性要求**
1. 裝 sharp + pnpm 允許 native build + next `serverExternalPackages`；禁止 client import
2. runtime nodejs；不收 multipart；無 SSRF（只 fetch 該 draft 的 original_file_url）
3. 雙 auth：worker Bearer 或登入 canOperate
4. 單次 1 draft、≤12 張；EXIF rotate；WebP q≈82
5. 失敗不塞假 processed；finalize 殼 501／not_implemented
6. 零 migration；不 push；verify-d3 無網 fixture；b15 可 skip 並註明
7. 文件：清單 D3／D1 骨架進度；差異備忘若有定案差補一條

**發包**：工人可動工 D-open。

### D-open 收工核帳（Grok 代理指揮；程式通過）

**Git**：`f14b3e9` → `193a60e` → `3dd570f` → `d73acbc` 齊；未 push。

**複跑**：typecheck 綠；`node scripts/verify-d3-sharp.mjs` ALL passed（含 live sharp）。

**抽查通過**
- sharp 僅 `sharpProcess.ts` import；client 無 sharp
- Q1-A 等比 inside 2048；Q2-A supabase_temp 寫回；Q3-A image_status done
- Q4-A de_text/regen skip；Q5-A 未改 B14；Q6-A 僅 sharp-batch
- 雙 auth；finalize 501；零 migration；差異 20

**待實機**：登入後 POST sharp-batch 真圖、Storage 見 .webp（累積清單追加）。

**下一包裁決（預設）**：**D5 圖審頁**（吃 done + processed 預覽）。  
D1 真 Files 需 write_files＋實機 Shopify，排 D5 後或與部署同段；D2 Make 再後。

### 發包｜D5 圖審頁 — 2026-07-13

老闆：「繼續開工」。依路線裁決發 **D5 圖片審核頁**。  
工人先交設計方案再動 code。不做 D1 真 Files、D2 Make、D4 AI、C3/C5、BX-P。

### D5 設計方案裁決（Grok 代理指揮；可動工）

工人方案完整（Q1–Q7）。**整體 OK，放行。**

| 題 | 裁決 | 理由 |
|---|---|---|
| Q1 圖審通過 | **A** | `image_status` 維持 `done`；`image_flags.image_review=approved`（可加 reviewed_at ISO）；零 SQL |
| Q2 拒絕後 status | **A** | → `failed` + warnings「圖審拒絕：…」；不清 processed |
| Q3 列表範圍 | **A** | 卡：待審 done + processing + failed；確認鈕只待審 |
| Q4 一鍵確認 | **A** | 硬擋未展開 viewed；session 記憶可重整重算 |
| Q5 誰可操作 | **A** | canOperate；operator 僅自己；admin 可全部 |
| Q6 範圍下拉 | **A** | 僅 admin 顯示「我的／全部」 |
| Q7 卡上重跑 sharp | **A** | 本包不做；拒絕後靠既有 sharp-batch／未來 D2 |

**硬性**
1. 確認／拒絕走 server API + service role（image_status 敏感；flags 也統一由 API 合併寫入，勿整包覆寫洗掉其他 keys）
2. 不挪用 reviewed_at／reviewed_by（文案審核）
3. 滑桿標「處理後（暫存）」；不寫 CDN；detail 不進審
4. 無假 ETA／Email；零 migration；差異 21；typecheck＋b15 每 commit
5. 不做 D1/D2/D4/D6/C3/C5/BX-P

**發包**：工人可動工 D5。

### D5 收工核帳（Grok 代理指揮；程式通過）

**Git**：`0aa74da` → `6aacac7` 齊；未 push。

**複跑**：typecheck 綠；`verify-d5-image-review.mjs` ALL passed。

**抽查通過**
- /review 非 ComingSoon；Q1–Q7-A 對齊
- flags merge／clear；confirm 不碰 draft.status／reviewed_at
- reject → failed + warnings；保留 processed
- viewed 硬擋 batch；admin 下拉；無重跑 sharp 鈕
- 滑桿「處理後（暫存）」；差異 21；CSS tokens

**待實機**：登入＋至少一筆 sharp 後 done 的草稿走圖審清單。

**下一包預設**：**D1 真 Shopify Files**（finalize 實作 stagedUploadsCreate→fileCreate）。  
D2 Make 次之（接 B14→sharp→finalize）。

### 發包｜D1 真 Shopify Files（finalize 實作）— 2026-07-13

老闆：「繼續下一包開工，跑完交回 Fable。」  
→ 發 **D1 真 Files**：實作 `filesUpload` + `POST /api/images/finalize` 真 stagedUploadsCreate→上傳→fileCreate→寫 `cdn.shopify.com` 進 `processed_file_url`。  
不做 D2 Make、D4、D5 大改、C3、清暫存全自動 Cron（可註解預留）。  
**本包收工核帳後**：代理指揮結束，交回 Fable 總指揮（老闆指定）。

### D1 設計方案裁決（Grok 代理指揮；可動工）

**整體 OK。** Q1–Q5 全 **A**。

| 題 | 裁決 | 理由 |
|---|---|---|
| Q1 finalize 內 sharp | **A** | 不跑 sharp；只上傳 processed／fallback original |
| Q2 CDN 未 READY | **A** | 短輪詢最多約 5×~800ms；無 URL 則 fail、不寫假 CDN |
| Q3 重試 | **A** | 同張最多 1 次網路 retry；權限錯不重試 |
| Q4 刪 temp | **A** | best-effort 只刪該張 …/processed/{id}.webp；不刪 original |
| Q5 類型／時機 | **A** | 只 main+variant；spec/detail skip；不強制圖審通過 |

**Q5-extra**：本包 **不改** D5「處理後（暫存）」文案（文件註明 finalize 後可能已是 CDN）。

**硬性**：雙 auth；≤12；序列上傳；idempotent CDN skip；flags merge only；零 migration；verify live skip 不假綠；差異 22。

**發包**：工人可動工 D1。收工後代理指揮做最後核帳，再交回 Fable。

### D1 收工核帳（Grok 代理指揮；程式通過）＋交回 Fable

**Git**：`16ff974` → `4c4a242` 齊；未 push／deploy。

**複跑**：typecheck 綠；`verify-d1-files` ALL passed（live SKIP 誠實）；`verify-d3-sharp` 仍綠。

**抽查通過**
- filesUpload 真 staged→multipart→fileCreate→CDN 輪詢；無 NOT_IMPLEMENTED 永久 stub
- finalize 雙 auth、≤12、不跑 sharp；main+variant only；idempotent CDN skip
- 失敗不塞假 CDN；best-effort 刪 processed temp path 安全檢查
- 不碰 image_flags（D5 安全）
- 差異 22／清單 D1 真 CDN＋清 processed temp

**待實機**：sharp-batch → finalize → Shopify 後台 Files + DB CDN URL。

### 代理指揮結束｜交回 Fable（老闆指定）

**交棒摘要（給 Fable）**  
> ⚠ **歷史段落**（D1 核帳後曾短暫交棒）。**勿當現況**。  
> **現況以 2026-07-13 夜「D4 正式核帳完成＋代理總指揮交接狀態」為準**（D1–D5 已過；代理續至週五早）。

1. （當下）A/B 主幹；C1/C2/C4/C6；D3／D5／D1 真 Files — 之後已接 D2-open＋D4。
2. （當下未做、後已補）D2／D4；仍未做：D6 通知、D7、D8…
3. 指揮文件：`docs/總指揮手冊.md`、本檔。

（原句「Grok 代理總指揮任務到此結束」已作廢——老闆指定續代至週五早。）

---

## 2026-07-13 晚｜老闆回家後進度＋Grok 續代至週五早上

### 老闆回報（已記錄）
1. **Supabase migrations 自查全 TRUE**（含 018–025 等關鍵欄位／表；026 可選若也 true 一併視為有跑）。
   消解先前「手冊寫 014–025 全跑 vs 待確認清單仍標待執行」的文件矛盾——**以老闆 SQL 查詢為準：DB 已齊**。
2. **Vercel 環境變數已補**（含 `CRON_SECRET` 等）。
3. 先前 **git push 成功**（origin 已同步）；部署以 Vercel Deployments 為準。
4. **Fable 額度週五早上才恢復** → 老闆指定 **Grok 繼續代理總指揮至週五早上**，再交回 Fable。

### 代理期邊界（至週五早上）
- 續行指揮循環：方案裁決、發包、核帳、進度筆記。
- 不 push／deploy 除非老闆再明確要求。
- 實機畫面仍以老闆為主；指揮可複跑 typecheck／腳本。

### 下一包裁決
**開工：D2-open（送圖→處理自動鏈，Make 可選）**

理由：
- D3／D1／D5 已齊；最大缺口是「按送圖後仍要手動／腳本打 sharp＋finalize」。
- 完整 Make Scenario 需老闆在 Make.com 建流程，易卡在帳號／額度教學。
- 本包以 **伺服器端可選自動鏈** 為主：B14 送圖成功後，對 keep 圖觸發 sharp-batch（可再接 finalize）；
  `MAKE_WEBHOOK_URL` 若有設則一併通知 Make（預留官方 Scenario），無則不擋主流程。
- 不做 D4 AI、D6 通知全文、C3、BX-P。

**不建議本包硬做**：完整 Make 節點逐步教學（可文件化給老闆另開）；D6 Email/LINE。

### 文件對帳修正（老闆要求：自查全 TRUE 必須寫死，防再不同步）

- 已在 `docs/施工清單.md` 新增 **「Supabase migration 狀態」** 唯一真相節：
  2026-07-13 晚老闆 SQL 自查全 TRUE → 001–025 已生效；026 可選。
- 待確認清單中 **017–025／016／Vercel env 待補** 等舊「待執行」條已全部劃到 **已解決**。
- 殘留待確認僅：內容型（A21-3 專區 URL 要老闆填值）、OpenAI 模型策略、SHOPIFY_LOCATION_ID 建議。
- `docs/總指揮手冊.md` 同步：以施工清單該節為準，禁止再寫「可能沒跑 migration」。

### D2-open 設計方案裁決（Grok 代理；可動工）

老闆在家；方案完整。**整體 OK。**

| 題 | 裁決 | 理由 |
|---|---|---|
| Q1 何時 auto sharp | **A** | 整件 pipeline 皆 keep 才 sharp；混標整件不自動，等 D4 |
| Q2 auto finalize | **A** | 預設開：全 keep 且 sharp 至少 1 成功 → finalize；無 Shopify 誠實 fail/warn |
| Q3 webhook 時機 | **A** | batch 建立後立刻一封（含 chain 摘要）；無 URL 不報錯；不做完成信 |
| Q4 逾時 | **A** | maxDuration 60；序向；剩餘 <8s 停；未跑維持 queued；不 HTTP 自打 |
| Q5a 混標 batch | **A** | 整批維持 `queued` |
| Q5b warnings | **A** | 失敗才寫 draft warnings 短句 |
| Q6 UI | **A** | 只改 API message；b15 跳過 |

硬性：內聚函式不 fetch self；零 migration；差異 23 覆寫差異 20「不自動 sharp」；Make 最短文件可選。

**發包**：工人可動工 D2-open。

### D2-open 收工核帳（Grok 代理；程式通過）

**Git**：`f790fd8` → `8a6fae3` 齊；本地 ahead origin 2（未 push，待老闆同意）。

**複跑**：typecheck 綠；verify-d2-auto-chain／verify-b14／verify-d3／verify-d1 全過。

**抽查通過**
- Q1-A decideDraftAutoChainFromSnapshot 全 keep → run；de_text/regen → awaiting_d4
- Q2-A sharp 成功後 finalize 內聚；無 HTTP self-fetch
- Q3-A notifyMake image_batch_submitted；無 URL 不炸
- Q4-A 60s／8s budget helpers
- Q5a awaiting_d4 batch → queued；Q5b warnings merge
- Q6 message only；差異 23 覆寫差異 20

**待實機**：全 keep 送圖看 CDN／圖審；含去字送圖應 queued + awaiting_d4 訊息。

**下一包建議**（週五 Fable 前）：優先 **實機驗收 D2**；coding 若續做則 **D4 AI 去字／重生**（混標才完整）。D6 通知可後。

### 發包｜D4 AI 去字／重生 — 2026-07-13 晚

老闆：「開 D4 工人指令」。Grok 代理至週五早。

**範圍精神**：接上 D2 的 awaiting_d4 缺口；de_text／regenerate 真跑 Image API。
長等待（20–60s）文件傾向 Make 直呼；本包可提「Vercel 同步／worker 路由／Make 可選」方案供裁決。
不做 D6 全文、C3、BX-P；零 migration 預設。

### D4 設計方案裁決（Grok 代理；可動工）

| 題 | 裁決 | 理由 |
|---|---|---|
| Q1 混標接 D2 | **C Hybrid** | 必有獨立 `/api/images/ai-process`；送圖鏈僅時間夠時最多試有限張 AI，否則 awaiting_d4 |
| Q2 長等待 | **A** | Vercel 內呼 OpenAI；Make 可選打我們的 API 排程；差異備忘寫「不強制 Make 直呼 Image API」 |
| Q3 autoSharp/Finalize | **A** | AI 成功後預設 sharp+finalize；失敗不進 finalize |
| Q4 generated 寫入 | **A** | AI 成功寫 generated_file_url；sharp/finalize 只動 processed_* |
| Q5 無 description | **A** | 標題拼 prompt＋警告仍試 |
| Q6 batch 狀態 | **A** | D4 跑完更新 item/batch 終態 |
| Q7 張數逾時 | **A** | ≤12 候選；序向；60s／<8s 停 |
| Q8 UI | **A** | 無新 UI |
| Q9 模型 | **A** | env 可蓋；需能 edit+generate；不能 edit 則 de_text 誠實失敗 |

硬性：內聚不自打 HTTP；失敗不覆寫好 CDN；零 migration；verify-d4 mock；不 push。

**發包**：工人可動工 D4。

### D4 收工核帳（Grok 代理；程式通過）＋ push

> 初核（Codex 代寫筆記 `31df8c5`）後，**2026-07-13 夜 Grok 代理總指揮正式複核通過**。
> 工人報告寫「未 push」為收工當下狀態；其後已 push，以 origin 為準。

**Git 對帳**
- Branch：`codex/nestory-v0.1-safety-skeleton`，與 `origin` **同步**（working tree clean）。
- D2-open：`f790fd8` feat + `8a6fae3` docs
- D4：`093e76e` feat + `965d2d8` docs
- 指揮筆記：`31df8c5` docs（D4 audit pass / push note）
- Push 範圍（歷史）：`9a7f23a..965d2d8` 含 D2-open+D4；HEAD 可含後續 docs commit。

**指揮複跑（2026-07-13 夜，Grok）— 全部 exit 0**
| 項 | 結果 |
|---|---|
| `tsc --noEmit` | ✅ TYPECHECK_OK |
| `node scripts/verify-d4-ai-process.mjs` | ✅ ALL passed（mock，不打真 Image API） |
| `verify-d2-auto-chain.mjs` | ✅ ALL passed（含 hybrid `run_mixed`） |
| `verify-d3-sharp.mjs` | ✅ ALL passed |
| `verify-d1-files.mjs` | ✅ ALL passed（live Shopify SKIP，預設） |
| `verify-b14-image-batch.mjs` | ✅ ALL passed |
| b15-style-probe | 跳過（D4 無 UI） |
| migration | 零（正確） |

**裁決對帳（Q1-C／Q2–Q9 A）— 程式抽查**
| 裁決 | 證據（檔案／行為） | 裁定 |
|---|---|---|
| Q1-C Hybrid | `decideDraftAutoChainFromSnapshot`：de_text/regen → `run_mixed`；`AUTO_CHAIN_MAX_AI_IMAGES_PER_DRAFT=1`；未完 `awaiting_d4` | ✅ |
| Q2-A 長等待在 Vercel | `openai-image-provider.ts` 直呼 OpenAI；Make 不必放第二份 key；`ai-process` 獨立 API | ✅ |
| Q3-A autoSharp/Finalize | route 預設 `true`；AI 成功後 sharp `afterAi` → finalize | ✅ |
| Q4-A generated 寫入 | 成功才寫 `generated_file_url`；sharp/finalize 動 processed；失敗 omit CDN 欄 | ✅ |
| Q5-A 無 description | `buildRegeneratePrompt` 標題仍試＋warning | ✅ |
| Q6 batch 終態 | hybrid 彙總 item/batch；awaiting 維持 queued；Make 可帶 `d4` 摘要 | ✅ |
| Q7 張數／逾時 | ≤12；maxDuration 60；budget helpers；序向 | ✅ |
| Q8 無 UI | 無 components 引用 `runAiProcess` | ✅ |
| Q9 模型 env | `OPENAI_IMAGE_*`；dall-e-3 等 edit gate → de_text 誠實失敗 | ✅ |
| 硬性 | 內聚不 HTTP 自打；失敗不蓋 `shopify_cdn` processed；差異 **24** 齊 | ✅ |

**文件對帳**
- 施工清單：`[x] D4` 已勾、差異 24 註記齊
- `docs/Mockup差異備忘.md` 差異 24：✅ 完成現況
- `docs/Make接Webhook最短說明.md`：ai-process 呼叫範例已補

**裁定**：**D4 程式核帳正式通過**。真 Image API／真 CDN 實機仍屬老闆／部署環境，**不擋** D4 程式收工（與 D1/D3 同原則）。

**待實機（D4 專項，併入下方累積清單）**
1. 全 keep 送圖 → sharp → CDN → 圖審可見
2. 混標（含 de_text／regenerate）→ keep 先完成、其餘 `awaiting_d4`／可再打 `POST /api/images/ai-process`
3. 缺 key 或不能 edit 模型 → 誠實錯誤、不塞假圖、不蓋既有 CDN

**下一包建議（代理期至週五早）**
1. **優先（零 coding）**：老闆累積實機（C2/C4/C6 + D1–D5 送圖鏈）
2. **coding 若開工**：D6 批次通知（Email Resend + LINE Messaging API Flex；**勿用 LINE Notify**）
3. 暫緩：C3／C5／BX-P／E／F／G

---

## 2026-07-13 夜｜D4 正式核帳完成＋代理總指揮交接狀態

### 指揮身分（目前）
- **代理總指揮**：Grok（本對話起正式接手核帳與發包，延續 07-13 晚代理期）
- **交回時點**：使用者指定 **週五早上 Fable 額度重置後** 交回正式總指揮
- **工人／其他模型接續**：一律先讀 `docs/總指揮手冊.md` → 本檔最新節 → `docs/施工清單.md` 進度筆記頂部

### 現況一句話（2026-07-13 夜）
Phase A／B 主幹完成；C 單人殼（C1/C2/C4/C6）完成（C3/C5 暫緩）；  
Phase D **D1–D5 程式全通過核帳**（D4 今夜正式複核綠）；  
剩餘 D6 通知、D7 批次發布、D8 Showmore…；E／F／G 未開。  
DB 001–025 已齊；origin 已含 D2-open+D4；**實機債大量累積、待老闆一次勾**。

### 待老闆實機累積清單 — 追加 Phase D（D1–D5）

#### D2／D4 送圖鏈（需登入＋有標記圖片；約 15–20 分）
- [ ] **全 keep**：▶ 送圖 → message 含自動處理 → `processed_file_url` 變 Shopify CDN → `/review` 待審
- [ ] **混標**（至少 1 張 de_text 或 regenerate）：keep 先處理；未跑完 AI → `awaiting_d4`／batch 仍 queued
- [ ] **補跑 AI**：`POST /api/images/ai-process`（session 或 `WORKER_API_TOKEN`）→ generated→sharp→finalize
- [ ] 失敗路徑：拔 key 或 dall-e-3 跑 de_text → 誠實錯誤、**不**出現假 CDN、既有 CDN 不被蓋

#### D1／D3／D5（若尚未勾）
- [ ] D3 sharp 單商品：keep 出 temp WebP；detail 略過；未標記 skip
- [ ] D1 finalize：CDN URL；processed temp 盡力刪；original 保留
- [ ] D5 圖審：滑桿、確認 flags、拒絕 failed、一鍵未 viewed 硬擋（見施工清單 D5 步驟）

#### 仍累積（見較早 C2/C4/C6 節，不刪）
- C2 設定、C4 商品庫、C6 頂欄匯率、B18 真人密度

### 週五早交回 Fable 交接包（給正式總指揮）

**必讀順序**
1. `docs/總指揮手冊.md`（指揮循環）
2. 本檔：本節＋「D4 收工核帳」＋「全專案路線裁決」
3. `docs/施工清單.md`：Phase D 勾選狀態＋進度筆記頂 10 行＋「Supabase migration 狀態」
4. `docs/Mockup差異備忘.md`：差異 16–24（尤其 23 hybrid 被 24 覆寫、24 D4）
5. `git log --oneline -20` 對帳 origin

**已完成（勿重做）**
- A 主幹、B1–B17/B19、BX4、C1/C2/C4/C6
- D1 真 Files、D2-open 自動鏈、D3 sharp、**D4 AI hybrid（已核帳）**、D5 圖審

**未做／暫緩**
| 項 | 備註 |
|---|---|
| D6 | 雙通道通知：Resend Email + LINE **Messaging API** Flex（禁止 LINE Notify） |
| D7 | Make 批次發布＋發布紀錄 |
| D8/D8a/D9/D10 | Showmore／嵌圖／健檢／影片 |
| C3/C5 | 單人版暫緩 |
| BX-P | 老闆累積截圖後一次打磨，禁止零碎美化 |
| A19/A20/A25 | 效能／CSV HTML |
| E/F/G | 儀表板／選品／爬蟲 |

**風險／債**
- 實機債：上表未勾；程式綠≠真 Image API／真店 CDN 已跑
- `SHOPIFY_PUBLISH_MOCK` 等 env 以 Vercel／本機為準；指揮不讀 `.env.local` 內容
- 部署前硬關卡：金鑰輪換＋Vercel env（見總指揮手冊）
- 未同意前不 push／deploy

**建議 Fable 接任後第一動作**
1. 與老闆對「實機債要先清還是開 D6」
2. 若開 D6：先讀 `docs/自動化流程設計-2026-07-08.md`【自動·二】四種事件，工人先交設計方案
3. 核帳時複跑 `verify-d4`／`verify-d2` 確認無回歸

**代理期內若再開工**：Grok 繼續裁決／發包／核帳；每包結尾更新本檔＋施工清單進度筆記。
---

## 2026-07-13 夜｜Push 完成＋下一包裁決 **D6-open（圖片批次完成通知）**

### Push
- 已 `git push`：`31df8c5..5cf7d20`（含 D4 正式核帳文件＋歷史交棒標註）
- Branch `codex/nestory-v0.1-safety-skeleton` 與 origin **同步**

### 指揮身分
- 老闆指定：Grok **正式接手 Fable 職責**（代理總指揮至週五早，職責等同：裁決／發包／核帳／下一包決策）
- 本節起 **下一包 coding 唯一工項：D6-open**

### 下一包裁決（總指揮）

**開：D6-open｜圖片批次完成通知骨架（事件 #1）**

| 選項 | 為何不／要 |
|---|---|
| 只清實機債 | 重要，但屬老闆操作，**不能當工人 coding 包**；實機與 D6 可並行 |
| 完整 D6 四事件 | 事件 #2 需發布批次／C5；#3 需 F；#4 需 E 預算——一次做會空殼 |
| D7／D8 | 依賴發布紀錄或 Showmore 定案測試，排在通知之後較順 |
| **D6-open** | D1–D5 已齊；`image_batches` 終態已有；缺口是「批做完沒人知」——最高 ROI |

**本包必做**
1. **Notify 中心**（可換層）：同一事件 → Email（Resend）＋ LINE Messaging API Flex（**禁止 LINE Notify**）
2. **事件 #1 only**：圖片批次全部到終態（done／failed／partial 等「整批不再 queued／processing」）→ 觸發一次通知
   文案方向：「N 件圖片處理完成（失敗 M；含重生類可優先看）」＋深連結 `/review`
3. **接線點**：送圖自動鏈／`ai-process`／sharp 路徑在「可能讓 batch 變終態」時呼叫共用 `tryNotifyImageBatchIfComplete(batchId)`（冪等：同一 batch 只通知一次）
4. **每日 Cron 掃卡住批次**（>24h 未終態）→ 可寄「批次卡住」提醒（可與 #1 同通道；無 key 誠實 skip）
5. **設定／env**：`.env.example` 列 `RESEND_*`／`LINE_CHANNEL_*`／收件人；無 key → **不 500、不假寄**，log／回傳 `skipped`
6. **腳本** `scripts/verify-d6-notify.mjs`（mock，不打真 Resend/LINE）
7. **零 UI 或極小**：設定頁若已有「自動化與通知」區，可加誠實開關／說明；**BX-P 禁止打磨版面**
8. 文件：施工清單 D6 進度註記；差異備忘若定案與【自動·二】不同則補一條

**本包明確不做**
- 事件 #2 發布批次完成、#3 週選品、#4 月預算 80%（只留 type／stub 註解可）
- D7 Make 批次發布、D8 Showmore、C3/C5、假 ETA、BX-P 美化
- 不要求老闆本包立刻申請 Resend/LINE（無 key 可 mock 驗收）；有 key 再實機

**裁決預填（工人方案若對齊可少問；有衝突再提）**
| 題 | 建議預設 | 說明 |
|---|---|---|
| Q1 本包事件範圍 | **A 僅 #1＋卡住 Cron** | 見上 |
| Q2 雙通道 | **A 兩通道都接，無 key skip** | 非「有一個就擋」 |
| Q3 冪等 | **A DB 記 notified_at 或 flags** | 優先 image_batches 加欄（migration 只產檔；零 migration 用既有 JSON 需說清楚） |
| Q4 收件人 | **A env 白名單** | 例 `NOTIFY_EMAIL_TO`／`LINE_USER_ID`；全隊 profiles 延後 |
| Q5 觸發 | **A 終態時同步 tryNotify** | 不另開 Make 專責通知 |

**發包狀態**：工人先交**設計方案**；總指揮放行後再動 code。貼給工人的完整開場指令見老闆對話。

---

## 2026-07-13 夜｜D6-open 設計方案 **放行**

### 工人方案結論
- 盤點正確：025 已有 `notify_sent_at`／`stuck_notified_at`／stuck 狀態／stuck_scan 索引 → **本包零 migration**。
- 地雷正確：`partial_failed` 不可當「整批可通知」；**以 image_batch_items 全終態**（done|failed|skipped，無 queued|processing）為準。
- 架構：唯一入口 `tryNotifyImageBatchIfComplete`；NotifyCenter 雙通道；Cron stuck；不公開 batch-done API。

### 裁決表（放行）

| 題 | 裁決 | 備註 |
|---|---|---|
| Q1 事件範圍 | **A** | 僅 #1 完成＋卡住；#2–#4 stub 註解 |
| Q2 雙通道 | **A** | Resend + LINE Flex；無 key 各 skip；禁 LINE Notify |
| Q3 冪等 | **A′ 用既有欄** | 寫 `notify_sent_at`／`stuck_notified_at`；**零 migration** |
| **Q3b 無 key 是否 claim** | **B′ 不 claim** | 見下 |
| Q4 收件人 | **A** | env 白名單 |
| Q5 觸發 | **A** | 終態路徑同步 tryNotify；錯誤吞掉不影響送圖/AI 回應 |
| Q6 status=stuck | **A** | Cron 可寫 status→stuck + stuck_notified_at |
| Q7 公開 notify API | **A 不做** | 後續再薄殼 |
| Q8 設定頁 | **A 只改說明文案** | 零 CSS／零版面；BX-P |

### Q3b 細則（必守）
- **至少一通道真正 sent 成功** → 才寫 `notify_sent_at`（或 stuck 的 `stuck_notified_at`）。
- **全 skipped（無 key／未就緒）** → **不寫** claim 欄 → 日後補 key 後，終態再觸發或 Cron 補掃「items 全終態且 notify_sent_at is null」可補寄。
- **全通道 error（有 key 但 API 失敗）** → **不 claim**，允許重試；避免「假裝已寄」。
- **一成功一失敗** → **claim**（避免 Email 重複轟炸）；失敗通道寫 log／result 即可。
- claim 應用條件更新（`notify_sent_at is null`）避免競態雙寄。

### 補充硬性
1. 終態判定**只信 items**，不單信 batch.status。
2. 深連結：`APP_BASE_URL`（或 NEXT_PUBLIC_  baseless server-only）+ `/review`；無 base 用相對路徑誠實寫在信裡或 skip 深連結字串仍給 path。
3. tryNotify 失敗**永不**讓 send-images / ai-process 500。
4. verify-d6-notify.mjs mock 覆蓋：未全終態不寄、冪等、無 key skip、不 claim、有 sent 才 claim、hybrid partial items 仍 queued 不寄。
5. 回歸：typecheck + verify-d2 + verify-d4 + verify-b14 不破。
6. 文件：清單 D6 註記、差異備忘若需、`.env.example`。

**發包**：工人可動工；commit `feat(D6)`／`docs(D6)`；不 push。

---

## 2026-07-14｜D6-open 收工核帳（Grok 總指揮｜**程式通過**）

### Git
- `c5664a6` feat(D6): image batch complete notify skeleton
- `956af9e` docs(D6): mark D6-open done and add diff 25
- 本地 **ahead origin 2**（工人未 push）；working tree clean at audit start

### 指揮複跑（2026-07-14）— 全 exit 0
| 項 | 結果 |
|---|---|
| tsc --noEmit | ✅ |
| verify-d6-notify | ✅（含 terminal／partial_failed+queued 不寄／Q3b claim／無 LINE Notify） |
| verify-d2 / d4 / b14 | ✅ 不破 |
| 零 migration | ✅ |
| b15 | 跳過（僅 Settings 文案） |

### 裁決對帳
| 項 | 裁定 |
|---|---|
| 終態只信 items | ✅ itemTerminal.ts |
| Q3b B′ | ✅ shouldClaimAfterDispatch + 條件 claim notify_sent_at |
| 接線 | ✅ auto chain + ai-process safeTryNotify |
| Cron stuck | ✅ /api/cron/stuck-batches + vercel 0 17 * * * |
| 雙通道 skip | ✅ resend／lineMessaging |
| 禁 LINE Notify | ✅ |
| 差異 25／清單 [x] D6 | ✅ |

**裁定：D6-open 程式核帳通過。** 真 Resend／LINE 實機屬老闆 env，不擋收工。

### 下一包建議（總指揮）
1. **優先（零 coding）**：老闆實機債（送圖鏈＋有 key 真通知）
2. **coding**：**D7-open** 批次發布限速＋發布紀錄骨架（接事件 #2 資料面；C5 可同包或薄殼）
3. 暫緩：#3/#4、C3、BX-P、D8 可後

未 push D6 程式：等老闆一句「可以推」。

---

## 2026-07-14｜產品裁決：商品佇列 IA 收斂（小包 Q-slim）＋下一包 D7

### 老闆提問（白話）
佇列若不是批次發布紀錄，跟工作檯結果卡很像；只加不減下是否仍該留？有沒有讓流程更好？
若沒有 → 規劃小包，並開下一包。

### 規劃者立場（總指揮代 Fable；拋開「只加不減」口號看資訊架構）

**結論：保留「全部草稿清單」能力，但不要再當一級主航（尤其手機四格之一）。**

| 面向 | 判斷 |
|---|---|
| 跟工作檯重疊？ | **高**。兩邊都有階段篩選、批次核准／發布／封存、進單件。工作檯結果卡資訊更密（文案／圖標記）。 |
| 佇列獨有價值？ | **有，但窄**：(1) 跨 session 找回舊草稿 (2) 輕列表掃 100 筆 (3) 不開工作檯也能批次收尾。這是「檔案櫃」不是「第二工作檯」。 |
| 讓流程更好？ | **當主 tab 時幾乎沒有**——單人店主一天多數時間在「新增→生成→審卡」；多一個像工作檯的入口＝分心與「我該點哪」。 |
| Mockup／只加不減 | 「只加不減」是**功能不降級**，不是**導航永遠多一格**。檔案櫃可併進工作檯入口或「更多」，能力保留即可。 |
| 發布紀錄？ | 佇列 **≠** C5／D7 發布紀錄。後者管「批次匯出／上架結果／失敗重跑」；前者管「草稿生命週期」。兩者都要，但 **UI 不該長成兩個半吊子列表**。 |

**會不會刪掉 `/drafts`？**  
**不刪路由、不砍批次 API。** 砍的是「一級入口與心智模型重複」，不是後端能力。

### 小包規劃：**Q-slim｜佇列降級為檔案櫃**（1 session，約半包～1 包）

**目標**：導航不再把佇列當第二工作檯；能力可達。

| 步驟 | 做什麼 |
|---|---|
| 1 | 手機四格：`佇列` 移出 primary → 進「更多」（空出的一格預留給 C5 發布紀錄，或暫「更多」三＋設定；**本包不硬做 C5**） |
| 2 | 桌機側欄：佇列改文案「全部草稿」或移到主區下方次要位；預設仍可進 |
| 3 | 工作檯結果區頂加一條次要連結／按鈕：「查看全部草稿」→ `/drafts`（空狀態也顯示） |
| 4 | `/drafts` 頁標題改「全部草稿（檔案櫃）」＋一行說明：跨日找回／批次收尾；**不重做列表 UI**（BX-P 不碰） |
| 5 | 差異備忘補一條；nav.ts／MobileTabbar／AppSidebar／施工清單註記 |
| 明確不做 | 刪 API、刪封存、合併 ResultCard 大重構、C5 全文、BX-P 美化 |

**驗收**：手機看不到主 tab「佇列」；更多裡進得去；工作檯一鍵到列表；批次仍可用。

**排程**：可插在 D7 前或後。老闆若嫌導航吵 → **D7 前先做 Q-slim**；若急發布紀錄 → D7 先、Q-slim 緊接。

### 本回合老闆方向解讀
「規劃進小包，然後做下一包」→ **Q-slim 入清單備工**；**下一包 coding 開工 D7-open**（批次發布＋發布紀錄骨架）。Q-slim 不與 D7 搶同一工人 session，除非老闆改口先 Q-slim。

### 下一包：**D7-open**（預設）
- Make／伺服器批次發布限速（~600ms）、發布結果可追
- C5 發布紀錄頁至少吃得到「有資料的批次卡」骨架（可與 D7 同包或 D7 後立刻 C5）
- 不做 D8 Showmore 大改、不做完整事件 #2 通知（可留 hook）
- 工人先設計方案


---

## 2026-07-14｜老闆澄清佇列爭議＋最小導覽改動＋續 D7

### 老闆產品觀點（記給 Fable，**未結案**）
- 已有批次**發布紀錄**規劃（C5／D7）時，再留一份「草稿佇列」易與工作檯結果卡重複。
- **前端使用者角度**：多一頁＝流程變複雜；資訊應集中在工作檯**卡片區**。
- 若佇列要留，較有意義的形態可能是：**連動 Shopify 後台尚未上架的草稿**，從此發布或退回修改——否則意義弱。
- 指揮／Fable 分歧：先前 Q-slim 主張「檔案櫃降級保留」；老闆傾向「沒意義就不要一級頁」。
- **裁決狀態**：完整刪／併／Shopify 連動 **等 Fable 回來討論**；本回合不當最終產品結案。

### 本回合已執行（稱職指揮：老闆允許的最小改動）
- 導覽對齊 Mockup 主線：佇列／全部草稿**移後**（側欄墊底、手機出主 tab 進更多、設定前）。
- 文案「商品佇列」→「全部草稿」；差異 **26**。
- **不做**整頁刪除、不做 Shopify 草稿連動、不開 Q-slim 全包。

### 下一包
仍 **D7-open**（跳過佇列產品大辯論）。工人指令見老闆對話。

---

## 2026-07-14｜D7-open 設計方案 **放行**

### 方案結論
盤點正確：batch/publish 無間隔連發；publish_jobs 非批次卡；/records ComingSoon；publish 與 image_batches 必須分表。
架構對齊 D2：伺服器內聚 `runPublishBatch`、600ms gap、maxDuration 60／&lt;8s 停、Make 可選、route 薄殼。

### 裁決表

| 題 | 裁決 | 備註 |
|---|---|---|
| Q1 整體架構 | **A** | 新表 publish_batches＋items；內聚限速；C5-lite /records |
| Q2 時間預算未跑完 | **A 標 skipped** | reason=`time_budget`；batch 可進終態（partial）；未發草稿維持可再發。**勿**長期留 processing/queued 整批卡死 |
| Q3 重跑失敗 | **A-lite** | 紀錄頁可對「有失敗」批次顯示「重送失敗件」→ **新建** publish batch（呼叫同一 runPublishBatch），**不**改寫舊 batch 歷史 |
| Q4 單件也建批 | **A** | 比照 B14 1A；1 件也有 batch 卡 |
| Q5 C5 勾選 | **A** | D7 [x]；C5 維持 [ ] 並註「骨架已接 D7；多 kind／商品庫更新／完整 Mockup 未做」 |
| Q6 角色 | **維持** | canPublish=admin\|reviewer 不動；operator 403 另開小 fix 不塞本包 |

### 硬性補充
1. 600ms 在 **server 件間**；env `PUBLISH_ITEM_GAP_MS` 可覆寫，預設 600。
2. 禁止 HTTP 自打 publish；`publishDraft` 不重寫 GraphQL。
3. 027 **只產檔**；UI 在表不存在時誠實提示「請執行 migration 027」，不假列表。
4. 事件 #2：欄位 `notify_sent_at` + type stub；**不真寄**。
5. 可選 Make `publish_batch_submitted`；失敗不 500。
6. 建議 `current_publish_batch_id` 做（debug 友善）。
7. 樣式：D5／現站 tokens、.schip、pills；BX-P 禁止；差異 **27**。
8. verify-d7-publish-batch.mjs + typecheck + b15；不 push。
9. 本包不改 nav／佇列／D8／清暫存圖。

**發包**：工人可動 code。commit `feat(D7)`／`docs(D7)`。

---

## 2026-07-14｜D7-open 收工核帳（Grok 總指揮｜**程式通過**）

### Git
- `eecea5f` feat(D7): rate-limited publish batches + records skeleton
- `cba6d45` docs(D7): mark done, diff 27, acceptance checklist
- 核帳時本地 ahead origin 2（工人未 push）

### 指揮複跑 — 全綠
| 項 | 結果 |
|---|---|
| tsc --noEmit | ✅ |
| verify-d7-publish-batch.mjs | ✅ ALL passed |
| b15-style-probe | ✅ 三主題正常 |

### 裁決對帳
| 項 | 裁定 |
|---|---|
| Q1 內聚限速＋分表 | ✅ runPublishBatch + 027 |
| Q2 time_budget → skipped | ✅ 腳本＋註解 |
| Q3 A-lite 重送新建批 | ✅ PublishRecordsPanel |
| Q4 單件也建批 | ✅ [id]/publish → runPublishBatch |
| Q5 C5 不勾滿 | ✅ 清單註記 |
| Q6 角色 | ✅ canPublish 未改 |
| 差異 27 | ✅ |
| ComingSoon 移除 | ✅ records/page |

**裁定：D7-open 程式核帳通過。** 027 SQL 與真店發布屬老闆實機。

### 老闆待辦
1. Supabase 貼 027_publish_batches.sql
2. 1～2 件核准並建草稿 → /records 見卡

### 下一包建議
1. 清實機債（027＋D7 發布＋累積 C2/D 圖鏈）
2. coding：D8 Showmore 或 事件 #2 通知接 publish_batches
3. 佇列去留仍等 Fable

---

## 2026-07-14｜延後統一實機＋下一包 **D8-open**

### 老闆指示
- **不**現在逐包實機；「沒有實績跑過的」先記帳。
- 做到一個階段後 **統一測完整項目**。
- 先直接開下一包 coding。

### 累積實機債（統一驗收清單｜指揮不可當「已驗過」）

> 測完一項再勾。本表為**唯一待測總帳**（含先前散落項）。

#### 環境／SQL
- [ ] migration **027** publish_batches（D7 紀錄頁前提）
- [ ] 確認 001–026 仍全 TRUE（若環境有換專案）
- [ ] Vercel env：Shopify／OpenAI／TAVILY／CRON／可選 RESEND／LINE／APP_BASE_URL

#### C 殼／設定
- [ ] C2 設定入口、定價雙向、權限
- [ ] C4 商品庫 Modal
- [ ] C6 頂欄匯率（套用中 vs 今日參考）
- [ ] 導覽差異 26（全部草稿在後／手機主 tab）

#### 上架主線（文案）
- [ ] 工作檯：生成→結果卡→核准
- [ ] B3 截圖辨識；網址僅查重（爬蟲仍停用）

#### 圖管線 D1–D5
- [ ] 全 keep 送圖 → sharp → CDN → 圖審
- [ ] 混標／ai-process／awaiting_d4
- [ ] D5 確認／拒絕

#### 通知 D6
- [ ] 有 key 時圖片批完成 Email／LINE；無 key 不炸
- [ ] stuck cron（可部署後）

#### 發布 D7
- [ ] 1～2 件批次／單件發布 → `/records` 批次卡
- [ ] 失敗重送＝新批
- [ ] 件間限速體感（可看 log）

#### 佇列產品（等 Fable）
- [ ] 刪頁 vs 卡片集中 vs Shopify 草稿連動 — **未裁決**

### 下一包裁決：**D8-open｜Showmore 匯出可用化**

**理由**：老闆曾痛點「Showmore 跟 Mockup 差很多」；D7 剛齊；統一實機前先把**匯出主路徑**補到「能對齊規格、可測」比再堆通知 #2 更有價值。

**D8-open 範圍（本包；完整 D8 可拆）**
1. **加價 +5%**：匯出時套用設定頁／pricing 的 Showmore markup → 再美化尾數（一次算清；可寫 showmore 專用欄或匯出當下算——方案裡選並說明）
2. **A25 一併**：CSV Body 走 `formatPlainTextAsHtml`（與 Shopify payload 一致），修「純文字不分段」
3. **圖片欄**：決策樹 **預設案 A**（填 Shopify CDN／已有 processed 公開 URL）；案 B zip **本包只文件／按鈕 disabled 誠實**「需實測後再開」
4. **欄位對齊**：對照 Showmore 必要欄＋現有 `buildShowmoreCsv`；缺的必填給誠實預設或黃字，不假資料
5. **文案改寫版 v2 全套**（標題公式／公版尾段 team_settings 長跑）→ **本包可做「輕量」**：若已有 description 則套用結構檢查／尾段附加；**完整 LLM 改寫另標 D8b** 避免一包爆炸
6. UI：匯出入口可小改說明／加價提示；**BX-P 禁止**；可選在 `/records` 或匯出 toast 提示「非 Shopify 批次帳」
7. `scripts/verify-d8-showmore.mjs`；差異 28；027 無關

**明確不做**：D8a 嵌圖全做（可列 TODO）、D9 健檢預覽全文、D10 影片、事件 #2、Q-slim 刪頁、Fable 佇列辯論。

**發包**：工人先設計方案。

---

## 2026-07-14｜D8-open 設計方案 **放行**

### 方案結論
盤點正確：showmore 不加價、Body 未 HTML、定價 % 未接線、圖案 A 已近。拆 D8b（LLM／showmore_* 欄／公版尾段）正確。

### 裁決表

| 題 | 裁決 | 備註 |
|---|---|---|
| Q1 加價寫哪 | **A** | 匯出當下算；body 帶 %、server 預設 5、clamp 0–100；**零 SQL** |
| Q2 A25 Matrixify | **A** | Showmore＋Matrixify 同 `formatPlainTextAsHtml`／isLikelyHtml |
| Q3 多款式 | **A** | 本包維持 draft 層單一列「單一款式」；variant 展開列 → **D8-variants** 另項 |
| Q4 匯出後 draft 狀態 | **A** | Showmore 成功後可比照 Matrixify 標 `csv_ready`（若現狀 matrixify 有做）；失敗不假標 |
| Q5 簡述輕量模板 | **B 空** | 本包簡述欄空字串；不做假行銷句（誠實）。D8b 再模板／LLM |
| Q6 /records 灰字 | **A 可做一行** | 可選；無則 skip，不重做 C5 |
| Q7 清單勾選 | **A** | 註 **D8-open 完成**；完整 D8（含 D8b／案 B／嵌圖）**不勾滿** |

### 硬性
1. 售價／原價：`beautifyNestoryPrice` after markup；原價必須 > 售價否則 `nextBeautifiedPriceAbove`；**成本不加價**。
2. 圖：processed → original；略過 spec；無圖空、不塞假 CDN。
3. 案 B zip：**不做**；文案 disabled／title 誠實。
4. UI：toast＋設定「匯出時已套用」；BX-P 禁止。
5. `verify-d8-showmore.mjs`；差異 **28**；不 push；不要求 027／實機。
6. 報告禁止寫「已實機通過」。

**發包**：工人可動 code。`feat(D8)`／`docs(D8)`。

---

## 2026-07-14｜D8-open 收工核帳（Grok 總指揮｜**程式通過**）

### Git
- `8505341` feat(D8): Showmore export markup, HTML body, case A images
- `b319a17` docs(D8): D8-open note, diff 28
- 核帳時 ahead origin 2 → 本節後 push

### 指揮複跑 — 全綠
| 項 | 結果 |
|---|---|
| tsc | ✅ |
| verify-d8-showmore | ✅（加價／原價>售價／HTML／圖優先／無案 B／文案已套用） |
| b15-style-probe | ✅ 三主題 |

### 裁決對帳
| 項 | 裁定 |
|---|---|
| Q1 匯出當下算 | ✅ showmorePricing + route body % |
| Q2 A25 雙路徑 | ✅ showmore + matrixify |
| Q3 單列／簡述空 | ✅ verify |
| Q4 csv_ready | ✅（jobs manual+export showmore；零 SQL enum） |
| Q5 簡述空 | ✅ |
| Q6 records 灰字 | ✅ |
| Q7 完整 D8 不勾滿 | ✅ 差異 28 |
| 成本不加價 | ✅ |

**裁定：D8-open 程式核帳通過。** 不宣稱 Showmore 後台／實機通過。

### 統一實機債追加
- [ ] 真 CSV 匯入 Showmore：案 A CDN 圖是否成功（失敗再開案 B）
- [ ] 設定改加價 % → 匯出 CSV 售價／原價／成本肉眼對

### 下一包建議
1. **D9** 匯出前健檢＋預覽（Mockup 匯出體驗）
2. **D8b** LLM／模板改寫（較重）
3. 或事件 #2／D8a 嵌圖
4. 繼續堆 code 或等階段統一測 — 老闆定

---

## 2026-07-14｜總指揮裁決下一包：**D9-open**（匯出前健檢＋預覽）

### 決策（老闆授權直接裁，不開投票）

| 候選 | 為何不現在 |
|---|---|
| D8b 文案改寫 v2 | 重（LLM＋欄位＋設定）；匯出路徑剛齊，先防「髒 CSV 出去」ROI 更高 |
| 事件 #2 通知 | 實機未跑，通知價值低；資料面已有 notify_sent_at 可後接 |
| D8a 嵌圖 | 依賴真發布／CDN 實機；統一測前優先級低 |
| **D9-open** | **勝出**：接在 D8-open 後，對齊 Mockup「健檢→預覽→下載」；零／少依賴實機；統一測時一次驗匯出全鏈 |

### D9-open 範圍（本包）

**必做**
1. 匯出前 **欄位健檢**（純規則，不 LLM）：標題空、無圖、無售價、缺成本可選警告、description 空等——分 **擋下（error）** vs **可帶警告繼續（warn）**
2. **預覽**：Modal／底部抽屜（&lt;960）顯示：件數、加價%、每件售價／原價摘要、警告列表；確認後才打 `/api/exports/showmore`（Matrixify 同走一層更佳）
3. 接線：工作檯＋全部草稿的 ⬇ Showmore（及 Matrixify 若改動成本低）
4. 樣式：現站 modal／.schip／tokens；**BX-P 禁止**
5. `scripts/verify-d9-export-preflight.mjs`；差異 29；完整 D9 若含「商品頁預覽 iframe」可標未做

**明確不做**：D8b LLM、案 B zip、D8a、D10、事件 #2、刪佇列、要求實機／027。

**發包**：工人先設計方案。

---

## 2026-07-14｜D9-open 設計方案 **放行**

### 方案結論
對齊 Mockup「健檢→預覽→下載」；接 D8 加價摘要；B11 modal 殼；零 SQL。範圍正確（無 iframe／D8b）。

### 裁決表

| 題 | 裁決 | 備註 |
|---|---|---|
| Q1 健檢資料來源 | **A** | 共用 `exportPreflight` 純函式；工作檯有完整 props 即時算；佇列不足 → `POST /api/exports/preflight` |
| Q2 error/warn 分級 | **採用工人表** | 見下微調 |
| Q3 Matrixify 同層 | **A** | 同一 Modal，kind showmore\|matrixify；Matrixify 無加價%列 |
| Q4 export 二次擋 | **A** | UI 擋為主；export route **不**強制 preflight（腳本／單路徑不炸） |
| Q5 單件產生 CSV | **A** | ResultCard 單件 Matrixify 也走預覽，避免繞過 |
| Q6 iframe | **不做** | 完整 D9 不勾滿 |

### Q2 分級確認（微調一句）
- **Error**：標題空、售價空/≤0、選中 0 件、狀態不在 approved|api_failed|csv_ready；**Showmore 無可用商品圖（略 spec）= error**；**Matrixify 無圖 = warn**（非 error）。
- **Warn**：描述空、缺成本（維持 warn 不改 error）、特價無原價、多款式未展開、僅 original 無 processed、庫存/重量預設總提示。
- 缺成本 **維持 warn**（不改 error）——成本對 Showmore 非必填星號同規，避免誤擋可匯商品。

### 硬性
1. 確認後才 `downloadCsv`；error 時主鈕 disabled。
2. Showmore 預覽售價用 `applyShowmoreMarkup`／showmorePricing 與匯出一致。
3. 樣式 B11 modal + .schip；&lt;960 底部抽屜；BX-P 禁止。
4. verify-d9-export-preflight.mjs；差異 29；完整 D9 不勾滿。
5. 不 push；不要求 027／實機。

**發包**：工人可動 code。`feat(D9)`／`docs(D9)`。

---

## 2026-07-14｜D9-open 收工核帳（**程式通過**）＋下一包 **#2-open**

### D9 核帳
- commits `54b914e` feat + `29ab510` docs
- 複跑：typecheck ✅、verify-d9 ✅（Showmore 無圖 error／Matrixify 無圖 warn／缺成本 warn／Q4 不強制 route／Q5 單卡）、b15 ✅
- 差異 29／完整 D9 不勾滿 ✅
- **裁定：D9-open 程式通過**（不宣稱實機通過）

### 統一實機債追加
- [ ] 工作檯／全部草稿／單卡：⬇ 匯出先預覽再下載
- [ ] error 硬擋、warn「仍要下載」、改加價% 預覽數字變
- [ ] 手機 <960 抽屜

### 下一包裁決：**#2-open｜發布批次完成通知**

| 候選 | 為何不現在 |
|---|---|
| D8b 文案改寫 | 重；匯出鏈已 D8+D9，改寫可等統一測後依痛點再上 |
| D8a 嵌圖 | 偏實機 CDN／商品頁觀感，統一測後更準 |
| 停工統一測 | 老闆節奏仍是先堆 code；記帳已夠 |
| **#2-open** | **勝出**：D6 通知中心＋D7 `publish_batches.notify_sent_at` 已齊；補「發布做完通知我」閉環；包小、可 mock、不依賴 Showmore 後台 |

**#2-open 範圍**
1. 事件 `publish_batch_done`：batch 終態後 `tryNotifyPublishBatchIfComplete`（對稱 image #1）
2. 接線 `runPublishBatch` 結束（含 skipped／partial）；冪等 claim `notify_sent_at`（Q3b 同：≥1 通道 sent 才 claim）
3. Email Resend 全文＋LINE Flex 精簡；無 key skip；模板連 `/records`
4. verify 腳本擴充或 `verify-d6` 加 #2 cases；差異 30
5. 不做 #3/#4、D8b、iframe、BX-P

**發包**：工人先設計方案。

---

## 2026-07-14｜#2-open 設計方案 **放行**（publish_batch_done）

### 方案結論
對稱 D6 #1；接 runPublishBatch；零 migration；無 UI。正確。

### 裁決表

| 題 | 裁決 | 備註 |
|---|---|---|
| Q1 終態門檻 | **A** | 只看 items 全 done\|failed\|skipped |
| Q2 Email 清單 | **B** | 失敗/略過必列；成功最多 20；LINE 只件數 |
| Q3 接線位置 | **A** | 僅 runPublishBatch 終態後 safeTry；補 key 補寄記 TODO 不塞本包 |
| Q4 verify | **A** | 擴充 verify-d6-notify.mjs |
| Q5 Settings | **A** | 不改文案（避 BX-P） |

### 硬性
1. Q3b：≥1 sent 才 claim notify_sent_at；全 skip/error 不 claim
2. notify 失敗不影響 runPublishBatch.ok
3. 禁 LINE Notify；深連結 /records
4. 零 migration；差異 30；不 push；不要求實機

**發包**：工人可動 code。`feat(notify2)` 或 `feat(D6-#2)`／`docs(...)`。

---

## 2026-07-14｜#2-open 收工核帳（**程式通過**）＋下一包 **D8a-open**

### #2 核帳
- commits `ff57378` feat + `1b0335c` docs
- 複跑：typecheck ✅、verify-d6-notify（含 #2）✅、verify-d7 ✅
- Q1–Q5／Q3b 抽查：items 終態、Email≤20、runPublishBatch hook、claim、/records、禁 Notify ✅
- 差異 30 ✅
- **裁定：#2-open 程式通過**（真寄信／027 屬統一實機債）

### 統一實機債追加
- [ ] 有 RESEND／LINE 時發布 1 件 → 收到完成通知；同批不洗版
- [ ] 無 key 時發布仍成功
- [ ] 027 已跑（通知 claim 與紀錄頁前提）

### 階段判斷
D1–D9-open ＋ 通知 #1/#2 程式主幹已閉環。統一測仍延後。

### 下一包裁決：**D8a-open｜描述嵌圖（Shopify）**

| 候選 | 為何不現在 |
|---|---|
| D8b 文案改寫 | 重；等實機看文案痛點 |
| 停工只測 | 老闆節奏仍可堆功能；測項已記帳 |
| D10 影片 | 較邊緣 |
| **D8a-open** | **勝出**：發布頁資訊密度／SEO 圖；吃既有 CDN／processed；可 mock；接 publish payload 邊界 |

**D8a-open 範圍**
1. Shopify 發布 descriptionHtml：文案後自動插 1 主圖 + 1 詳情（或 scene）圖，含 ALT
2. 可關：env 或 team_settings／既有設定習慣（方案裁）
3. Showmore 是否吃 HTML 圖：本包 **誠實文件 + 可選不插**（首件測試屬實機債）
4. verify 腳本；差異 31；不改 nav／BX-P
5. 不做 D8b、D10、#3/#4

**發包**：工人先設計方案。

---

## 2026-07-14｜D8a-open 設計方案 **放行**（描述嵌圖）

### 方案結論
嵌圖只在 Shopify 邊界（payload／可選 Matrixify）；不寫 DB；可關；Showmore 預設不插。CDN 誠實。正確。

### 裁決表

| 題 | 裁決 | 備註 |
|---|---|---|
| Q1 開關 | **A** | env `DESCRIPTION_EMBED_IMAGES`；未設＝開；0/false/off＝關 |
| Q2 第二張 | **A** | detail → generated_detail → 其他非 main（略 spec；variant 最後） |
| Q3 Settings | **A** | 不改 UI；.env.example 說明 |
| Q4 Showmore | **A** | 預設不插；`SHOWMORE_DESCRIPTION_EMBED_IMAGES` 預設 off |
| Q5 Matrixify | **A** | Body HTML 同嵌（與 API 發布一致） |
| Q6 無 CDN | **A** | 有可用 URL 就嵌；文件寫清非保證 CDN |

### 硬性
1. 最多 2 張；0 則不塞假圖；不寫 description_html
2. 順序：formatPlainText → embed → internal link → FAQ JSON-LD
3. 略 spec；alt 優先；HTML escape
4. verify-d8a；差異 31；零 migration；不 push
5. 不做 D8b／D10／#3/#4／BX-P

**發包**：工人可動 code。`feat(D8a)`／`docs(D8a)`。

---

## 2026-07-14｜D8a-open 收工核帳（**程式通過**）＋ **Phase D 程式收斂閘**

### D8a 核帳
- commits `62f5fb6` feat + `21670c9` docs
- 複跑：typecheck ✅、verify-d8a ✅（選圖／max2／env off／Showmore 預設 off／payload 順序）
- Q1–Q6 全 A 落地；差異 31 ✅
- **裁定：D8a-open 程式通過**（真店描述見圖屬統一實機債）

### 統一實機債追加
- [ ] 真店發布 → 描述文末 1～2 圖；env off 後無圖
- [ ] Showmore 預設介紹無 `<img>`（若開 env 再測後台）

### Phase D 程式收斂（總指揮判斷）

| 已程式通過（mock／腳本） | 仍未做／重 |
|---|---|
| D1–D5 圖鏈、D6 #1、#2、D7 發布帳、D8-open 匯出、D8a 嵌圖、D9-open 健檢 | D8b 文案改寫、D10 影片、完整 D9 iframe、#3/#4、E/F/G |

**下一包裁決：不開新 coding 包，進入「統一實機」階段。**

理由：主上架閉環（生成→圖→發布→通知→匯出健檢→嵌圖）已齊；再堆 D8b 而不測，風險是實機債爆炸、改方向成本高。老闆先前約定「做到一階段再統一測」——**現在就是這個階段**。

若老闆強制續 coding：優先序 **E1 儀表板今日待辦**（測的時候超好用）＞ D10 ＞ D8b。

### 統一實機總帳（測完勾）

**SQL／env**
- [ ] 027 publish_batches
- [ ] 001–026 仍齊
- [ ] Vercel：Shopify、OpenAI、TAVILY、CRON、可選 RESEND/LINE、APP_BASE_URL

**主線 30–45 分**
- [ ] 登入／C2 設定／C6 匯率
- [ ] 新增 1 件：圖→生成→結果卡
- [ ] 核准並建草稿 → /records 有批
- [ ] （有 key）發布完成通知
- [ ] 送圖全 keep → 圖審確認
- [ ] ⬇ Showmore：預覽健檢→下載；加價數字合理
- [ ] 真店商品描述有嵌圖（D8a）

**可選加深**
- [ ] 混標／ai-process、CSV 進 Showmore 後台（案 A 圖）、無 key 通知 skip

### 交 Fable
佇列去留／Shopify 草稿連動仍 open；本階段不處理。

---

## 2026-07-14｜老闆續 coding（統一測延至週五 Fable）＋下一包 **E1-open**

### 老闆指示
- 統一實機 **等到週五 Fable 回來再測**
- 期間 **繼續下一包 coding**

### 下一包裁決：**E1-open｜儀表板今日待辦卡**

| 候選 | 為何不現在 |
|---|---|
| D8b 文案改寫 | 重、且缺實機回饋；週五測後較準 |
| D10 影片 | 邊緣 |
| **E1-open** | **勝出**：測的時候一眼看「文案待審／圖審／失敗／待發布」；讀既有 draft／image 狀態即可；可 mock；對齊施工清單 E1 |

**E1-open 範圍**
1. `/dashboard` 取代 ComingSoon
2. **今日待辦卡**（可點跳轉）：文案待審、圖審待確認、處理／發布失敗、可發布／待發布等（具體分桶方案裁）
3. 資料：session+RLS 讀 product_drafts／必要時 image 狀態；admin 看全部或可切「我的」
4. 樣式：現站 panel／.schip／tokens；BX-P 禁止；手機可讀
5. verify 腳本純邏輯分桶；差異 32
6. **不做** E2 漏斗、E3 Make 額度、E4 成本明細全文、E5 熱圖、E6 AI 顧問

**發包**：工人先設計方案。

---

## 2026-07-14｜E1-open 設計方案 **放行**（儀表板今日待辦）

### 方案結論
正確對齊：E1＝待辦卡，不是 Mockup metric 整頁。4 桶＋積壓＋session/RLS＋重用 stageFilter／圖審分類。零 SQL。

### 裁決表

| 題 | 裁決 | 備註 |
|---|---|---|
| Q1 「今日」語意 | **A** | 積壓待辦，不限日曆今天 |
| Q2 失敗一桶 | **A** | union by draft；副標可含圖失敗 n；主跳 /drafts stage=failed |
| Q3 admin 預設範圍 | **A** | 預設「我的」；可切全部（同圖審） |
| Q4 0 件 | **A** | 卡仍顯示 0，不藏 |
| Q5 跳轉 stage | **A** | 寫 sessionStorage 既有 key，對齊 pills |
| Q6 下方 E2–E6 | **A** | 一行 muted 誠實佔位，無假數字 |

（截斷題若與工人建議 A 一致則同上；若有 Q 未列視同 A。）

### 硬性
1. 4 桶：文案待審／圖審／失敗／待發布；重用 matchesStage、classifyReviewQueueItem
2. 不改 /review 行為、不改 nav IA
3. tokens／.schip／.ir-scope；BX-P 禁止
4. verify-e1-todo-buckets.mjs；差異 32；typecheck＋b15；不 push
5. 不做 E2–E6、D8b、實機宣稱

**發包**：工人可動 code。`feat(E1)`／`docs(E1)`。

---

## 2026-07-14｜E1-open 收工核帳（**程式通過**）＋下一包 **D10-open**

### E1 核帳
- commits `8ca29ad` feat + `0f3ce80` docs
- 複跑：typecheck ✅、verify-e1 ✅（4 桶／union 失敗／0 件／stage 深連／archived 排除）、b15 ✅
- 差異 32／清單 E1 [x] ✅
- **裁定：E1-open 程式通過**（登入畫面歸週五統一實機）

### 統一實機債追加
- [ ] /dashboard 四卡數字與真實佇列一致
- [ ] 點卡跳轉＋stage pills 對得上
- [ ] admin 我的／全部

### 下一包裁決：**D10-open｜影片欄位（YouTube → Shopify）**

| 候選 | 為何不現在 |
|---|---|
| D8b | 仍重；週五測後再做 |
| E2 漏斗 | 可後；E1 已夠開工 |
| **D10-open** | 清單下一項；包可控制；接 publish 媒體邊界 |

**D10-open 範圍**
1. draft 可存 video_urls（YouTube）；migration 只產檔若需
2. 發布時 EXTERNAL_VIDEO 掛進 Shopify 媒體（文件核對 Admin API）
3. Showmore：內文附連結（可選輕量）
4. 表單入口輕量（不 BX-P）；verify 腳本；差異 33
5. 不做 D8b、完整 E2–E6

**發包**：工人先設計方案。統一測仍週五 Fable。

---

## 2026-07-14｜D10-open 設計方案 **放行**（YouTube 影片）

### 方案結論
video_urls 欄已在 005；表單輕量＋payload EXTERNAL_VIDEO＋Showmore 文字連結。零 SQL。正確。

### 裁決表

| 題 | 裁決 | 備註 |
|---|---|---|
| Q1 存 string[] | **A** | 純 URL |
| Q2 表單位置 | **A** | 第 4 段收合「影片連結」 |
| Q3 上限 | **A** | 最多 3 條 |
| Q4 非法 URL | **A** | 草稿可存；發布 skip 非法＋warnings 黃字 |
| Q5 Showmore | **A** | 有網址就附內文尾連結 |
| Q6 Matrixify | **A** | 本包不碰 |
| Q7 卡片編輯 | **A** | 只左表單＋persist |
| Q8 migration | **A** | 零 SQL |

### 硬性
1. media = IMAGE… + EXTERNAL_VIDEO…（圖前片後）
2. 只認 YouTube；不重寫 publish GraphQL 本體
3. 不寫回 description_html；BX-P 禁止
4. verify-d10-video.mjs；差異 33；不 push
5. 不做 D8b／E2–E6／原生上傳

**發包**：工人可動 code。`feat(D10)`／`docs(D10)`。

---

## 2026-07-14｜D10-open 收工核帳（**程式通過**）＋下一包 **D8b-open**

### D10 核帳
- commits `0e29063` feat + `c43ff6f` docs
- 複跑：typecheck ✅、verify-d10 ✅（正規化／EXTERNAL_VIDEO／Showmore／表單／零 migration）、b15 ✅
- Q1–Q8 全 A 落地；差異 33 ✅
- **裁定：D10-open 程式通過**（真店輪播歸週五統一測）

### 統一實機債追加
- [ ] 表單貼 YouTube → 真店媒體輪播可播
- [ ] 非 YouTube → 黃字略過、發布仍成功
- [ ] Showmore 內文尾影片連結

### Phase D 程式狀態
D1–D10-open（含 D8-open／D8a／D9-open）主幹已齊；**完整 D8 僅剩 D8b 文案改寫**；完整 D9 iframe 未做。

### 下一包裁決：**D8b-open｜Showmore 文案改寫輕量版**

| 候選 | 為何不現在 |
|---|---|
| 停工等週五 | 老闆要續 coding |
| E2 漏斗 | 可後；E1 已夠 |
| **D8b-open** | 完整 D8 最後一塊；匯出鏈 D8+D9 已穩，適合補「改寫版」 |

**D8b-open 範圍（控制爆炸）**
1. 匯出 Showmore 時產生／使用改寫欄或邊界改寫（方案裁：規則模板優先，LLM 可選）
2. 標題公式／結構符號／公版尾段（team_settings 可選；migration 只產檔若需）
3. 一次寫入避免重複算（若寫欄）
4. verify-d8b；差異 34
5. **不做**：案 B zip、完整 LLM 長跑若無 key、iframe、BX-P
6. 工人先方案：必須拆「規則可綠」vs「LLM 可選」避免卡金鑰

**發包**：工人先設計方案。週五統一測不變。

---

## 2026-07-14｜D8b-open 設計方案 **放行**（Showmore 規則改寫）

### 方案結論
規則模板必綠、無 key；Shopify/DB 不改；LLM 本包只 hook。正確控範圍。

### 裁決表

| 題 | 裁決 | 備註 |
|---|---|---|
| Q1 寫庫 | **A** | 匯出邊界現算；零新欄 |
| Q2 標題 | **A** | 嚴格模板 v2 骨架 |
| Q3 FAQ | **A** | 有則轉純文字；無則 2 條公版 |
| Q4 尾段 | **B** | **僅 code 預設**；本包**零 migration**。可讀既有 team_settings 若 key 已在（不產 028、不依賴 SQL） |
| Q5 LLM | **A** | 只留介面／註解 hook；`rewriteMode: "rules"`；不真呼叫 |
| Q6 清單 | **A** | 註 D8b-open 完成；完整 D8 不勾滿 |

### 硬性
1. 無依據整行略過；內文禁來源平台／價格
2. 只影響 Showmore 匯出；加價／圖／D9／D10 鏈不破
3. verify-d8b + 複跑 verify-d8／d9；差異 34；不 push
4. 不做案 B、iframe、E2、BX-P、完整 LLM 寫欄

**發包**：工人可動 code。`feat(D8b)`／`docs(D8b)`。
