# UX-B4-P04「手機結果卡 — 三排重排＋chrome 精簡＋長按提示」工人開場指令

> **用法**：開**新的 Grok 工人對話**，把下方 ```text``` 框**整段貼上**。  
> **工人**：**Grok**（只實作＋commit feat；**不 push**）。  
> **來源**：老闆原 #6/#7 ＋ **2026-07-21 補充截圖**（全站結果卡手機三排骨架）。  
> **同步**：`docs/UIUX本輪改動同步-2026-07-21.md` §2.6（**已修訂**，以本檔＋同步檔為準）。  
> **前提**：B3-P04 長按多選＋左滑保留；**不要拆手勢**。  
> **依賴**：建議 **P02（標題後銷售小標）先做或同工人連做**；P06（失敗紅字上標題列）可平行但同檔要協調。  
> **分支**：`codex/nestory-v0.1-safety-skeleton`。  
> **⚠ 覆寫**：舊版 P04「手機藏全部快捷含重生」**作廢** → 手機**保留重生**在第三排左；**核准仍藏**（左滑有）。

---

```text
你是 Nestory【UX-B4-P04｜手機結果卡三排重排】工人（Grok）。
臨時代理指揮規格已定案（含老闆第二波截圖）。本包體量中大，先回 8 行骨架再動工。

════════════════════════════════════
必讀
════════════════════════════════════
1. AGENTS.md（tokens、.sel、三主題、禁止 !important、不 push）
2. docs/UIUX本輪改動同步-2026-07-21.md §2.6（修訂版：三排骨架）
3. 動檔：
   - src/components/listing/ResultCard.tsx（rc-header 整段、isNarrow、swipe、quick）
   - src/components/listing/DraftResultsPanel.tsx（頂部長按提示）
   - src/app/globals.css（.rc-header／.rc-thumb／.rc-price-mini／mobile @media max-width 959）
4. 長按／左滑：B3-P04 邏輯勿拆壞

════════════════════════════════════
避撞
════════════════════════════════════
禁止：
- push
- 改桌機 header 資訊架構（桌機維持現況勾選／快捷／×／▸）
- 拆長按多選、左滑 action 內容語意
- 改核准／重生 API
- 新 !important；自創色

可以：
- isNarrow 專用 DOM 結構或 modifier class（.rc-header--mobile 等）
- 手機條件渲染

════════════════════════════════════
本包只做（僅 isNarrow === true；所有預覽結果卡站別共用）
════════════════════════════════════
適用：文案待審核／圖片審核／完成待發布／失敗／需修改等**所有** ResultCard 摺疊預覽。

【A】三排骨架（老闆定案）

第 1 排：標題列
- 左起：標題文字（.rc-title）
- 標題**同一排後方**：小標／狀態
  · 銷售狀態（P02：海外現貨／二手現貨…）若已實作則放這
  · 若 P06 已把 failReason／失敗相關紅字掛標題後，接同一 flex wrap 列
- 不要把大段 meta 塞爆第一排

第 2 排：左圖右 chips
- **左側**：主圖縮圖（.rc-thumb，維持可辨尺寸，建議 ≥56–72px 級）
- **右側**：現有「有的沒的」預覽 chips 集合：
  · 站別／失敗／需修改／日期（若仍顯示）
  · IP／角色／類型／語氣
  · 警告計數 ⛔／⚠／🔍
  · 不要再把縮圖跟標題擠在同一橫列左側一坨

第 3 排：左重生、右價格
- **左側**：↻ 重生按鈕（僅文案站／可重生的站顯示；其他站可空或顯示該站合理次要操作——預設：isCopyStation 顯示重生）
- **右側**：售價 mini（.rc-price-mini：售價＋刪線定價＋利潤若有）
- **不要**在手機卡面放「✓ 核准」大鈕（核准改左滑／批次）
- **不要** ×、**不要** ▸ 三角（點整卡 header 展開仍有效）

【B】chrome 精簡（覆寫舊「全藏快捷」）
| 元素 | 手機 |
|---|---|
| checkbox | 隱藏（長按多選；is-checked 外框即可） |
| ✓ 核准 | 隱藏（左滑保留） |
| ↻ 重生 | **顯示於第 3 排左** |
| × dismiss | 隱藏（左滑刪／批次） |
| ▸／▾ toggle | 隱藏（點卡展開） |
| 列表頂提示 | 「長按卡片可多選；左滑可快捷」一句（可 localStorage 看過淡） |

【C】互動
- 點 header 空白／標題／圖 → 展開／收合（既有 handleHeaderClick）
- 點重生 → stopPropagation，開 regen modal（既有）
- 長按 → 多選；左滑 → 快捷；selectMode 時點卡 toggle
- 展開後 body／tabs **不在本包重做**

【D】CSS
- 用 flex／grid 做三排；wrap 友善長標題
- tokens only；三主題＋375 寬自查
- 觸控目標重生鈕 ≥44px 高

════════════════════════════════════
桌機
════════════════════════════════════
完全維持現況（含 checkbox、核准+重生、×、▸）。本包 CSS 必須包在 max-width:959 或 isNarrow 條件。

════════════════════════════════════
與其他包銜接
════════════════════════════════════
- P02 銷售小標：掛第 1 排標題後
- P06 失敗紅字／需修改同大：可同 session 或先做 P06；若尚未合併，本包預留標題列右側 slot
- 不要重做 P01 Tags 分頁

════════════════════════════════════
驗收
════════════════════════════════════
[ ] 手機三排：標題｜圖+chips｜重生+價
[ ] 所有站別摺疊卡都用同一骨架（缺價／無重生時該格可空，不破版）
[ ] 無 checkbox／核准／×／▸
[ ] 長按多選、左滑、點開展開仍可用
[ ] 桌機不變
[ ] 有一句長按提示
[ ] typecheck 綠；不 push

════════════════════════════════════
收工
════════════════════════════════════
commit：feat(UX-B4-P04): mobile result card 3-row layout
回報：DOM 結構草圖、哪些站顯示重生、桌機是否誤傷。

開工先回 8 行：
1. 是否 isNarrow 另組 header JSX
2. 三排 class 命名
3. chips 從哪幾個現有區塊搬
4. 核准如何藏、重生如何留
5. 與 P02/P06 slot
6. swipe/長按是否動到
7. 不做清單
8. 驗收自測步驟
```

---

## 總指揮備註

| 項 | 內容 |
|---|---|
| 體量 | 中大；ResultCard header 重構 |
| 雷 | 與 P02/P06 同檔衝突 → 建議同一工人串做或先 pull |
| 覆寫 | 舊 P04「藏重生」已作廢 |
