# UX-B3-P06「規格維度 B 方案 + 列排序 + 選圖放大」工人開場指令

> **用法**：開**新的 Grok 工人對話**，把下方 ```text``` 框**整段貼上**。  
> **工人**：**Grok**（實作＋commit；**不 push**）。  
> **粗包**：外部 Claude 第三輪 6／6 ＋ 拖曳／選圖補充（已潤飾合併）。  
> **分支**：`codex/nestory-v0.1-safety-skeleton`（開工前 `git pull`；P05 約 `6ba1856` 已 push）。  
> **前包**：B3-P01～P05 ✅。本包為第三輪收尾。

---

```text
你是 Nestory【UX-B3-P06｜規格編輯 B 方案】工人（Grok）。
本包三塊：① 維度區 B 方案 UI ② 規格列排序 ③ 選圖縮圖放大＋預覽。
業務展開／維度 CRUD 盡量重用 @/lib/variants 既有函式，不要重寫 cartesian。

════════════════════════════════════
必讀
════════════════════════════════════
1. AGENTS.md（tokens、禁止 !important、不 push）
2. src/components/listing/VariantEditor.tsx（全文 UI 區）
3. src/lib/variants（expandAndMergeVariantRows、appendDimensionValue、
   removeDimensionValue、canExpandFromDimensions、MAX_VARIANT_DIMENSIONS…）
4. src/components/listing/ImageUploader.tsx — HTML5 拖曳排序約 719–743 模式
5. src/app/globals.css — .vh-*、.v-dim-*、.vgrid-row、.pick-grid .pk
6. 可對照：docs/mockups/nestory-v7-mockup.html（vdrag 把手）

════════════════════════════════════
避撞
════════════════════════════════════
禁止：
- push／deploy
- 改 generate／pricing 公式核心；勿動 calculatePrice 語意
- 引入 dnd-kit／react-beautiful-dnd 等套件（原生 HTML5 + 手機 ▲▼）
- 新增 !important；自創新色 hex
- 拿掉 double-confirm（expand／remove-dim 的 armConfirm 必須保留）

可以：
- VariantEditor.tsx UI 重組
- globals.css 新規／調整 .v-dim-chip／.pick-grid
- 若需 reorderRows 純函式：可放 VariantEditor 內或 lib/variants 小函式

注意：Button 只有 size sm|md（無 lg）→ 主按鈕用 size="md" + fullWidth 或 class 撐滿。

════════════════════════════════════
現況（問題）
════════════════════════════════════
- 維度要先點「＋新增維度」；軸值區與一排平權按鈕（新增維度／依角色／套用成本／展開）搶眼
- 規格列無 ⠿ 排序
- .pick-grid .pk 僅 46×46，選圖難辨

════════════════════════════════════
① 維度區 B 方案（主）
════════════════════════════════════
目標版面：
  顏色： [粉] [藍] [輸入 加軸值…]
  尺寸： [S] [M] [輸入 加軸值…]
  ＋ 新增一個規格類型（ghost，dimensions.length < MAX）
  [ 依軸值展開列 ]  ← 滿寬 primary 主按鈕
  次要：「⋯」收納「套用成本到全部款式」「依角色建立」

實作要點：
1. 常駐顯示已有 dimensions 列（不必先開 pop 才看得到軸值）
   - 若 dimensions 為空：可預設提示或一鍵加入常用「顏色」「尺寸」空維度
     （QUICK_DIMS 已存在；勿自動塞假軸值）
2. 每維度：label（dim.name）+ chips（values）+ 行內 input Enter 加入
   - 沿用 appendDimensionValue／removeDimensionValue／axisValueDraft
   - chip × 移除軸值；維度整刪若保留，放次要或不顯眼 × + armConfirm
3. 「＋ 新增一個規格類型」→ 沿用 addDimension／dimOpen 選單或簡化流程
4. 主 CTA：依軸值展開列
   - onClick = expandFromAxisValues（含 armConfirm 二次確認）
   - class：.vh-expand-primary 滿寬、視覺最大
5. 輔助收進 ⋯ 選單（details/summary 或小 pop-menu）：
   - 套用成本到全部款式（applyCostToAllVariants）
   - 依角色建立（既有 charOpen 流程）
6. 可刪／簡化 vh-steps-hint 與舊 vh-btns 平權四鈕列（避免雙軌）
7. chip 樣式：方角微圓 border-radius: 6px（沿用／覆寫 .v-dim-chip、.v-axis-val）

CSS 建議 class（可微調命名但語意對齊）：
.vh-dims / .vh-dim-row / .vh-dim-label / .vh-dim-values
.vh-dim-add-input / .vh-add-dim-ghost / .vh-expand-primary
.vh-more（⋯ 容器）

════════════════════════════════════
② 規格列拖曳排序
════════════════════════════════════
- 桌機（!isNarrow 或 min-width 960）：每列 .vdrag ⠿
  HTML5 drag 模式抄 ImageUploader；
  reorderVariantRows(fromKey, toKey) 重排 rows 後 onRowsChange
- 手機：不依賴 HTML5 drag；列上 ▲▼ 兩鈕 moveRow(index, ±1)
- CSS：
  .vdrag { cursor: grab; color: var(--text-muted); touch-action: none; }
  .vgrid-row.is-drag-over { outline: 2px dashed var(--accent); … }
- 拖的是「列順序」不是維度順序；鍵用穩定 row id／index 字串

若 rows 無穩定 id：用 index 字串即可，drop 時 splice 重排。

════════════════════════════════════
③ 選圖縮圖放大 + 預覽
════════════════════════════════════
1. .pick-grid .pk：46 → **72×72**；gap 可 8px
2. 桌機：hover 顯示 .pk-zoom-preview（cover 160px、accent 框）
3. 手機：長按 ~450ms → 全螢幕預覽
   - zoom modal 必須 **createPortal(..., document.body)**
   - 勿放在 .pop-menu／transform 子孫（避 containing block 陷阱）
4. 點 pk 仍＝選圖 updateRow + 關 picker；長按不要誤觸選圖
   （長按成功設旗標吞 click，手法可比 P04）
5. tokens only；modal 底 color-mix black 60%

════════════════════════════════════
不做
════════════════════════════════════
- 不改 MAX_VARIANT_* 上限語意
- 不自動展開列（仍需按主按鈕）
- 不重寫 VariantEditor 以外大表單

════════════════════════════════════
驗收
════════════════════════════════════
B 方案：
[ ] 維度＋軸值 chip 常駐可見（有維度時不必先點「新增維度」才看到列）
[ ] 「依軸值展開列」是唯一滿寬 primary 主按鈕
[ ] 套用成本／依角色在 ⋯ 次要選單，不與主鈕平起平坐
[ ] 展開二次確認仍在

排序：
[ ] 桌機 ⠿ 拖曳改列序
[ ] 手機 ▲▼ 改列序

選圖：
[ ] pk 72×72 可辨
[ ] 桌機 hover 放大預覽
[ ] 手機長按 portal 全螢幕預覽；點遮罩關閉

全域：
[ ] 三主題 tokens；無 !important
[ ] typecheck 綠
[ ] 不 push

════════════════════════════════════
收工
════════════════════════════════════
commit 建議（可拆 1～2 個 commit，訊息清楚即可）：
  feat(UX-B3-P06): variant B-layout, row reorder, pick-grid zoom
回報：動檔、主按鈕／⋯ 放哪、排序鍵策略、portal 是否用上。

開工先回 5 行：
1. 維度常駐列是否重用 v-dim-axes 改造
2. 展開仍走 expandFromAxisValues + arm
3. ⋯ 收納兩輔助
4. 桌機 drag／手機 ▲▼
5. pick 72 + portal 長按預覽
```

---

## 總指揮備註

| 項 | 內容 |
|---|---|
| 工人 | **Grok** |
| 體量 | 第三輪最大包；可接受拆 2 commits |
| 雷 | Button 無 `size="lg"`；rows 可能無穩定 id |
| 雷 | zoom modal 必須 portal |
| 推送 | 核帳後代 push；本輪 6／6 收工 |
