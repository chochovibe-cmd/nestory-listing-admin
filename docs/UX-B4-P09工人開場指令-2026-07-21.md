# UX-B4-P09「圖片輸入區還原到 B2-P10 完成版」工人開場指令

> **用法**：開**新的 Grok 工人對話**，把下方 ```text``` 框**整段貼上**。  
> **範圍（老闆定案）**：**只還原「新增商品」圖片輸入區**（主圖／詳情圖縮圖列）到 **UX-B2-P10 完成版**。  
> **錨點 commit**：外觀＝**`2cc900b`**（P10 核帳＋× 左上）；功能本體＝**`ed342ce`**。  
> **其他一律不改**：結果卡、手機三排、Tags、規格 Variants、雙欄 P07、Shopify、站② 圖審流程語意。  
> **不 push**（核帳後指揮代 push）。

---

```text
你是 Nestory【UX-B4-P09｜輸入區縮圖還原 P10 完成版】工人（Grok）。
老闆定案：只有圖片輸入區要長得跟 B2-P10 做完時一樣；其餘 B4 保留。

════════════════════════════════════
必讀
════════════════════════════════════
1. AGENTS.md（tokens、禁止新增 !important、不 push）
2. 錨點（必對 diff）：
   git show 2cc900b:src/app/globals.css
   git show ed342ce:src/components/listing/ImageUploader.tsx
   git show 2cc900b -- src/app/globals.css   # 含 × 左上那 6 行
3. 現況：HEAD 含 B4-P08（wrap／72×96／規格圖下 chip／× 右上）— 本包要覆寫回 P10
4. 動檔允許：
   - src/app/globals.css（僅 .pthumb* 輸入區相關；見下方）
   - src/components/listing/ImageUploader.tsx（僅提示文案若與 P10 不一致）
5. 禁止動：ResultCard*、VariantEditor、DraftResultsPanel、publishDraft、
   Station2ImagePanel.tsx（站② JSX）、P07 的 .workbench-panes / panel overflow-x:clip 規則

════════════════════════════════════
目標外觀（P10 完成＝2cc900b）
════════════════════════════════════
輸入區 .pthumb-strip（ImageUploader 主圖／詳情圖）：
1. 橫向一排可滑：flex-wrap: nowrap; overflow-x: auto; align-items: flex-end;
   gap: 11px; padding-bottom: 6px; -webkit-overflow-scrolling: touch;
2. 正方形縮圖：.pthumb-img 96×96；.pthumb.is-main .pthumb-img 120×120；object-fit: cover
3. .pthumb-img-wrap：position: relative; display: inline-block;（不要 column flex 把規格圖擠到圖下）
4. 規格圖：.pthumb-spec-badge 維持 absolute 角標 top:6px; right:6px;（圖右上）
5. × 刪除：.pthumb-img-wrap > .thumb-remove → top:6px; left:6px; right:auto;（圖左上，P10 核帳定案）
6. 提示文：對齊 P10「主圖右上角可標規格圖」（或同義一句，勿寫「縮圖下方」）

════════════════════════════════════
必須撤掉的 B4-P08 改動（輸入區）
════════════════════════════════════
在 globals.css 裡移除或覆寫回 P10：
- .pthumb-strip 的 flex-wrap: wrap、overflow-x/y: visible、padding: 8px 8px 4px（P08 為 clip 加的）
- .pthumb-img 72×72／主圖 96×96／aspect-ratio 規則（改回 96／120）
- .pthumb-img-wrap 的 inline-flex column
- .pthumb-img-wrap > .thumb-remove 右上 -6px（改回左上 6px）
- .pthumb-strip .pthumb-spec-badge { position: relative; … } 整段刪掉（規格圖回角標）

建議做法：
  直接對照 `git show 2cc900b:src/app/globals.css` 中 .pthumb-strip 到 .pthumb-spec-badge
  手機 media 觸控那段，把「輸入區」行為還原成該快照；
  不要整檔 checkout（會蓋掉 P07／結果卡等）。

════════════════════════════════════
P07 相容（必守）
════════════════════════════════════
- 不要改 .panel.workspace-input-panel 的 overflow-x: clip
- 若橫滑條被裁：只在 .pthumb-strip 上保留可捲（overflow-x: auto），
  必要時 strip 左右各加少量 padding（≤6px），但外觀仍以 P10 為準
- 不要為了縮圖把整欄 overflow 改回 visible

════════════════════════════════════
站②邊界
════════════════════════════════════
- 不改 Station2ImagePanel.tsx
- .s2-img-panel 既有規則可留；若 .pthumb-spec-badge 全域 absolute 對站②仍正確就好
- 本包驗收以「新增商品 → 主圖／詳情圖上傳列」為準

════════════════════════════════════
驗收
════════════════════════════════════
[ ] 輸入區縮圖：橫滑一排、96 正方、主圖 120
[ ] 規格圖角標在圖右上；× 在圖左上；不重疊
[ ] 提示文恢復右上角標示語意
[ ] 結果卡／手機三排／Tags／規格 Variants／左右欄不重疊 — 目視無被本包改壞
[ ] typecheck 綠；不 push

════════════════════════════════════
收工
════════════════════════════════════
commit：fix(UX-B4-P09): restore input thumbs to B2-P10 complete look
回報：改了哪些 selector、是否動 ImageUploader 文案、與 2cc900b 對照結果、P07 是否仍不重疊。

開工先回 4 行：錨點 2cc900b 是否對上、只動輸入區確認、P08 要撤哪幾條、站②不動。
```

---

## 總指揮備註

| 項 | 內容 |
|---|---|
| 老闆定案 | 只還原圖片輸入區到 P10 完成版 |
| 錨點 | `2cc900b`（外觀）／`ed342ce`（引入） |
| 會撤 | B4-P08 對輸入區的 wrap／圖下規格 chip |
| 保留 | P07 雙欄、P01–P06、站② |
