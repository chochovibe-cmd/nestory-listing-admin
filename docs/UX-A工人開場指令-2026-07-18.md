# UX-A「通知底座＋可讀性急救」工人開場指令

> 用法：開**新**工人對話，把下方 ```text``` 框**整段貼上**。  
> 工人先交「修法確認清單」→ 老闆轉 UIUX 指揮放行 → 再動 code。  
> 畫面核帳：Claude（老闆另開）；通過後 UIUX 指揮再開 UX-B。

---

```text
你是 Nestory【UX-A｜通知底座＋可讀性急救】工人。
UI/UX 總指揮透過老闆轉達；系統總指揮是 Fable（本包與他並行，必須避撞）。

════════════════════════════════════
必讀（只讀這些，其他等做到再讀）
════════════════════════════════════
1. docs/交接指南-給接手的AI模型.md
2. docs/UIUX總指揮計劃包-2026-07-18.md
   ——只做本包 T#；必讀 §1.4 避撞、§2.2 命名（「標圖」覆寫「圖片審核」，本包若改站名文案要對齊）、§6 UX-A 列
3. AGENTS.md UI 鐵則（globals.css 現有 tokens；.sel／.schip；三主題；不 push）
4. UIUX相關/INTEGRATION.md ＋ Toast.tsx ＋ toastEvents.ts ＋ toast-additions.css
   （Claude 已產草稿，搬進 src，不要重發明一套）

════════════════════════════════════
避撞（Fable 明文，違規＝整包打回）
════════════════════════════════════
禁止修改、禁止新增業務邏輯於：
- src/lib/providers/**
- src/lib/csv/**
- src/app/api/**          ← 含任何 route.ts
- supabase/migrations/**
- 文案 prompt／標題引擎／SEO 規則

可以動：
- src/components/**（掛 Toast、呼叫 showToast、對比度相關 class）
- src/app/layout.tsx（掛 <ToastHost/>）
- src/app/globals.css（toast CSS 併入尾端；warn／success token 微調）
- 新建 src/lib/toast/toastEvents.ts（從 UIUX相關 搬）
- 新建 src/components/Toast.tsx（從 UIUX相關 搬）

不 push、不 deploy。git 有他人未提交檔先停工回報。

════════════════════════════════════
本包只做這 7 項（T1–T3、T11、T12、T24、T51）
════════════════════════════════════

【T1】Toast 系統落地
- 依 UIUX相關/INTEGRATION.md：
  - toastEvents → src/lib/toast/toastEvents.ts
  - Toast.tsx → src/components/Toast.tsx
  - toast-additions.css → 併入 globals.css 檔尾（不要另開無限新 token；沿用既有 --surface/--border/--success/--warn/--danger/--idle）
  - layout.tsx 在 MobileTabbar 附近掛 <ToastHost />
- export showToast(message, variant?, duration?)
- typecheck 過；三主題下 toast 文字可讀（草稿已用 surface 底＋左側色條，勿改回 mockup 實心黃底）

【T2】通知三層（本包建立慣例＋接高頻點，不要求一次清完全站）
規則：
  A. 瞬時成功／輕提示 → showToast(..., "success"|"info")
  B. 進行中 → 維持原地（既有 gen-card、b3-status、按鈕 busy 文案）
  C. 阻斷錯誤 → 欄位旁 field-msg 或既有 modal；可另加 toast error 補強，不刪必要紅字
最低必接（本包）：
  - WorkspaceInputPanel：截圖／規格截圖辨識成功與失敗（見 T3）
  - WorkspaceInputPanel：明顯的「圖片已寫入…」類常駐成功句 → toast（見 T24）
  - 至少 2 處既有「只 setMessage 成功後使用者容易看不到」的瞬時成功改 toast
    （例如儲存版本、快速核准成功——有就接，檔案限 components/listing/）
不要：用 toast 取代 window.confirm；不要大重構 DraftResultsPanel 批次邏輯。

【T3】辨識完成 toast ＋ 列出已填入欄位（老闆本輪 U1）
- 截圖辨識／網址抓取／規格截圖：完成時 toast，例如：
  「辨識完成：已填 標題、成本、規格」（只列本次真正寫入或覆寫的欄位名）
- 若 2A「只填空」導致某欄未動，不要謊稱已填
- 失敗：toast error 或 warn，文案白話
- 進行中可保留「可先填其他欄位」原地提示（既有就好），重點是「好了要跳一下」
- 只改 components + 若 planScreenshotFill 結果已在前端，用現有回傳組字；
  禁止為了這項去改 recognize API（api/ 禁區）

【T11】.schip--warn（及同類用 --warn 當文字色）在 nordic／kitty 對比度
- 問題：--warn 淺黃當 color 近白底幾乎隱形
- 修法建議：狀態字用 color-mix(in srgb, var(--warn) 55%, var(--text)) 或專用
  --warn-text token 三主題都設可讀深色；底／邊仍可用淡 warn
- 檢查 .schip--run、.rc-status 等同一問題一併修
- 三主題用 b15-style-probe 或肉眼說明怎麼驗

【T12】Kitty 主題 --success 與 --accent3 同色（#1d4f91）語意撞車
- 只改 kitty 的 --success 為可辨識的「完成綠」（勿自創一堆色；可對齊 dark/nordic 的 success 感覺，
  在 kitty 上略調到與 accent 橘、accent3 藍都分得開）
- 不改 dark／nordic 的 success 除非必要
- 回報新 hex 與對比意圖一句話

【T24】「圖片已寫入資料庫」等嵌在表單的成功句 → toast
- 搜 ImageUploader／WorkspaceInputPanel 內固定成功 notice
- 改 toast success；不要佔表單常駐高度

【T51】失效 token 小清
- globals.css 殘留 var(--line-soft)、var(--radius)（無 -s/-m/-l）等：
  改成 --border-soft / --radius-s|m|l 或既有等價
- 只修明確失效處；不做全檔美化

════════════════════════════════════
本包明確不做
════════════════════════════════════
- UX-B 桌機三塊／手機分頁 IA
- UX-C 卡片減肥、語氣顯示、退回理由完整改版（Fable P0 reason／P1 tone 進行中）
- Ctrl+V、拖排序、批次工具列、工廠橋接
- 任何 api/、providers、csv、migration
- push／deploy
- 自稱「實機通過」（畫面核帳是 Claude）

════════════════════════════════════
流程
════════════════════════════════════
1. 先貼「修法確認清單」：每個 T# 用 2–3 句說你要怎麼改、動哪些檔。
   等老闆回「指揮放行」再動 code。
2. Commit 建議：
   feat(UX-A-T1): toast host
   fix(UX-A-T11): warn contrast
   fix(UX-A-T12): kitty success token
   feat(UX-A-T3): recognition filled-fields toast
   …可合併合理，但訊息要寫得出 T#
3. 每步：pnpm typecheck
   有 CSS：node scripts/b15-style-probe.mjs（失敗誠實報原因）
4. 收工更新：
   - docs/UIUX總指揮計劃包-2026-07-18.md §9 一行
   - docs/施工清單.md 進度筆記一行
5. 回報老闆（白話）：
   - 做了什麼／改了哪些檔／class 或 token 變更
   - 給 Claude 核畫面步驟（登入→截圖辨識一張→看 toast；切 nordic/kitty 看「處理中」chip；看 kitty 完成態）
   - 需要老闆手動？Y/N

鐵則：只加不減既有能力；找不到可沿用樣式就先問；手機 toast 勿被 tabbar 挡住（草稿已 bottom+safe-area，保持）。
```

---

## 給 Claude 核帳用（UX-A 完工後老闆可另開）

| # | 步驟 | 通過標準 |
|---|---|---|
| 1 | 任意頁操作觸發成功 toast | 底部中央出現，可點關，不被手機 tabbar 完全挡住 |
| 2 | 新增商品→截圖辨識成功 | toast 含「已填 …」欄位列表，與實際有填一致 |
| 3 | 辨識失敗（斷網或壞圖） | 有 error/warn toast 或明確失敗提示 |
| 4 | nordic + kitty：找「處理中／warn」chip | 字可讀，不是淺黃隱身 |
| 5 | kitty：完成態綠／成功色 | 與連結藍、強調橘可分辨 |
| 6 | 表單不再常駐「圖片已寫入資料庫」牆 | 改為短暫 toast |
| 7 | dark 主題抽樣 | toast／chip 無迴歸 |

核完請回 UIUX 指揮：通過項／缺漏截圖說明 → 再開 **UX-B**。
