# UX-B3-P02「生成結果 Header 批次工具列整併重設計」工人開場指令

> **用法**：開**新的 Grok 工人對話**，把下方 ```text``` 框**整段貼上**。  
> **工人**：**Grok**（只實作＋commit feat；**不 push**、不改 docs，除非指令明寫）。  
> **粗包來源**：外部 Claude 第三輪 2／6（已由總指揮潤飾）。  
> **核帳／push**：老闆確認畫面後再代 push。  
> **本輪**：第三輪 包 2／6（B3-P02）。  
> **分支**：`codex/nestory-v0.1-safety-skeleton`（HEAD 約 `fb68dde`／P01 `2ee67a7` 已 push；開工前 `git pull`）。  
> **前包**：B3-P01 ✅ 已核帳 push；B2-P01～P15-r2 勿重做。

---

```text
你是 Nestory【UX-B3-P02｜生成結果批次工具列】工人（Grok）。
UI/UX 總指揮透過老闆轉達；本包規格已潤飾定案，照做即可。

════════════════════════════════════
必讀（只讀這些）
════════════════════════════════════
1. AGENTS.md（UI 鐵則：tokens、.sel／.schip、三主題、禁止新增 !important、不 push）
2. docs/交接指南-給接手的AI模型.md
3. 本包動檔（先完整讀再改）：
   - src/components/listing/DraftResultsPanel.tsx
     · 搜尋：rc-panel-header、results-batch-toolbar、batch-actions、
       selectedIds、allSelected、someSelected、toggleAll、showToolbar、
       openSequentialReview、isCopyStation、isImageStation、isReadyStation
   - src/app/globals.css
     · 搜尋：.rc-panel-header、.rc-header-actions、.rc-header-seq-btn、
       .results-batch-toolbar、.batch-actions、.batch-more、.results-batch-check

════════════════════════════════════
避撞（違規＝打回）
════════════════════════════════════
禁止：
- push／deploy
- 改批次業務邏輯：batchApproveOnly、batchStationReview、batchArchiveOrUnarchive、
  batchSetGenerateDetail、openStation3Modal、busy／batchArm 語意
- 改站別 pills、排序、只看我的 scope、FactoryBridgeStrip、ResultCard 本體
- 改 SequentialReviewOverlay 流程（只允許搬「逐件」按鈕位置，onClick 仍呼叫 openSequentialReview）
- 自創新色 hex；禁止新增 !important
- 大重構 DraftResultsPanel 資料流

可以：
- DraftResultsPanel.tsx header／toolbar JSX 重組
- globals.css 新增 .rc-header-select-all、.rc-batch-strip、.rc-batch-count 等
  （顏色只用 var(--accent)／var(--surface2)／var(--border)／color-mix 等既有 token）
- 調整／收斂 .results-batch-toolbar 樣式（可改名或標 deprecated 若已不再渲染）
- 手機 ≤959 讓批次條滿寬可讀

════════════════════════════════════
問題根因（已對照程式碼確認，2026-07-21）
════════════════════════════════════
現況（DraftResultsPanel 約 L1362–1552）：

1. panel-header 右側：只有「▶ 逐件審核／標圖」（B2-P02 只搬了這顆）
2. panel-body 內仍有整條 .results-batch-toolbar：
   - 永遠（showToolbar 時）渲染：全選 checkbox + 說明文字
   - 未選取時文字＝「勾選商品以使用批次操作」→ 空盪大長條
   - selectedIds.size > 0 才在條內塞 .batch-actions

老闆要的是整併重設計，不是只搬家單一按鈕：
- 未選取 → header 精簡（全選 + 逐件），**不要**空長條
- 有選取 → header 下方一條**有底色**的批次動作條（已選 N 筆 + 原 batch 按鈕）

════════════════════════════════════
本包只做：工具列 IA 重排（行為不變）
════════════════════════════════════

【P02-A】Header 右側精簡列（無選取也要有）
────────────────────────────────
條件：維持 showToolbar 語意（有佇列才顯示全選；無草稿時不要空全選）
  const showToolbar = workQueueDrafts.length > 0 || scopedDrafts.length > 0;
  （既有，勿改計算）

結構示意：
<div className="panel-header rc-panel-header">
  <h2>◈ 生成結果（三站工作佇列）</h2>
  <div className="rc-header-actions">
    {showToolbar ? (
      <label className="rc-header-select-all">
        <input
          checked={allSelected}
          onChange={toggleAll}
          ref={(el) => { if (el) el.indeterminate = someSelected; }}
          type="checkbox"
          aria-label="全選目前列表"
        />
        <span>全選</span>
      </label>
    ) : null}
    {isCopyStation || isImageStation ? (
      <Button
        size="sm"
        className="rc-header-seq-btn"
        disabled={busy || visibleDrafts.length === 0}
        onClick={openSequentialReview}
        title={…既有 title 字串保留…}
        type="button"
      >
        {isImageStation ? "▶ 逐件標圖" : "▶ 逐件審核"}
      </Button>
    ) : null}
  </div>
</div>

要點：
- 逐件按鈕仍只在站①／②；title／disabled／文案**原封不動**
- 全選 checkbox 邏輯沿用 allSelected／someSelected／toggleAll（indeterminate 保留）
- .rc-header-seq-btn 的 margin-left:auto 若放在 .rc-header-actions 內可能多餘；
  以 actions 容器 margin-left:auto 為準即可（CSS 可微調，勿破桌機 header）

【P02-B】有選取才出現批次動作條（取代空長條）
────────────────────────────────
1. **刪除或停止渲染**未選取時的 .results-batch-toolbar 空長條
   （含「勾選商品以使用批次操作」那句）
2. 僅當 selectedIds.size > 0 時渲染精簡條（建議放在 panel-body 頂、
   gen-card 之後／stage-filter 之前——與現 toolbar 同位即可）：

{selectedIds.size > 0 ? (
  <div
    className="rc-batch-strip"
    role="toolbar"
    aria-label="批次操作"
  >
    <span className="rc-batch-count">已選 {selectedIds.size} 筆</span>
    <div className="rc-batch-actions batch-actions">
      {/* 把現有 isCopyStation / isImageStation / isReadyStation 的
          batch-actions 子樹原封搬進來——Button 的 onClick／title／
          variant／loading／更多 menu 一律不改語意 */}
    </div>
  </div>
) : null}

3. 不再依賴「空 toolbar + 內層條件」；未選取＝整條不存在
4. showToolbar 與「有選取才顯示 strip」的關係：
   - 無草稿：header 無全選、無 strip
   - 有草稿未選：header 有全選（+ 站①②逐件）、無 strip
   - 有選取：header 全選 + strip 含已選 N 筆與動作

【P02-C】CSS（globals.css，tokens only）
────────────────────────────────
新增（可用粗包數值；色必須 token／color-mix）：

.rc-header-actions {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 10px;
  margin-left: auto;
}
/* 檔內已有 .rc-header-actions——以重用／微調為主，勿重複衝突規則 */

.rc-header-select-all {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  font-size: 12px;
  color: var(--text-muted);
  cursor: pointer;
  user-select: none;
  white-space: nowrap;
}
.rc-header-select-all input {
  width: 16px;
  height: 16px;
  margin: 0;
  cursor: pointer;
}
@media (max-width: 959px) {
  .rc-header-select-all input {
    width: 20px;
    height: 20px; /* 對齊卡片 checkbox 觸控 */
  }
}

.rc-batch-strip {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 10px;
  padding: 8px 14px;
  margin: 0 0 10px;
  border-radius: var(--radius-s);
  background: color-mix(in srgb, var(--accent) 8%, var(--surface2));
  border: 1px solid color-mix(in srgb, var(--accent) 25%, var(--border));
  /* sticky 可保留方便捲動仍見批次鈕；若 sticky 與 panel overflow 衝突可改 static */
  position: sticky;
  top: 0;
  z-index: 5;
}
.rc-batch-count {
  font-size: 12px;
  font-weight: 700;
  color: var(--accent);
  flex-shrink: 0;
  white-space: nowrap;
}
.rc-batch-strip .batch-actions,
.rc-batch-actions {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 6px;
  flex: 1 1 auto;
  min-width: 0;
}

手機（≤959）：
.rc-batch-strip {
  width: 100%;
  box-sizing: border-box;
}
.rc-batch-strip .batch-actions {
  width: 100%;
}
.rc-batch-strip .nb-btn,
.rc-batch-strip .batch-primary-action.nb-btn,
.rc-batch-strip .batch-more > summary.nb-btn {
  min-height: 44px;
}
.rc-panel-header {
  /* 標題 + actions 可換行，勿橫向爆版 */
  gap: 8px;
}
.rc-header-actions {
  width: 100%;
  margin-left: 0;
  justify-content: flex-end; /* 或 space-between：全選左、逐件右——選可讀者 */
}

既有 .results-batch-toolbar：
- 若 JSX 已不使用，可保留 CSS 不動（無害）或加註解「B3-P02 改 .rc-batch-strip」
- 不要留半殘：JSX 不得再渲染空的 results-batch-toolbar

.batch-more 定位／menu 樣式繼續沿用；若 strip 內 menu 被 overflow 裁切，
可把 strip overflow: visible（panel.results-panel 已 overflow:visible）

════════════════════════════════════
不做／不要誤傷
════════════════════════════════════
- 不改 ResultCard 列上單筆 checkbox
- 不改 stage-filter-row／sort／scope
- 不改 gen-card 進度區
- 不改批次 double-confirm（batchArm）
- 不新增第二套批次 handler
- 站③「發布／匯出」批次鈕要在有選取的 strip 裡（與現況一致）

════════════════════════════════════
驗收清單（做完自勾）
════════════════════════════════════
未選取：
[ ] header 右側：全選（有佇列時）+ 站①② 的逐件按鈕
[ ] **沒有**「勾選商品以使用批次操作」空長條
[ ] 沒有佔高的空白 .results-batch-toolbar

有選取：
[ ] header 下方（或 body 頂）出現 .rc-batch-strip
[ ] 顯示「已選 N 筆」（N 正確）
[ ] 站①：批次核准 + 更多（移出佇列）可用
[ ] 站②：批次標圖通過 + 更多（開／關詳情圖、移出）可用
[ ] 站③：發布／匯出可用
[ ] 全選／半選 indeterminate／取消選取後 strip 消失

手機 ≤959：
[ ] strip 滿寬可讀，按鈕可點（≥44px）
[ ] header 不橫向爆版
[ ] 三主題（dark／nordic／kitty）strip 底色用 accent mix，可辨識

全域：
[ ] typecheck 綠
[ ] 無新增 !important
[ ] 只動 DraftResultsPanel.tsx + globals.css（必要時）

════════════════════════════════════
收工格式
════════════════════════════════════
1. commit 建議：
   feat(UX-B3-P02): compact results header select-all; batch strip when selected
2. **不要 push**
3. 回報：動檔、驗收自勾、typecheck、是否需代 push

════════════════════════════════════
開工第一則回覆
════════════════════════════════════
先交「修法確認」（≤6 行）：
1. 未選取：header 全選+逐件；刪空 toolbar
2. 有選取：.rc-batch-strip + 已選 N + 原 batch-actions
3. showToolbar 門檻保留
4. 動檔清單
5. sticky 是否保留在 strip（預設：是）
6. Ambiguity（有才問）

確認後直接實作。
```

---

## 總指揮備註（不貼給工人）

| 項 | 內容 |
|---|---|
| 工人 | **Grok** |
| 粗包 | 外部 **Claude** 第三輪 2／6 |
| 代號 | UX-B3-P02 |
| 痛點 | 空「勾選商品以使用批次操作」長條仍在；只搬了逐件鈕 |
| 定案 | 未選＝header 精簡；有選＝accent 淡底批次條 |
| 行為 | 批次 handler／逐件 title **零語意變更** |
| 推送 | 工人不 push；核帳後代 push |
