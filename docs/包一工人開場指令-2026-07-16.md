# 包一「表單穩定性」工人開場指令（貼給工人的完整內容在下方框內）

> 用法：開新工人對話，整段貼上。工人交「修法確認清單」後貼回給 Fable 放行再動 code。

```text
請依序讀這三份，其他文件等做到相關項目再讀：
1. docs/交接指南-給接手的AI模型.md
2. docs/施工清單.md（唯一進度真相，先看進度筆記頂部）
3. docs/實機測試回饋-2026-07-16.md（本包依據；引用編號，不要重新編號）

你是本包工人，總指揮是 Fable（透過老闆轉達）。本包代號【包一｜表單穩定性】，
只做下列 5 項，全部是老闆實機測出的 bug，指揮已完成診斷，照診斷修，不要重查案：

【1】表單資料消失（回饋 6）
- src/components/listing/ImageUploader.tsx：
  a. beginUpload/endUpload 在 setState updater 內呼叫父層 onUploadingChange
     → React「Cannot update a component while rendering」錯誤。把 onUploadingChange
     移出 updater（用 useEffect 監聽 uploadingCount 變化再通知父層）。
  b. 每次上傳完（149 行）與每次規格圖 toggle（186 行）都 router.refresh() → 頁面重刷風暴
     （伺服器 log 出現十幾次 GET /drafts/new），表單狀態被洗掉。本元件已維護本地 previews
     state，移除這兩處 refresh；若有依賴 refresh 的下游，改事件/回呼通知。
- 加碼防線：輸入區主要欄位自動暫存 localStorage（節流數秒），重開頁還原＋「已還原上次未完成的填寫」提示。

【2】上傳體驗＋還原載圖（回饋 2、16）
- 拖圖進來立即用 URL.createObjectURL 顯示本地縮圖（狀態「上傳中…」），背景逐張上傳完成後
  替換為正式 URL；失敗的那張標紅可重試，不整批卡住。
- 每張縮圖加刪除 ×（刪 DB row＋Storage 檔案 best-effort）。
- 還原草稿時把該草稿已上傳的 product_images 撈回 previews 顯示（回饋 16），
  取代「伺服器上已有 N 張圖」的文字提示。
- 拖拉排序本包可不做（記進度筆記留給包三）。

【3】image_status 語意分離（回饋 52，根因已確認）
- /api/analyze-images（Vision）寫 image_status=processing/done，
  與 D3/D5 圖片處理管線共用同一欄 → 沒送圖的草稿全部跑進 /review 佇列。
- 修法方向（你提方案）：Vision 改寫獨立欄（migration 只產檔）或 image_flags 內 key；
  /review 佇列查詢（src/components/review/ImageReviewPanel.tsx）只認真正送圖管線的狀態。
- 驗收：生成過文案但沒送圖的草稿，不得出現在 /review。

【4】錯誤誠實化（027 教訓＋回饋 50 的顯示部分）
- src/lib/drafts/publishBatch.ts isMissingPublishBatchesError 過寬（含表名就當缺表）。
  已發生真實事故：RLS 遞迴 42P17 被翻成「請執行 027」（migration 028 已修 DB 端）。
  收緊為：只認缺表訊號（42P01、PGRST205、"does not exist"＋表名、"schema cache"＋表名），
  其他錯誤顯示原文。E3 Make 額度卡若用同款 matcher 一併收緊。
- /review 失敗卡顯示失敗原因：吐出 product_images.processing_error 與 draft warnings 相關句，
  取代只有「處理失敗」一句（一鍵重生按鈕屬包三，本包不做）。

【5】定價修正（回饋 8、49、55）
- 款式成本繼承：款式列成本空白＝跟隨上方商品成本（欄位灰字提示「同商品成本 NT$xxx」），
  填了才覆蓋；「售價未算」只在上下都沒填時出現。
- 必填擇一：上方成本或「每一款式列成本齊全」擇一滿足即可送生成；只填款式但沒填齊 → 紅字擋。
- 卡片價格顯示區間（多款式不同價時顯示如 269~279）。
- 查證回饋 55 的疑似跨稿污染：吉伊卡哇（卡片 1580/定價2280）Showmore CSV 卻輸出原價 4480
  （恰為另一商品米菲的售價）。追 Showmore 匯出的原價取值來源，找到根因修掉；
  若查證結果是使用者改過價，誠實回報即可不硬修。

鐵則與驗收：
- 先貼「修法確認清單」（每項 2–3 句你打算怎麼改＋動哪些檔），等指揮放行再動 code。
- 每項一個 commit：fix(P1-1)…fix(P1-5) 前綴；每步 npm run typecheck；
  跑 node scripts/verify-b14-image-batch.mjs、verify-d2-auto-chain、verify-d5-image-review、
  verify-d8 系列（匯出）確認不破；UI 有動就跑 b15-style-probe（失敗誠實回報）。
- 【3】若需 SQL 只產檔（029 起跳），不跑 CLI。不 push。BX-P 禁止順手美化。
- CSS 動之前先讀 src/app/globals.css，只用現有 tokens。
- 收工報告：changed files、commits、每項驗收步驟、對照回饋編號念出完成/未完成。
- 完成後照 AGENTS.md 收工儀式收尾（✅ 本包完成…建議現在開新對話…）。
- 你無法跑登入後的真瀏覽器實測——不用勉強，寫清楚「待指揮實測」即可，
  Fable 會在內建瀏覽器做驗收；你能做的是把 verify 腳本補到位。
```
