# UX-B4-P01「Tags 分頁 — 顏色分類／× 移除／＋新增」工人開場指令

> **用法**：開**新的 Grok 工人對話**，把下方 ```text``` 框**整段貼上**。  
> **工人**：**Grok**（只實作＋commit feat；**不 push**、不改 docs，除非指令明寫）。  
> **來源**：老闆截圖 #1 + Mockup Tags 分頁；總指揮同步 `docs/UIUX本輪改動同步-2026-07-21.md`。  
> **分支**：`codex/nestory-v0.1-safety-skeleton`（開工前 `git pull`／對帳 log）。  
> **前輪**：B2-P08 已有前綴分組；B3 第三輪已收；**勿重做分組**，本包是上色＋chip 增刪 UX。

---

```text
你是 Nestory【UX-B4-P01｜Tags 分頁 Mockup 化】工人（Grok）。
UI/UX 臨時代理指揮透過老闆轉達；規格已定案，照做即可。

════════════════════════════════════
必讀（只讀這些）
════════════════════════════════════
1. AGENTS.md（tokens、.sel／.schip、三主題、禁止新增 !important、不 push）
2. docs/交接指南-給接手的AI模型.md（鐵則／地雷）
3. docs/UIUX本輪改動同步-2026-07-21.md §2.1（本包定案）
4. Mockup：docs/mockups/nestory-v7-mockup.html 搜尋 rc-tag t-ip / Tags 分頁
5. 本包動檔（先完整讀再改）：
   - src/components/listing/result-card/ResultCardTagsPanel.tsx
   - src/lib/drafts/tagDisplayGroups.ts（可擴 helper，非必須）
   - src/app/globals.css（.rc-tag、.rc-tags-grouped 一帶）
   - 可對照：src/lib/nestoryTagsV2.ts 的 NESTORY_TAG_PREFIX_ORDER

════════════════════════════════════
避撞（違規＝打回）
════════════════════════════════════
禁止：
- push／deploy
- 改 tag 規則引擎產出語意（nestoryTagsV2 生成邏輯）
- 改 /api/generate、migrations
- 拆掉 B2-P08 分組列（label 列保留）
- 自創 hex 色；禁止新增 !important
- 重做警告一鍵新增角色（既有 Button 保留）

可以：
- ResultCardTagsPanel UI 與 onTagsChange 字串編輯
- globals.css 為 .rc-tag 加前綴 modifier（color-mix 現有 --accent／--accent2／--accent3／--success／--warn）
- 小 helper：prefix → className

════════════════════════════════════
現況（已確認）
════════════════════════════════════
- 有 groupTagsByPrefix 分組 + .rc-tag 單一樣式（無色差）
- 底部一整條 comma-separated edit-input
- Mockup：.rc-tag.t-ip / t-char / t-type / t-ctx / warned + 每顆 × + .rc-tag.add「+ 新增」

════════════════════════════════════
本包只做
════════════════════════════════════

【A】顏色分類
1. 依 tag 前綴套 class（建議映射，可微調命名但語意一致）：
   - IP_ → t-ip（accent 淡底）
   - 角色_ → t-char（accent3）
   - 類型_ → t-type（accent2）
   - 定位_／主題_／銷售_／品相_／來源感前綴 → t-ctx 或分組用 success 淡底
   - 含「需修改」／警告語境 → warned（warn 色；若現有 warning chip 邏輯可接）
   - 其他 → 維持中性 .rc-tag
2. 顏色寫法對齊 Mockup：background: color-mix(in srgb, var(--accent) 13%, var(--surface)); 等
3. 三主題（dark／nordic／kitty）肉眼合理即可

【B】每顆 × 移除
1. chip 內 button aria-label="移除 {tag}"
2. 點 ×：stopPropagation；從 tags 字串移除該完整 tag，onTagsChange 回傳新字串
3. 保留逗號分隔格式與 ResultCard 既有 save 路徑（不新 API）

【C】＋新增
1. 分組區或 chips 列尾：虛線「+ 新增」chip（對齊 Mockup .rc-tag.add）
2. 點擊 → 顯示小型 input（或 contentEditable）；Enter／確認加入；Esc 取消
3. 去空白；已存在則不重複加入；可選擇自動補前綴提示但**不要猜**前綴強制改寫——使用者打什麼加什麼（可 placeholder 示範「角色_某某」）
4. 主操作改為 chip 增刪後：底部 raw edit-input 可：
   - 方案 A（建議）：收進 details「編輯完整字串」次要
   - 方案 B：保留但降權（muted 小字）
   擇一實作，註明於收工報告

【D】保留
- 分組 label 列
- 提醒區＋一鍵新增角色規則庫

════════════════════════════════════
不做
════════════════════════════════════
- Collections 專區 UI（Mockup 有但本包不強制，除非同一面板已有資料可順手只讀顯示——預設不做）
- 改 Shopify tags 發布 payload

════════════════════════════════════
驗收
════════════════════════════════════
[ ] Tags 分頁可見多色 chip（至少 IP／角色／類型有色差）
[ ] × 可移除且 state／之後存檔字串正確
[ ] ＋新增可加入新 tag
[ ] 分組與警告一鍵新增仍在
[ ] 三主題無崩；無新 !important
[ ] typecheck 綠
[ ] 不 push

════════════════════════════════════
收工
════════════════════════════════════
commit：feat(UX-B4-P01): tags chip colors, remove, and add
回報：動檔、前綴→class 表、raw input 採 A 或 B。

開工先回 5 行：
1. 前綴 class 映射表
2. 移除如何改 tags 字串
3. 新增 UI 放哪
4. raw input 處理
5. 是否動 tagDisplayGroups
```

---

## 總指揮備註

| 項 | 內容 |
|---|---|
| 體量 | 中小；純 UI |
| 雷 | tags 是 comma string，移除要比對完整 trim 後 token |
| 推送 | 核帳後代 push |
