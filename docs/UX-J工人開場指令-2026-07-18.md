# UX-J「表單殼＋設定誠實」工人開場指令

> 用法：開**新**工人對話，把下方 ```text``` 框**整段貼上**。  
> **依賴**：建議 UX-I 核過後再開（可與 I 同日但勿同檔大衝突）。  
> 前包：A～H ✅；I＝操作回饋。本包＝二手 UI＋設定連線差額＋狀態語彙減噪。

---

```text
你是 Nestory【UX-J｜表單殼＋設定誠實】工人。
UI/UX 總指揮透過老闆轉達；Fable 避撞。

════════════════════════════════════
必讀
════════════════════════════════════
1. docs/交接指南-給接手的AI模型.md
2. docs/UIUX總指揮計劃包-2026-07-18.md §1.4、§6 UX-J（T52 T56 T58）
3. AGENTS.md UI 鐵則
4. docs/mockups/nestory-v7-mockup.html —— 二手 sh-switch ＋ secondhand-fields（功能/IA 對齊；視覺用現站 tokens）
5. 現況：
   - WorkspaceInputPanel.tsx（sale_status 下拉有「二手現貨」，缺獨立切換與等級/品況欄）
   - domain：is_secondhand、secondhand_grade、secondhand_condition、secondhand_notes 已存在
   - contentGenerator validation 已要求二手等級等——UI 要讓老闆填得到
   - SettingsPanel.tsx：ConnRow「Vision / Image Provider」ok={null} 寫死佔位（R58）
   - StatusBadge vs 站籤（Claude 1-2 雙語彙）

════════════════════════════════════
避撞
════════════════════════════════════
禁止：api/、providers/、csv/、migration、prompt
禁止：改 tag 規則引擎、改 is_secondhand 後端語意（只接既有欄位寫入 draft／generate payload）
可以：components、globals、純顯示

════════════════════════════════════
本包只做：T52、T56、T58
════════════════════════════════════

────────
【T52】二手模式 UI（R59）
────────
定案（對齊 Mockup IA，視覺現站）：
1. 輸入區標題列（panel-hdr 旁）加 **一般｜二手** 切換（.sel 選取語意；可做 switch 或二顆 pill）。
2. 二手＝on 時展開專屬欄：
   - 等級：SS／S／A／B／C／D（對齊 SECONDHAND_GRADES／現有後端字串）
   - 狀況描述 secondhand_condition
   - 備註 secondhand_notes
3. 連動：
   - 開二手 → is_secondhand true；sale_status 可自動偏向「二手現貨」若合理且不強制覆蓋使用者已選的其他狀態——**修法清單寫清規則，等指揮若需再拍**
   - 關二手 → 清或保留欄位：建議保留草稿值但隱藏；送出時 is_secondhand false
4. 生成 payload／draft 寫入必須帶上上述欄位（對齊現有 generate body 欄位名；**不新造 API**）。
5. 手機：欄位在手風琴合理段；觸控 ≥44px。

不做：改標題引擎怎麼組【二手】字（那是 Fable 已有邏輯）。

驗：切二手→填 SS→生成請求／草稿列看得到欄位；切回一般 UI 收合。

────────
【T56】狀態語彙減噪（Claude 1-2）
────────
痛點：站籤（pipeline）與 StatusBadge（DraftStatus）並陳易混。

指揮定案（輕量，不重做資料模型）：
1. **工作檯結果卡收合列**：主狀態以**站／流程文案**為主（審文案／標圖／待發布／失敗）；
   StatusBadge 若與站重複（例如站①又掛 ready_for_review）→ 收斂只留一個主要 chip，或 Badge 改顯示「更細的子狀態」且中文。
2. **佇列 DraftQueueList**：可保留多 Badge，但 csv_ready／archived 等必須是 UX-H map 後正確色。
3. 不做 DB 廢除 status 欄；不做英文 stage 字曝光給使用者。
4. 若某处仍寫「圖片審核」指站② → 改「標圖」（顯示層 only）。

驗：老闆掃一眼卡片不會同時看到兩套互相打架的「待審／已上架」綠。

────────
【T58】設定頁 Vision／Image 連線誠實（R58）
────────
痛點：Settings 寫死 Phase D 佔位 ok={null}。

指揮定案：
1. **優先**接既有「重新檢查連線」同一條 status 資料：若 response 已有 openai／image 欄就顯示真偽。
2. 若現有 status **沒有** image 欄：
   - **禁止**本包新開 api route（避撞）
   - UI 改誠實文案：「需伺服器連線檢查（尚未接）／請看 Vercel 是否設 OPENAI_API_KEY」類，
     **不要**再顯示假的「Phase D 圖管線」就緒感
   - 可在修法清單註「需 Fable 補 status 欄位」——UI 先去假
3. 有欄位則 ConnRow ok 真綠／真紅，與 Shopify／Supabase 列一致。

驗：設定→連線：Vision/Image 不再像「已就緒但其實沒查」。

════════════════════════════════════
本包不做
════════════════════════════════════
T47/T50、OCR、tag 規則資料、push

════════════════════════════════════
流程
════════════════════════════════════
1. 修法清單（T52 連動 sale_status 規則必寫）→ 指揮放行
2. commits：feat(UX-J-T52)／fix(UX-J-T56)／fix(UX-J-T58)
3. typecheck；b15-probe
4. §9＋施工清單；不自稱通過
```

---

## 核帳表（UX-J）

| # | 通過標準 |
|---|---|
| 1 | 二手切換在輸入區可見；開則等級／品況／備註 |
| 2 | 二手欄會進草稿／生成相關 state（DevTools 或 UI 回讀） |
| 3 | 卡片狀態不雙語彙打架；無「圖片審核」指站② |
| 4 | 設定 Vision/Image 非假就緒 |
| 5–6 | 三主題 1280＋375 |
```
