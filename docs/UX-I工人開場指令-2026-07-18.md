# UX-I「操作回饋補完」工人開場指令

> 用法：開**新**工人對話，把下方 ```text``` 框**整段貼上**。  
> 工人先交「修法確認清單」→ 老闆轉 UIUX 指揮放行 → 再動 code。  
> 畫面核帳：Claude／核帳員。  
> **前包**：UX-A～H ✅。本包＝主線後**剩餘 UI 債第一包**（老闆指示：後段不要堆著，全做完）。

---

```text
你是 Nestory【UX-I｜操作回饋補完】工人。
UI/UX 總指揮透過老闆轉達；系統總指揮是 Fable（避撞）。

════════════════════════════════════
必讀
════════════════════════════════════
1. docs/交接指南-給接手的AI模型.md
2. docs/UIUX總指揮計劃包-2026-07-18.md
   ——§1.4 避撞、§6 UX-I（T47 T53 T54 T55 T57）、§9 進度
3. AGENTS.md UI 鐵則（globals.css tokens；.sel／.schip；三主題；不 push）
4. 現況檔：
   - src/components/listing/ImageUploader.tsx、WorkspaceInputPanel.tsx（拖圖／貼上）
   - src/components/listing/DraftResultsPanel.tsx、ResultCard.tsx（失敗站）
   - src/components/review/ImageReviewPanel.tsx（已有 formatReviewFailReasons，查缺口）
   - src/lib/images/imageReview.ts（formatReviewFailReasons）
   - src/app/globals.css（.modal-overlay 手機已有 flex-end——對齊所有 modal）
   - Modal 們：ApproveSummaryModal、Station3PublishModal、ExportPreflightModal、
     ProductLibraryModal、RegenCopyModal 等
   - 仍用 setError 的：ProductLibraryModal、DashboardTodoPanel、PublishRecordsPanel 等
5. 痛點：BX3 全頁拖圖；R50 失敗原因；Claude 2-2 手機 bottom sheet；Toast 殘餘；Esc 快捷

════════════════════════════════════
避撞（強制）
════════════════════════════════════
禁止：src/lib/providers/**、src/lib/csv/**、src/app/api/**、migrations、prompt／標題引擎
禁止：改 publish／generate 語意、改失敗計數公式
禁止：push／deploy；自稱實機通過
可以：components/**、globals.css、純顯示 helper（src/lib/drafts 或 images 僅顯示格式化）

════════════════════════════════════
本包只做：T47、T53、T54、T55、T57
════════════════════════════════════

────────
【T47】全頁拖圖遮罩（BX3）
────────
痛點：拖圖進瀏覽器時，若只在小 dropzone 才接，容易「拖到空白處沒反應」或整頁開檔。

指揮定案：
1. 在**工作檯新增頁**（含手機「新增」）當使用者從系統拖**檔案（圖片）**進入視窗時：
   - 出現**全頁半透明遮罩**（沿用 surface／accent token，禁止自創色）
   - 文案一句：「放到這裡上傳商品圖」或分區提示（主圖／詳情——若技術上只能進預設區，寫清楚預設進主圖）
2. drop 落點：優先落到現有 ImageUploader 主圖區 pipeline（與點選上傳同一套）；
   不可另寫第二套 storage 上傳。
3. 拖離／放開／Esc → 遮罩消失；**純文字拖曳**不要攔截（對齊 UX-D 貼上規則）。
4. 不影響既有 dropzone 內拖排序（T22）。

動檔建議：WorkspaceInputPanel 或 layout 級 client 監聽 dragenter/dragleave/drop；
globals `.page-drop-mask` 類。

驗：從資料夾拖 JPG 進頁→遮罩→放開進上傳列；三主題＋手機 OK。

────────
【T53】失敗原因顯示補齊（R50 差額）
────────
痛點：失敗件只有「處理失敗」不夠；DB 已有 processing_error／warnings／generation_error。

指揮定案：
1. **工廠**（ImageReviewPanel）：展開失敗已有 formatReviewFailReasons——確認：
   - 收合列也有**一行**短原因（truncate），不要只寫「失敗」
   - processing_error 有值必顯；多圖失敗可「首則＋另 N 則」
2. **工作檯失敗籤／失敗卡**（DraftResultsPanel／ResultCard）：
   - generation_error 或 validation／warnings 摘要**可見**
   - 有合理主動作時保留「重生／重試」入口（已有則不刪；沒有不硬造 API）
3. 白話、不丟 stack trace；空原因才寫「原因未記錄，請重試或看紀錄」。
4. 不改 worker／API 寫入邏輯。

驗：故意失敗或既有 failed 件——收合＋展開都能看到原因字。

────────
【T54】Modal 手機 bottom sheet 對齊
────────
痛點 Claude 2-2：手機 modal 應貼底滑出。globals 已有 `.modal-overlay` @960 flex-end——
可能有漏網 modal 仍置中或自寫 overlay。

指揮定案：
1. 盤點所有 modal／dialog overlay class。
2. 統一：&lt;960px 貼底、圓角上方、可捲、關閉鈕 ≥44px；桌機維持置中。
3. 已符合的不重寫；缺的改 class 或補 media query。
4. 不做新動畫庫。

驗：手機 375 開核准／發布／商品庫／匯出預檢——皆底部抽屜感，無橫溢。

────────
【T55】殘餘錯誤回饋改 toast
────────
痛點：Toast 已落地，但 Library／Dashboard／Records／部分上傳仍局部 setError 黃條。

指揮定案：
1. **瞬時成功／失敗**改 `showToast`（error／success／warn）。
2. **阻斷性「整頁不能用」**（未登入、缺 env）可保留頁內 notice——不要全改成一閃就沒的 toast。
3. 優先：ProductLibraryModal、DashboardTodoPanel、PublishRecordsPanel、ImageUploader 的
   操作失敗（刪圖失敗、標記失敗等若還只 setMessage）。
4. 不重做 Toast 底座。

驗：觸發一個載入失敗／操作失敗——有 toast 或合理頁內阻斷，不兩套打架。

────────
【T57】快捷鍵最小集
────────
來源：交接／計畫 Esc 關彈窗；BX1 加分縮到最小可用。

指揮定案：
1. **Esc**：所有仍開著的 modal／重要 popover（⋯更多已有）可關；busy 中是否可關——對齊各 modal 現況（有的 busy 禁 Esc 則保留）。
2. **漏網補 Esc**（Station3／Regen／其他自寫 overlay）。
3. **可選加分**（時間夠再做，清單註明做沒做）：
   - 工作檯表單 focus 在非 textarea 時 **Ctrl+Enter**＝按生成（textarea 內 Ctrl+Enter 勿搶換行——若衝突則只做 Esc）
4. 不做全站 vim 快捷、不做改審核流程的隱藏鍵。

驗：Esc 關得掉主要 modal；不破壞輸入中文。

════════════════════════════════════
本包不做
════════════════════════════════════
- T52 二手 UI、T50 next/font、T58 設定 Vision 檢查 → **UX-J／K**
- BX-P 無截圖亂改像素
- Fable：OCR／定價／CSV 多款式／影片／api

════════════════════════════════════
流程
════════════════════════════════════
1. 先貼「修法確認清單」（每項 2–3 句＋擬改檔）等「指揮放行」。
2. Commit 建議：
   feat(UX-I-T47): full-page image drop mask
   fix(UX-I-T53): surface fail reasons workbench and factory
   style(UX-I-T54): mobile modal bottom sheet audit
   refactor(UX-I-T55): residual errors to toast
   feat(UX-I-T57): escape closes remaining modals
3. pnpm typecheck；CSS → node scripts/b15-style-probe.mjs
4. 計劃包 §9、施工清單一行
5. 回報核帳步驟；不自稱實機通過

鐵則：tokens only；三主題＋手機；避撞；不 push。
```

---

## 給核帳員（UX-I 完工後）

| # | 步驟 | 通過標準 |
|---|---|---|
| 1 | 全頁拖圖 | 拖圖進新增頁有遮罩，放開進上傳，不開成瀏覽器開檔 |
| 2 | 工廠失敗 | 收合／展開看得到原因（非空白「失敗」） |
| 3 | 工作檯失敗 | 失敗卡／籤有 generation_error 或同等摘要 |
| 4 | 手機 modal | 主要 modal 貼底、可關、≥44px |
| 5 | toast | Library／儀表板／紀錄操作失敗有合理回饋 |
| 6 | Esc | 主要 modal 可 Esc 關 |
| 7 | 三主題 1280 | 無爆版 |
| 8 | 手機 375 | 遮罩與 modal 可用 |

## 老闆可順驗

- 拖圖手感、失敗件能不能看懂原因、手機彈窗是否像 App  
```
