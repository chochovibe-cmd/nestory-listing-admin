# UX-F「站②＋生圖工廠橋接」工人開場指令

> 用法：開**新**工人對話，把下方 ```text``` 框**整段貼上**。  
> 工人先交「修法確認清單」→ 老闆轉 UIUX 指揮放行 → 再動 code。  
> 畫面核帳：Claude。  
> **前包**：UX-A～E ✅（2026-07-18）。

---

```text
你是 Nestory【UX-F｜站②＋工廠橋接】工人。
UI/UX 總指揮透過老闆轉達；系統總指揮是 Fable（避撞）。

════════════════════════════════════
必讀
════════════════════════════════════
1. docs/交接指南-給接手的AI模型.md
2. docs/UIUX總指揮計劃包-2026-07-18.md
   ——§1.3 工廠橋接、§1.4 避撞、§2.1 雙軌（三站＋工廠獨立頁）、§2.5 站② 卡片、§6 UX-F（T29 T30 T35）
3. AGENTS.md UI 鐵則
4. 現況：
   - ResultCard.tsx（isImageStation、標記、stationReview、images tab）
   - DraftResultsPanel.tsx（批次審核、工作佇列資料）
   - QuickPreviewPanel／StationJumpStrip（跳轉模式可參考）
   - src/lib/images/imageReview.ts（classifyReviewQueueItem、queue kinds、banner）
   - src/lib/drafts/stationRoute.ts（decideStation2Review、formatAiEstimateMessage）
   - ImageUploader.tsx（上傳／DnD 排序可複用模式，勿整抄破壞）
   - /review ImageReviewPanel（工廠本體——本包不重做工廠內滑桿）

════════════════════════════════════
避撞與資料策略（Fable 曾說 UX-F 前確認 query）
════════════════════════════════════
禁止：src/lib/providers/**、src/lib/csv/**、src/app/api/**、migrations、prompt
禁止：push／deploy；自稱實機通過
可以：components/**、globals.css、lib/drafts／lib/images **純分類／顯示** helper

資料策略（本包預設，不新 API）：
- 工作檯 page 已 load drafts + product_images → 橋接列用**既有 props** 在 client 分類
- 優先重用 imageReview.ts 的分類語意（排隊中／待驗／失敗），參數不足就**誠實少顯示**，不假 ETA
- 若某狀態無法從現有欄位分辨：空狀態寫「請到生圖工廠查看」+ Link /review，不硬猜
- 寫 sort_order／補圖：supabase client 直寫（同 ImageUploader／ResultCard 標記），不新 route

════════════════════════════════════
本包只做：T29、T30、T35
════════════════════════════════════

────────
【T29】右側／結果區「工廠橋接」迷你列（不是第 4 站）
────────
定案（§2.1）：
- 三站籤下方或結果面板頂部（站籤與卡片之間）加 **FactoryBridgeStrip**（名可自訂）
- 內容：
  - 標題：🏭 生圖工廠
  - 摘要：排隊 N · 待驗 M · 失敗 K（0 可省略該段）
  - 迷你卡：縮圖＋短標＋狀態 chip（排隊中／待驗／失敗）
  - CTA：「打開工廠 →」→ /review（可帶 ?section=pending 或既有 query 若 imageReview 支援）
- 點迷你卡：優先 router.push(`/review` + 可定位的 query／hash）；若無定位能力就打開工廠即可
- **N=M=K=0**：整列可隱藏，或一行 muted「目前沒有在工廠的商品」
- **不要**在橋接列做滑桿驗圖、不要改標記

分類建議（純 client，對齊工廠語意）：
- 參考 classifyReviewQueueItem / image_status / current_image_batch_id / process_intent
- 僅列出「真的進過送圖／處理管線」的稿，不要把站① 文案稿塞進來

動檔建議：新 FactoryBridgeStrip.tsx、DraftResultsPanel 組裝、可選 lib helper、globals 小樣式

────────
【T30】站② 展開：主圖｜規格圖｜詳情圖 分頁 + 排序 + 補圖
────────
定案（§2.5／U6）：
站②（isImageStation）展開後圖片區：
1. **三個子分頁**（不是文案五 tab 那層）：主圖｜規格圖｜詳情圖
   - 規格圖＝ is_spec_process 或既有規格語意；對齊 processMarks／image_type
   - 與 RESULT_CARD_TABS 的「圖片」tab 內部再分，或站② 預設進圖片 tab 後顯示三分頁
2. **每分頁內**：
   - 既有 process_intent 標記（保留／簡轉繁／去字／重生）
   - **拖曳排序**（HTML5 DnD，同 UX-D ImageUploader 模式；寫回 sort_order）
   - **補圖**：可選檔上傳到該 draft 該 image_type（主／詳情）；規格圖語意勿與 OCR 混淆
3. 站① 仍**無**圖片 tab（UX-C T7 不可回退）
4. 站③ 可維持現況或唯讀縮圖——本包以站② 為主，勿大改站③

驗：站② 三分頁可切；拖兩張互換後重載順序對；補一張主圖出現在列表。

────────
【T35】② 分流確認文案：全保留→待發布／有標→進工廠＋估價
────────
現況：stationReview／batchStationReview 已有 decideStation2Review + formatAiEstimateMessage；
UX-E 已 double-confirm。

本包強化文案（不改 API 分支語意）：
1. 全 keep：確認鈕／二次確認文案明確 **「通過 → 待發布（$0／不等工廠）」**
2. 有 AI 標記： **「送進生圖工廠（N 張 · 估價…）」**
3. 批次混態：摘要「X 件直達待發布 · Y 件進工廠」（若 decide 已能分，UI 列出來）
4. 成功後 toast：
   - 進工廠：「已送生圖工廠…」+ 可選「去工廠看」
   - 直達③：「已到待發布」
5. 命名：用「標圖／生圖工廠／待發布」，不用「圖片審核」當站② 名

動檔：ResultCard.tsx、DraftResultsPanel.tsx（批次文案）、可選 stationRoute 純字串 helper

════════════════════════════════════
本包不做
════════════════════════════════════
- 重做 /review 滑桿工廠本體（D5 已有）
- UX-G 頂欄／儀表板、UX-H 設計系統大收斂
- api／providers／csv／migration
- 第 4 站籤

════════════════════════════════════
流程
════════════════════════════════════
1. 先貼「修法確認清單」：
   - T29 分類規則（用哪些欄位）
   - T30 三分頁掛在哪一層 tab
   - T35 文案字串草案
   等「指揮放行」再動 code。
2. Commit 建議：
   feat(UX-F-T29): factory bridge strip
   feat(UX-F-T30): station2 image subtabs reorder upload
   feat(UX-F-T35): station2 route confirm copy
3. 每步 typecheck；CSS → b15-probe
4. 收工：計劃包 §9、施工清單各一行
5. 回報：資料不足時的降級行為、給 Claude 核帳步驟、手動？Y/N

鐵則：工廠獨立頁保留；橋接只預覽＋跳轉；只加不減；三主題＋手機。
```

---

## 給 Claude 核帳用（UX-F 完工後）

| # | 步驟 | 通過標準 |
|---|---|---|
| 1 | 工作檯結果區 | 有工廠橋接列（或 0 件時合理隱藏／空文案） |
| 2 | 有送工廠的稿 | 迷你卡狀態可辨；「打開工廠」進 /review |
| 3 | 站② 展開圖片 | 主圖／規格／詳情三分頁 |
| 4 | 站② 拖排序＋補圖 | 順序可改；可新增圖 |
| 5 | 站① | 仍無圖片 tab／未標記逼人 |
| 6 | 站② 審核 double-confirm | 全 keep vs 進工廠文案可分；有估價或 $0 |
| 7 | 三主題＋375 | 無爆版 |
