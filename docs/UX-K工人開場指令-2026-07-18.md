# UX-K「效能字體＋收尾打磨」工人開場指令

> 用法：開**新**工人對話，整段貼 ```text```。  
> **依賴**：建議 I／J 核後或可與 J 平行（少撞 globals 時注意）。  
> 本包收掉 T50 與文件／微視覺尾項；**UIUX 計劃表內可做項清完**（不含無截圖的 BX-P 亂改、不含 Fable §4.4）。

---

```text
你是 Nestory【UX-K｜效能字體＋收尾打磨】工人。
UI/UX 總指揮透過老闆轉達；Fable 避撞。

════════════════════════════════════
必讀
════════════════════════════════════
1. docs/交接指南-給接手的AI模型.md
2. docs/UIUX總指揮計劃包-2026-07-18.md §6 UX-K（T50 T59 T60）
3. AGENTS.md（**不要**整站改成 Mockup 柔皮膚；.sel 硬邊保留）
4. src/app/layout.tsx、src/app/globals.css 頂部 Google Fonts @import
5. UIUX相關/nestory-uiux-review-notes.md 2-3 微柔化（只取小劑量）

════════════════════════════════════
避撞
════════════════════════════════════
禁止 api/providers/csv/migration/prompt；禁止 push；不自稱實機通過

════════════════════════════════════
本包只做：T50、T59、T60
════════════════════════════════════

────────
【T50】next/font 自托管
────────
現況：globals.css `@import` Google Fonts（Noto Sans TC + Space Grotesk）——外連、FOUT、CSP／隱私差。

定案：
1. 用 `next/font`（google 或 local）在 layout 載入同等字重；CSS 變數掛到 body。
2. **移除** globals 頂部 Google `@import url(...)`。
3. 字重對齊現用：Noto 300/400/500/700；Space Grotesk 400–700（用不到的可減）。
4. 不改字級尺度（那是 H 的事）；只換載入方式。
5. 驗證：build/typecheck；畫面字體不爆、三主題 OK。

────────
【T59】小劑量收尾視覺（文件已點名、非 BX-P 截圖）
────────
只做**已寫在 Claude 筆記**且不違反 AGENTS 的：
1. **卡片／縮圖 hover**：若 H 的 --shadow-s 未覆蓋到的高頻塊，補齊同一 token（不要新色）。
2. **Topbar**：可加極輕 `backdrop-filter: blur(8px)` + 既有 surface 半透明——**可選**；
   若三主題對比變差就**不做**，修法清單二選一並說明。
3. **禁止**：改 .sel 硬邊、全站 radius、panel-hdr 中文 uppercase 回潮（H 已去）、大改配色。

────────
【T60】三主題 token 檢查清單（文件）
────────
Claude 1-11：改 token 要查三主題。
定案：新增短文件 `docs/三主題token檢查清單.md`（或寫進 UIUX 計劃包附錄）：
- 改 --warn／--success／--accent 等時必查 dark／nordic／kitty 對比
- 狀態字不可用過淺色當 text
- 附 b15-style-probe 指令一行
純文件，可 1 commit。

════════════════════════════════════
本包不做
════════════════════════════════════
- BX-P 老闆截圖逐像素（無圖不開）
- Fable §4.4 全部
- 重做 A～J 已過功能

════════════════════════════════════
流程
════════════════════════════════════
1. 修法清單 → 指揮放行
2. commits：feat(UX-K-T50)／style(UX-K-T59)／docs(UX-K-T60)
3. typecheck；b15-probe
4. §9 註「計劃表 UI 可做項清完（BX-P 除外）」
```

---

## 核帳表（UX-K）

| # | 通過標準 |
|---|---|
| 1 | 無 Google Fonts 外連 import；字體仍正常 |
| 2 | 三主題可讀；.sel 硬邊仍在 |
| 3 | token 檢查清單文件存在 |
| 4 | 1280＋375 無爆版 |
```
