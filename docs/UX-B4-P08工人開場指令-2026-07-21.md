# UX-B4-P08「主圖／詳情圖縮圖 — 恢復換行＋正方形＋角標不擠」工人開場指令

> **用法**：開**新的 Grok 工人對話**，把下方 ```text``` 框**整段貼上**。  
> **工人**：**Grok**（只實作＋commit；**不 push**）。  
> **來源**：老闆 2026-07-21 截圖——輸入區縮圖應往下排、正方形、規格圖標與 × 不擠。  
> **分支**：`codex/nestory-v0.1-safety-skeleton`（`git pull` 後開工）。  
> **對照 commit**（指揮已查 git）：

| 狀態 | Commit | 重點 |
|---|---|---|
| **還好（P10 前）** | `d25c067` 前／`ed342ce^` | `.pthumb-strip { flex-wrap: wrap }`；正方形 **64×64**（主圖 **96×96**）；× 在右上外側 |
| **改壞 1** | `ed342ce` **UX-B2-P10** | 改成 `flex-wrap: nowrap` + `overflow-x: auto`（只能橫滑、不往下排）；圖放大 96／120 |
| **改壞 2** | `2cc900b` | 輸入區 × 改到**左上**（要讓右上給規格圖）→ 與「規格圖」標在小圖上仍易擠 |
| **加劇** | `5f73952` **UX-B4-P07** | `.workspace-input-panel`／`.panel-body` 設 `overflow-x: clip` → 橫滑條被裁、外側 × 易被切掉 |

---

```text
你是 Nestory【UX-B4-P08｜輸入區縮圖排版還原】工人（Grok）。
臨時代理指揮已用 git 對過「還沒壞」與「修壞」的 commit，照修即可。

════════════════════════════════════
必讀
════════════════════════════════════
1. AGENTS.md（tokens、禁止新增 !important、不 push）
2. 本指令上方「對照 commit」表
3. 請本機對 diff（必做）：
   git show ed342ce -- src/app/globals.css
   git show ed342ce^:src/app/globals.css | 搜 pthumb-strip / pthumb-img / thumb-remove
   git show 5f73952 -- src/app/globals.css
4. 動檔以 src/app/globals.css 為主（輸入區 .pthumb*／.workspace-input-panel）
   可讀 ImageUploader.tsx 確認 class，但盡量不改 TSX

════════════════════════════════════
老闆要的（驗收標準）
════════════════════════════════════
1. 縮圖可以**往下換行排列**（多圖時多列），不要只剩一排橫滑
2. 縮圖是**正方形**（object-fit: cover + 等寬高）
3. **規格圖**角標與 **× 刪除**不要擠在同一角／重疊
4. 左欄 420px 內完整看得見，不被 P07 clip 切掉角標

════════════════════════════════════
根因摘要
════════════════════════════════════
- 換行消失：P10 把 .pthumb-strip 從 flex-wrap:wrap 改成 nowrap + overflow-x:auto
- 角標擠：× 與 規格圖 都貼在 96px 圖的上下角；P07 overflow-x:clip 再裁外側控件
- 站②圖審橫滑（.s2-img-panel）是另一套，**不要連站②一併改回 wrap**（除非樣式明確共用且驗過）

════════════════════════════════════
本包只做
════════════════════════════════════

【1】輸入區 .pthumb-strip 恢復換行（必做）
僅限表單上傳區（.workspace-input-panel 或 .upload-section 下的 .pthumb-strip）：

  flex-wrap: wrap;
  overflow-x: visible;   /* 不要靠橫滑當主排版 */
  overflow-y: visible;
  align-items: flex-start;
  gap: 10px 12px;        /* 略留白，角標不打架 */

若站② .s2-img-panel 也用 .pthumb-strip 且必須保留橫滑：
  → 用更精確 selector：
    .workspace-input-panel .pthumb-strip { wrap… }
    .s2-img-panel .pthumb-strip { 維持 nowrap + overflow-x:auto }

【2】正方形縮圖尺寸（必做）
對齊 P10 前的呼吸感，但可略大於 64（左欄較好點）：

建議輸入區：
  .workspace-input-panel .pthumb-img {
    width: 72px;   /* 或 64px，必須正方形 */
    height: 72px;
    object-fit: cover;
    aspect-ratio: 1;
  }
  .workspace-input-panel .pthumb.is-main .pthumb-img {
    width: 96px;
    height: 96px;
  }

不要用會壓扁的非 1:1 規則。

【3】× 與規格圖不擠（必做）
策略（擇一寫進報告）：

  建議 A（還原 P10 前 × 位置＋規格圖改放下方文字 chip）：
  - × 回到圖右上外側：top: -6px; right: -6px（.thumb-remove 預設）
  - 規格圖：不要蓋在圖右上；改在 .pthumb 下方小標（.pthumb-spec-label）或左下角小字
  - 取消「只為了騰右上而把 × 釘左上」的輸入區覆寫（.pthumb-img-wrap > .thumb-remove 左上規則）

  或 B（兩角分離＋外留白）：
  - × 左上外側 top:-6px; left:-6px
  - 規格圖 右下（bottom: 6px; right: 6px）或圖下方
  - .pthumb 加 padding: 6px 或 strip 加 padding，避免 clip 切角

禁止：× 與「規格圖」兩字重疊在同一角。

【4】P07 clip 不要誤殺縮圖角標（必做）
檢查 .workspace-input-panel / .panel-body 的 overflow-x: clip：
  - 若會切掉 ×／角標外側，對 .upload-section 或 .pthumb-strip 給足夠 padding
    （例如 padding: 8px 8px 4px）讓 absolute 角標仍在 clip 盒內
  - 或對 .upload-section { overflow: visible } 且不讓 strip 撐破整欄寬
  - **不可**為了縮圖又把整欄 overflow-x 改回 visible 導致 P07 左右欄重疊回歸

【5】回歸
[ ] 主圖 5 張：自動換 2 列以上（視欄寬），正方形
[ ] 詳情圖 10+ 張：往下排，不必只靠橫滑
[ ] × 可點、不跟「規格圖」重疊
[ ] 桌機左欄不蓋右結果卡（P07 仍成立）
[ ] 手機上傳區可捲、觸控 × ≥ 可點
[ ] 站②圖審若有橫滑需求仍可用
[ ] typecheck 綠；不 push

════════════════════════════════════
不做
════════════════════════════════════
- 不改上傳／標記 API
- 不重做 ImageUploader 業務流程
- 不重開左右欄重疊（P07）

════════════════════════════════════
收工
════════════════════════════════════
commit：fix(UX-B4-P08): restore wrap square thumbs and uncrowd spec/remove
回報：
1. strip wrap 的最終 selector
2. 縮圖 px
3. ×／規格圖位置策略 A 或 B
4. 如何避免 P07 clip 切角標
5. 站②是否受影響

開工先回 5 行：對照 commit 是否一致、wrap 做法、尺寸、角標策略、P07 相容。
```

---

## 總指揮備註

| 項 | 內容 |
|---|---|
| 老闆痛點 | 換行消失、角標擠、不像正方形網格 |
| 主兇 | B2-P10 橫滑 nowrap；P07 clip 加劇 |
| 還原錨點 | `ed342ce^` 的 wrap + 64/96 正方形 |
| 推送 | 核帳後代 push |
