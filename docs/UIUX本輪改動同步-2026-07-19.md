# UIUX 本輪改動同步（2026-07-19）

> **給其他對話／AI 模型**：先讀本檔再動手，避免重複做或覆蓋。  
> **分支**：`codex/nestory-v0.1-safety-skeleton`（相對 origin **超前、未 push**）  
> **typecheck**：綠（`node node_modules/typescript/bin/tsc --noEmit`）  
> **權威進度**：施工清單 `docs/施工清單.md` ＋ 本檔 ＋ `docs/UIUX總指揮計劃包-2026-07-18.md` §9

---

## 0. 一句話

**UIUX 擴展 T74–T119 程式已寫待 Claude 核畫面；BX1–BX10 加分項做完；T112 胖鈕截圖已修；SYN-1 站②「生成詳情圖」UI 已接。未 push。**

---

## 1. 本輪 commits（由新到舊，與本包相關）

| Commit | 說明 |
|---|---|
| `34ab4d3` | **feat(UX): BX2 undo toast, BX6 FAB, BX7 cost, BX9 lightbox, BX10 tone** |
| `1658fbe` | docs(UX): design specs, worker prompts, checklist sync |
| `04f35c1` | **feat(UX): SYN-1 generate-detail toggle + BX5 batch busy label** |
| `0ed471f` | **feat(UX): AA–AC polish + T112-r3 fat-button fix** |

（更早尚有 SYN-1 管線後端、P6 等 Fable 包 commits，不在此列。）

---

## 2. 功能清單（完成狀態）

### 2.1 擴展 UX 包（T74–T119）— **程式完成、待 Claude 核畫面**

| 包 | T# | 狀態 |
|---|---|---|
| UX-T | T74–T78 a11y | 程式完成待核 |
| UX-Y | T79–T82 登入＋T96/97 | 程式完成待核 |
| UX-Z / AA | T98–T103, T107–T111 質感 CSS | 程式完成待核 |
| UX-AB | T83, T85, T101, T104 | 程式完成待核 |
| UX-X | T90, T92, T93（T91 不做） | 程式完成待核 |
| UX-AC | T112–T119 | 程式完成待核（含 T112-r3 截圖修） |

規格目錄：`docs/UIUX設計規格/`（含 `UX-AC-設計規格.md`、擴展總表）。

### 2.2 BX 加分項（施工清單 Phase B＋）

| 項 | 狀態 | 實作摘要 |
|---|---|---|
| BX1 逐件審核 | ✅ 已核 | UX-Q T70 |
| BX2 全 undo | ✅ | toast「復原」10s；核准／分流／封存 |
| BX3 全頁拖圖 | ✅ | UX-I T47 |
| BX4 恢復條 | ✅ | 既有 |
| BX5 批次主鈕進度 | ✅ | `busyLabel` |
| BX6 手機中央＋ | ✅ | FAB 新增 |
| BX7 成本小字 | ✅ | 生成鈕下粗估 NT$ |
| BX8 空狀態 | ✅ | UX-G＋AB |
| BX9 lightbox | ✅ | 工廠對比圖點放大 |
| BX10 語氣 per IP | ✅ | localStorage |
| BX-P 老闆打磨 | ⏸ | 刻意延後，勿順手美化 |

### 2.3 其他本輪 UI

| 項 | 狀態 | 說明 |
|---|---|---|
| T112-r3 胖鈕 | ✅ | 老闆截圖：一般 pill、footer 重新生成／核准 |
| T114 移出→✕ | ✅ | 卡右上 ✕；展開區不再放「移出佇列」 |
| SYN-1 UI 開關 | ✅ | 站②「生成詳情圖」＋批次開/關（後端 flag 原本就有） |
| T105-lite | ✅ | ir-btn 等高；**非**全站 class 收斂 |
| T105 全收斂 | ⏸ 轉 Fable | |
| T84 CSS modules | ⏸ 轉 Fable | 工程債 |
| T106 審核改名 | ⏸ 等老闆 | |

---

## 3. 行為細節（接續者必讀）

### BX2 Undo（`src/lib/drafts/quickUndo.ts` + Toast action）

| 動作 | 復原方式 | 備註 |
|---|---|---|
| 核准文案 | `POST /api/drafts/:id/return-stage` `target=copy_review` | 會變 needs_revision 語意站① |
| 標圖分流（待發布／送工廠） | 同上 `target=image_review` | **送工廠 best-effort**；深處理可能清不乾淨 |
| 封存 | `POST /api/drafts/batch/archive` `unarchive` | notice 復原鈕仍保留 |

- Toast 支援 `showToast(msg, variant, 10000, { actionLabel: "復原", onAction })`  
- 契約：`src/lib/toast/toastEvents.ts`（`actionLabel` + `toastActionCallbacks`）  
- UI：`src/components/Toast.tsx`（`.toast-main` + `.toast-action`）

### BX5 批次 busyLabel

- `DraftResultsPanel`：`busyLabel` 顯示在主批次鈕上  
- 例：核准中 N 筆、分流中 1/2、開詳情圖 k/n

### BX6 手機 tabbar

- 版面：**審核｜工廠｜中央凸起＋新增｜更多**  
- `src/components/MobileTabbar.tsx`、`src/lib/nav.ts`（`MOBILE_SIDE_TABS`）  
- CSS：`.mtb-fab`、`.mobile-tabbar--fab`；`--tabbar-h: 72px`（≤959px）

### BX7 成本粗估

- `src/lib/drafts/generateCostHint.ts` → `buildGenerateCostHint`  
- 依：模型 claude/openai、主圖+詳情張數、Web Search、測試模式 $0  
- 文案固定含「**僅供參考**」；**不是帳單**  
- ImageUploader 新增 `onCountsChange({ main, detail })`

### BX9 Lightbox

- `src/components/review/ImageLightbox.tsx`  
- 接在 `ImageCompareSlider`（生圖工廠）  
- 滾輪縮放、雙指 pinch、Esc／點背景關閉

### BX10 語氣記憶

- `src/lib/drafts/toneMemory.ts`  
- key：`localStorage["nestory_tone_by_ip"]`  
- 寫入：表單生成成功（`detectedIpName`）、卡片重新生成  
- 讀取：標題含 IP 名且目前仍是預設語氣時自動帶；開重生 modal 預填  
- API：`/api/generate` 成功 JSON 多回 **`detectedIpName`**（供 client 記憶；draft 本來就會寫 ip）

### SYN-1 生成詳情圖 UI

- Flag：`product_drafts.image_flags.generate_detail`（缺省＝開，見 `isGenerateDetailEnabled`）  
- 單卡：`Station2ImagePanel` 頂部 toggle（`.s2-compose-row` + 既有 `.toggle`）  
- 批次：站②「更多」→ 開·生成詳情圖／關·生成詳情圖  
- **後端管線已在 SYN-1 完成**；本輪只補 UI

### T112-r3 胖鈕（老闆截圖）

- 根因：footer 裸 `button` 吃全域 `min-height: 38px`；sh-mode pill 寫死 44  
- 修：  
  - `.panel-header .sh-mode-pills .pill-btn` 桌面 28px  
  - `.rc-actions button` / `.act-btn` 桌面 ~30px；手機 ≥44  
  - ResultCard footer 掛 `act-btn`／`fill`；刪展開區「移出佇列」

---

## 4. 主要動到的檔案

### 新增

| 檔 | 用途 |
|---|---|
| `src/lib/drafts/quickUndo.ts` | BX2 復原 API 包裝 |
| `src/lib/drafts/generateCostHint.ts` | BX7 成本粗估 |
| `src/lib/drafts/toneMemory.ts` | BX10 localStorage |
| `src/components/review/ImageLightbox.tsx` | BX9 |
| `docs/UIUX設計規格/*` | T74–T119 規格 |
| `docs/UX-*工人開場指令-*.md` | 歷史發包骨架 |

### 大改

| 檔 | 用途 |
|---|---|
| `src/app/globals.css` | T112-r3、BX6 FAB、toast action、lightbox、gen-cost-hint、s2-compose… |
| `src/components/Toast.tsx` | action 鈕 |
| `src/lib/toast/toastEvents.ts` | action 契約 |
| `src/components/MobileTabbar.tsx` | BX6 |
| `src/lib/nav.ts` | MOBILE_SIDE_TABS |
| `src/components/listing/DraftResultsPanel.tsx` | BX2／BX5／SYN-1 批次 |
| `src/components/listing/ResultCard.tsx` | BX2／T114／act-btn／語氣 |
| `src/components/listing/Station2ImagePanel.tsx` | SYN-1 toggle |
| `src/components/listing/WorkspaceInputPanel.tsx` | BX7／BX10 |
| `src/components/listing/ImageUploader.tsx` | onCountsChange |
| `src/components/review/ImageCompareSlider.tsx` | BX9 接線 |
| `src/app/api/generate/route.ts` | 回傳 `detectedIpName`（BX10） |

### 文件

| 檔 | 用途 |
|---|---|
| `docs/施工清單.md` | BX 勾選＋進度筆記 |
| `docs/UIUX總指揮計劃包-2026-07-18.md` | 狀態頭＋§9 |
| `docs/UIUX指揮交接-下一任接續.md` | 短交接 |
| **本檔** | 跨對話完整同步 |

---

## 5. 刻意不做／留給別人

| 項 | 誰 | 說明 |
|---|---|---|
| Claude 核畫面 AA～AC | Claude 審查 | 程式已寫勿重複大改視覺 |
| T106 審核 tab 改名 | 老闆 | 建議「進度」或「管理」 |
| T105 按鈕 class 全收斂 | Fable | 設計系統工程；已有 lite |
| T84 CSS modules 拆檔 | Fable | globals 單檔債 |
| BX-P 老闆版面打磨 | 老闆累積截圖後 | **禁止** session 順手美化 |
| 送工廠 100% 可逆 | — | 需 Fable 取消 batch 管線才做得到 |
| BX7 真帳單級成本 | Fable | 現為粗估 |

---

## 6. 接續開場建議（貼給下一個 AI）

```text
先讀：
1) docs/UIUX本輪改動同步-2026-07-19.md（本檔）
2) docs/施工清單.md 進度筆記最新幾則
3) docs/UIUX總指揮計劃包-2026-07-18.md 開頭狀態＋§9
4) AGENTS.md 鐵則

現況：T74–T119 待 Claude 核；BX1–10 已做；未 push。
不要重做 BX2/6/7/9/10 或 SYN-1 toggle。
下一優先：Claude 核帳 / 老闆 T106 / Fable T105·T84。
```

---

## 7. 驗收口語清單（老闆可點）

1. 核准一筆 → toast 有「復原」→ 點了回到審文案  
2. 封存 → toast 或 notice「復原」  
3. 手機底欄：中間大 **＋** 是新增  
4. 生成鈕下有「本次預估約 NT$…（僅供參考）」  
5. 生圖工廠點圖可放大、雙指縮放  
6. 同一 IP 第二次生成／重生，語氣會跟著上次  
7. 站②有「生成詳情圖」開關；一般 pill／核准鈕不再又胖又寬  

---

*寫於 2026-07-19 Grok UIUX 指揮 session；改動以 git `34ab4d3` 為本輪功能收口。*
