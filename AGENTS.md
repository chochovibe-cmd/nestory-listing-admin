# Nestory 新 AI Session 入口（2026-08-18 新增）

新的 Codex / Claude Code / ChatGPT / 其他 AI coding session：

1. 先讀 `AI_START_HERE.md`
2. 再讀 `docs/CURRENT_STATUS.md`
3. 再讀本檔其餘常駐規則

若工作涉及 UI，再讀 `docs/REGRESSION_AUDIT.md`、Mockup 與 `docs/Mockup差異備忘.md`。
**不要一開始通讀全部 docs；dated 施工文件只在需要歷史細節時查。**

> 下方為原本 AGENTS.md 內容，完整保留供歷史與規則追溯。

---

# Nestory 上架系統 — 給所有 AI 模型的常駐規則

接手任何工作前，先讀 `docs/交接指南-給接手的AI模型.md`（含施工順序、鐵則、已知地雷）。
UI 驗收標準是 `docs/mockups/nestory-v7-mockup.html`（v7.4，用瀏覽器打開看）。

## 文件地圖（先看這裡，防止舊方向污染）

**現役文件就下面這些，其他一律不是實作依據**。遇到內容衝突時的優先序：
`docs/Mockup差異備忘.md`（最新定案）＞ Mockup ＞ 施工清單各項註記 ＞ 四份規劃文件。

| 用途 | 文件 |
|---|---|
| 進度唯一真相（每 session 頭尾必讀寫） | `docs/施工清單.md` |
| **UIUX 跨對話同步（2026-07-21 本輪｜B4）** | **`docs/UIUX本輪改動同步-2026-07-21.md`** |
| UIUX 同步（2026-07-19｜B2／歷史） | `docs/UIUX本輪改動同步-2026-07-19.md` |
| UIUX 短交接 | `docs/UIUX指揮交接-下一任接續.md` |
| UIUX 計劃包／§9 | `docs/UIUX總指揮計劃包-2026-07-18.md` |
| 定案差異（與 Mockup/舊文件衝突時以此為準） | `docs/Mockup差異備忘.md` |
| UI 驗收標準 | `docs/mockups/nestory-v7-mockup.html` |
| 施工順序／鐵則／地雷 | `docs/交接指南-給接手的AI模型.md` |
| 規劃細節（四份，照施工清單代號引用） | `下一階段完整計畫-2026-07-07.md`／`自動化流程設計-2026-07-08.md`／`文案生成架構與角色權限-2026-07-08.md`／`計劃書總覽-給老闆看的.md` |
| 基礎設施參考（仍有效） | `rls-policy-guide.md`／`rls-smoke-tests.md`／`supabase-storage.md`／`admin-bootstrap.md`／`部署平台決策.md`／`部署決策重點摘要.md` |
| 使用者手冊 | `docs/我的操作手冊.md` |

⛔ **`docs/archive/` 一律不得引用**——那是 v0.1 舊方向的封存區（舊 worker 文案架構、
作廢的環境變數名、過時 payload 說明），只保留歷史，README 有逐份過時原因。
規劃文件裡若遇到與差異備忘矛盾的段落（例如「規格圖 OCR」），以差異備忘為準。

## UI 一致性鐵則（每次動到前端都適用，無例外）

1. **動 CSS 前，先讀 `src/app/globals.css` 現有的 class 與 CSS 變數**，優先重用既有元件樣式。
   禁止自創新的顏色值、圓角值、陰影值、字級——只能用現有 tokens。
2. **風格基準＝globals.css 現有三主題（dark／nordic／kitty，源自分支任務，使用者最喜歡這套）**。
   Mockup v7.4 的柔化樣式（淡框線＋柔陰影）只是示意，**不要**把現有主題改成那樣；
   除非使用者日後明確要求柔化，才以新主題方式提供（見風格試衣間）。
3. **（UX-BTN S3／T84）禁止新增 `!important`**。globals.css 既有約 13 處屬性是歷史
   補丁可保留；新規則衝突時用更精確 selector 或調整 source order，不要再加
   `!important`。真繞不開先寫進施工清單等確認。長期拆 CSS modules 仍歸 Fable／T84。
4. 設計語言約定（跨風格通用的 UX 規則，任何主題都適用）：
   - 使用者選取＝`.sel`（accent 外框／ring／fill 變體），**新 UI 一律用共用類**
   - 系統狀態＝`.schip`（透明／淡底＋狀態色字＋淡框——跟現站最終樣，**不是**實心色塊）
   - 選取與狀態兩者不可混用；勿把 chip 改回實心除非老闆明確要求
   - 狀態色：灰=未開始／黃=進行中／綠=完成／紅=失敗（error／revision 現況用 `--accent2`）；狀態文字用全文（「文案已生成・待審核」）
   - 卡片內部欄位不加框不加底色，用留白＋小型粗體標籤分層（資訊密度規範）
   - 按鈕兩級：影響結果的操作大尺寸（min-height 40px+）、輔助操作 mini
   - 任何新樣式必須三個主題＋手機版都測過
5. **新增功能的 UI 必須指出「沿用哪個既有元件的樣式」**；找不到可沿用的，先對照 Mockup
   相應區塊，仍沒有才提案新樣式並等使用者確認。
6. 改完前端要回報：動了哪些檔案、新增/修改了哪些 class、與現有哪個元件保持一致。
7. 手機（<960px）必查：觸控高度 ≥44px、彈窗為底部抽屜式、無橫向爆版。

## 工作規則

- SQL 只產 `.sql` 檔到 `supabase/migrations/`（編號接續），不跑 Supabase CLI
- `src/app/api/**/route.ts` 只能 export HTTP method（GET/POST…）——export 其他 helper
  本機 typecheck 會過但 **Vercel build 會炸**（教訓：P0-63，ad66017 修復）。
  共用邏輯抽到 `src/lib/` 再 import
- 敏感 key 只放伺服器端環境變數；部署平台是 Vercel（不是 Cloudflare）
- Tags/Collections 永遠由規則引擎產出；文案生成走同步呼叫，只有圖片與發布走 worker
- 不要 push/deploy，除非使用者明確同意
- 使用者是非技術背景店主，用白話溝通；每完成一步回報：做了什麼／改了哪些檔案／
  下一步建議／需要手動操作嗎（Y/N）
- **收工儀式（強制）**：本 session 約定的 3–5 項全部完成、清單打勾、進度筆記寫好之後，
  最後一則回覆必須用這個格式收尾——
  「✅ 本包完成（已打勾：A1、A2、A3）。**建議現在開新對話**繼續，
  下一包預計：A4–A6（一行說明）。」
  另外：若對話已經很長（多次修錯、大量來回）而本包還沒做完，**主動**在自然段落點建議收工——
  把已完成的先打勾、未完成的寫進進度筆記（含卡住原因），請使用者開新對話接續，
  不要硬撐到品質下降

## Mockup 的參考權限分級（重要：Mockup 不是照抄對象）

**動任何前台前，先讀 Mockup 對應區塊，再讀 `docs/Mockup差異備忘.md`（第二真相來源）**——
那份記錄「跟 Mockup 不同、但已定案」的差異；兩者衝突以差異備忘為準。每個 session 產生新的
定案差異都必須當場補進那份文件（Mockup 不會自己更新，舊 docs 可能殘留被推翻的方向）。

`docs/mockups/nestory-v7-mockup.html` 的權威範圍分三級：

| 層面 | 權威 | 規則 |
|---|---|---|
| 功能與流程（有什麼功能、放哪裡、互動順序、狀態邏輯、防呆） | **Mockup 說了算** | 照做 |
| 資訊架構（頁面劃分、卡片結構、分頁、欄位順序） | **Mockup 說了算** | 照做 |
| 視覺細節（線條粗細、陰影、圓角、間距、配色的具體數值） | **現有網站說了算** | Mockup 只是示意；以 globals.css 現行風格為基準去「優化」，不要整包換成 Mockup 的數值 |

另外兩條保護規則：
- **只加不減**：實作 Mockup 的項目時，若現有系統的某功能做得比 Mockup 完整或好用，
  **保留現狀**，不得為了對齊 Mockup 而降級或簡化。任何「移除／簡化既有功能」都要明列並經使用者同意
- **衝突交裁決**：現有做法與 Mockup 衝突且無法判斷優劣時，列出「現狀 vs Mockup 差異表」
  （各自優缺點一行）請使用者挑，不要自行取捨

## 資訊密度規範（Phase B 實作前台時強制適用；使用者已確認 Mockup 過於擁擠）

1. **Mockup 裡的灰色說明小字＝給工程師看的規格註記，不是 UI 文案**——正式版預設不顯示，
   改為：欄位旁 `?` 圖示點開說明／首次使用提示（看過即消失）／placeholder 示範。
   常駐畫面的文字只允許「警告與錯誤」
2. **核心/進階分層**：表單常駐可見僅四項——標題、商品圖片、成本價格、生成按鈕；
   Variant、二手、AI 文案設定、備註做成收合區（有內容才自動展開）
3. **呼吸感**：上傳區加高、縮圖放大（主圖 96px 級）、欄位間距放寬；寧可頁面長，不可擠
4. **手機表單改手風琴分段**（基本資料→圖片→價格規格→風格），一次一段，填完自動前進
5. Mockup 的示範狀態（查重警告、同步警告等）在真實 App 一律條件式顯示，沒事不出現

## 風格更新請求的處理方式（使用者說「換風格／調整視覺」時照此分級）

1. **調性微調**（換色、陰影深淺、圓角、字級）：直接改 `globals.css` 的 `:root` 變數值，
   不碰個別元件；改完告知動了哪幾個變數
2. **元件級**（按鈕形狀、卡片樣式等）：改共用 class 一處生效；先列出會影響哪些畫面再動手
3. **整體換風格**：**不要覆蓋現有主題**——做成新的 `data-theme` 主題讓使用者在真網站上
   切換比較（風格試衣間），使用者確認轉正後才取代或並存；期間所有微調都只動新主題
4. 使用者若提供截圖/網站參照物，以參照物為準；沒有參照物而形容詞模糊時，
   先做一個小範圍樣本（一顆按鈕＋一張卡片）給使用者確認方向，再套全站

## 新增或修改 UI 功能的標準流程（使用者要求任何前台變更時照此執行）

1. 先讀相關現有元件與 globals.css，再看 Mockup 對應區塊
2. 提「設計方案」文字版，格式固定三段：
   ① 照 Mockup 新增什麼（功能/位置）② 保留現狀什麼（現有做得更好的）③ 衝突點請使用者裁決
   ——**等使用者確認**
3. 確認後實作，遵守上方鐵則與權限分級
4. 完成後自我檢查兩主題＋手機版，回報比對結果