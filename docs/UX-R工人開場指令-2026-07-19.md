# UX-R「模式可視化」工人開場指令（R89）

> 用法：開**新**工人對話，整段貼 ```text```。  
> **優先**：低成本高價值；可與 O／P／Q 待核並行（檔案幾乎不撞 SequentialReview）。  
> 來源：老闆實測 89；Fable 2026-07-19 轉達。

---

```text
你是 Nestory【UX-R｜模式可視化 R89】工人。
UI/UX 總指揮透過老闆轉達；不改 test/LLM 實際呼叫語意，只做「看得見、按之前知道」。

════════════════════════════════════
必讀
════════════════════════════════════
1. docs/交接指南-給接手的AI模型.md
2. docs/UIUX總指揮計劃包-2026-07-18.md ——§6 UX-R（T71）、§9
3. AGENTS.md
4. 現況：
   - src/components/ModeSwitcher.tsx（readStoredRunMode；test／live 或等價）
   - src/components/HeaderToolsMore.tsx（Mode 進 ⋯ 更多——UX-G 後易被忘記）
   - src/components/listing/WorkspaceInputPanel.tsx（生成鈕；已 import readStoredRunMode）
5. 痛點：測試模式白跑一輪（浪費時間／誤以為有真文案）

════════════════════════════════════
避撞
════════════════════════════════════
禁止：api/、providers/、csv/、migration、prompt
禁止：改 run mode 的 server 行為、mock 生成邏輯
禁止：push／deploy；自稱實機通過
可以：components、globals

════════════════════════════════════
本包只做：T71
════════════════════════════════════

────────
【T71】生成模式可視化（R89）
────────
指揮定案（三層，都要有，成本低）：

1. **生成鈕旁常駐標示**（主）
   - 讀 `readStoredRunMode()`（或現有同源 store）
   - 顯示 chip／小標，例如：
     - 測試 → `測試模式`（用 --warn 系 schip／muted 醒目，**可讀**）
     - 正式／LLM → `正式生成` 或 `LLM`（success／muted，不要和測試一樣黃）
   - 放生成鈕旁或 btn 上方一行；手機不擠爆（可換行）
   - 切模式後 UI 要即時更新（聽 storage event 或既有 ModeSwitcher 事件；若無則輕量 custom event／同 tab storage）

2. **按生成當下提示**（防呆）
   - **測試模式**：按生成時 `showToast("目前是測試模式：不呼叫 AI", "warn")`（或短 confirm 文案）
   - **不**強制擋生成（測試模式本來就要能跑流程）；只要「按下去當下知道」
   - 正式模式：可不 toast（避免吵）；可選極短 info 省略

3. **頂欄／⋯ 更多內模式切換醒目**
   - HeaderToolsMore 裡 ModeSwitcher 列：加標籤「生成模式」或測試時外框 --warn
   - 可選：測試模式時 topbar 極小 schip「測試」（勿把 Provider 又平鋪回來；仍在 ⋯ 內）

4. **只加不減**：ModeSwitcher 功能不刪；設定頁說明可留

驗：
- 切到測試 → 生成鈕旁立刻看到「測試」
- 測試下按生成 → toast 提示
- 切回正式 → 標示變正式、無誤導黃標
- 三主題 1280＋375；typecheck；b15-probe

════════════════════════════════════
本包不做
════════════════════════════════════
- R87 成本一鍵套用（UX-S／包二 B）
- 改 test mock 內容、api
- 把 Mode 重新平鋪頂欄全部工具（G 減重保留）

════════════════════════════════════
流程
════════════════════════════════════
1. 修法清單（文案用字、是否 storage 監聽）→ 指揮放行
2. commit：feat(UX-R-T71): visible run mode on generate and header
3. typecheck；b15-probe
4. §9＋施工清單；不自稱通過；不 push
```

---

## 核帳表（UX-R）

| # | 通過標準 |
|---|---|
| 1 | 生成鈕旁可見目前模式 |
| 2 | 測試模式按生成有 toast／明確提示 |
| 3 | ⋯ 更多內模式可辨識 |
| 4 | 三主題＋手機不爆 |
```
