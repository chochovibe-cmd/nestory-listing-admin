# UX-B4-P05「Shopify 真發布 — 關閉 Mock 安全模式」工人開場指令

> **用法**：開**新的 Grok 工人對話**，把下方 ```text``` 框**整段貼上**。  
> **工人**：**Grok**（程式小改可 commit；**不 push**；**不讀 .env.local 內容寫進報告**）。  
> **來源**：老闆項 #7「讓現在可以成功發布過去」；同步 `docs/UIUX本輪改動同步-2026-07-21.md` §2.7。  
> **語意**：**關閉** `SHOPIFY_PUBLISH_MOCK`（設 `false`）才會真打 Shopify；不是把 mock 開成 true。  
> **分支**：`codex/nestory-v0.1-safety-skeleton`。

---

```text
你是 Nestory【UX-B4-P05｜Shopify 真發布開關】工人（Grok）。
臨時代理指揮規格已定案。本包以 env 切換為主，程式只做誠實狀態／防呆文件。

════════════════════════════════════
必讀
════════════════════════════════════
1. AGENTS.md（敏感 key 不進前端、不 commit secret）
2. docs/UIUX本輪改動同步-2026-07-21.md §2.7
3. 程式：
   - src/lib/shopify/publishDraft.ts（mockMode = env !== "false"）
   - src/app/api/status/route.ts（shopifyMock）
   - .env.example（SHOPIFY_PUBLISH_MOCK 註解）
4. 施工清單 A1／A14 註記（client_credentials 新制）

════════════════════════════════════
避撞
════════════════════════════════════
禁止：
- push／deploy
- 把 .env.local 的 secret 貼進 commit／報告
- 改 publish payload／GraphQL mutation 語意（A14 已通）
- 把 repo 預設改成 MOCK=false 且無警示（.env.example 預設 true 可留作防呆）

可以：
- 更新 .env.example 註解寫清楚 true=假發布、false=真發布
- 若設定頁／status 已顯示 shopifyMock，確認文案白話「目前為模擬發布／真實發布」
- 缺憑證時錯誤要誠實（不要 mock 成功假裝已上架卻無後台連結）— 若現況已如此則不動

════════════════════════════════════
本包步驟
════════════════════════════════════

【A】對帳現況（只報告，不洩漏 secret）
1. grep SHOPIFY_PUBLISH_MOCK 出現處
2. 確認 publishDraft mock 分支行為
3. 確認 status API 是否暴露 shopifyMock

【B】程式（最小）
1. .env.example 註解改寫成老闆看得懂：
   - true＝安全模擬，不會建真實商品
   - false＝真實呼叫 Shopify（需 CLIENT_ID／SECRET／STORE_DOMAIN）
2. 若前端有「部署狀態／連線」已顯示 mock，檢查用詞是否會讓老闆以為「開 mock 才能發」——改成「模擬中／已接真店」二擇一清楚句。
3. 無必要不改 publishDraft 邏輯。

【C】老闆手動（寫進收工報告「需要手動操作」）
本機 `.env.local`：
  SHOPIFY_PUBLISH_MOCK=false
並確認已有：
  SHOPIFY_STORE_DOMAIN
  SHOPIFY_CLIENT_ID
  SHOPIFY_CLIENT_SECRET
重啟 dev server。

Vercel Production／Preview（若要線上真發）：
  同樣設 SHOPIFY_PUBLISH_MOCK=false
  三個 SHOPIFY_* 已在 env
  Redeploy 後才生效。

【D】驗收建議（工人本機若有憑證可測；無則列步驟給老闆）
1. status 顯示 mock=false（或等同）
2. 核准一件 → 發布草稿
3. 回填 shopify_product_id 非 "mock-product-id"
4. shopify_admin_url 可開後台
5. 若失敗：warnings／error_message 可見，不要靜默 mock 成功

════════════════════════════════════
驗收
════════════════════════════════════
[ ] .env.example 註解清楚
[ ] 無 secret 進 git
[ ] 收工報告含老闆 env 檢查清單（Y 手動）
[ ] typecheck 綠（若有改 ts）
[ ] 不 push

════════════════════════════════════
收工
════════════════════════════════════
commit（若有 code）：docs/chore(UX-B4-P05): clarify shopify mock vs live publish
或僅報告「無需 code，請老闆改 env」。
回報：現況 mock 預設、需老闆改哪幾個環境、如何確認不是 mock-product-id。

開工先回 4 行：mock 判斷式、status 是否暴露、建議 env 清單、是否需要改 UI 文案。
```

---

## 總指揮備註

| 項 | 內容 |
|---|---|
| 體量 | 極小 code + 老闆 env |
| 風險 | 真發布會建 Shopify DRAFT；確認老闆要的是 draft 不是 active |
| 手動 | **Y** — 老闆必改 `.env.local`（與 Vercel） |
