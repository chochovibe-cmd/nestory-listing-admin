# UX-B3-P03「篩選列手機響應式 — 規則集中」工人開場指令

> **用法**：開**新的 Grok 工人對話**，把下方 ```text``` 框**整段貼上**。  
> **工人**：**Grok**（實作＋commit；**不 push**）。  
> **粗包**：外部 Claude 第三輪 3／6（已潤飾）。  
> **分支**：`codex/nestory-v0.1-safety-skeleton`（HEAD 約 `f1b9f2d`；開工前 `git pull`）。  
> **前包**：B3-P01／P02 ✅ 已 push。

---

```text
你是 Nestory【UX-B3-P03｜篩選列手機 CSS 集中】工人（Grok）。
本包是「整理」不是「加新功能」。照做即可。

════════════════════════════════════
必讀
════════════════════════════════════
1. AGENTS.md（tokens、禁止新增 !important、不 push）
2. 只動：src/app/globals.css
3. 開工前在 repo 執行（或同等搜尋）：
   搜尋 stage-filter / results-sort / sort-sel / results-scope
   列出所有 @media 內相關規則的行號，再動手

════════════════════════════════════
避撞
════════════════════════════════════
禁止：
- push／deploy
- 改 DraftResultsPanel.tsx／任何 TSX（本包純 CSS）
- 重寫整個 globals.css 的全部 @media（只整理「篩選列」系列）
- 動 .rc-batch-strip、.result-card 手機 grid、source-card（別包的）
- 新增 !important；自創新色

可以：
- 刪／合併重複的 stage-filter 手機規則
- 在「一處」寫完整手機邏輯
- 桌機基礎樣式（@media 外）可留原位微調，勿打散

════════════════════════════════════
根因（已 grep 確認）
════════════════════════════════════
.stage-filter-row 系列手機規則散落多處、斷點打架：

| 約略位置 | 斷點 | 在幹嘛 |
|---|---|---|
| ~1938 | 640px | pills 橫滑；sort 不強制 100% |
| ~7771、~7844 | 960／768 | sort label／sort-sel 寬度、44px |
| ~9404 | 959px | column 排、end 滿寬、scope/sort 各半 |

cascade 後段蓋前段 → 手機上 pills＋只看我的＋排序擠成一團。

本包範圍＝只收斂這些 class 的**手機覆蓋**：
.stage-filter-row
.stage-filter-pills
.stage-filter-end
.results-scope-label
.results-sort-label
.sort-sel
.ir-scope-select（僅 stage-filter-end 內）

════════════════════════════════════
定案（與粗包微調）
════════════════════════════════════
粗包建議 max-width: 640px。
本專案 shell／底欄手機化是 **959px**（641–958 也是「手機殼」）。

**定案：篩選列手機版只保留一個 @media (max-width: 959px) 區塊**
（可新建註解標題「UX-B3-P03: stage-filter mobile — single source」，
放在桌機 .stage-filter-* 基礎樣式後面，或檔案後段 959 區塊內——
但該系列規則在 959 內只能出現「這一組」，刪掉 640／768／960 的重複。）

目標版面（≤959）：
1. 整列改 column：上＝站別 pills（滿寬、可橫滑）；下＝只看我的 + 排序並排各半
2. pills：nowrap + overflow-x:auto + touch 滑動；scrollbar 可藏
3. .stage-filter-end：width 100%、flex、gap 8px
4. scope／sort label：flex 1 1 0；內層 select  width 100%
5. 觸控：select／pill 建議 min-height 36～44px（沿用既有 token，勿亂加）

建議單一區塊內容（可微調，語意對齊即可）：

@media (max-width: 959px) {
  /* UX-B3-P03: stage-filter mobile — 唯一覆蓋處 */
  .stage-filter-row {
    flex-direction: column;
    align-items: stretch;
    gap: 8px;
  }
  .stage-filter-row .stage-filter-pills {
    width: 100%;
    order: 1;
    flex-wrap: nowrap;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
  }
  .stage-filter-row .stage-filter-pills::-webkit-scrollbar {
    display: none;
  }
  .stage-filter-row .stage-filter-pills .pill-btn {
    flex-shrink: 0;
    min-height: 36px;
  }
  .stage-filter-row .stage-filter-end {
    width: 100%;
    order: 2;
    display: flex;
    flex-wrap: nowrap;
    gap: 8px;
    margin-left: 0;
    margin-top: 0;
    justify-content: stretch;
  }
  .stage-filter-row .stage-filter-end .results-scope-label,
  .stage-filter-row .stage-filter-end .results-sort-label {
    flex: 1 1 0;
    min-width: 0;
    width: auto;
    margin-left: 0;
    justify-content: center;
  }
  .stage-filter-row .stage-filter-end .results-scope-label .ir-scope-select,
  .stage-filter-row .stage-filter-end .results-sort-label,
  .stage-filter-row .stage-filter-end .sort-sel {
    width: 100%;
    min-width: 0;
    box-sizing: border-box;
  }
  .stage-filter-row .stage-filter-end .sort-sel {
    text-align: center;
    min-height: 36px;
  }
}

清理動作：
1. 刪掉 640px 裡只服務 stage-filter 的重複（若 pills 橫滑已併入 959）
2. 刪掉 768／960 裡 .stage-filter-row .results-sort-label／.sort-sel 的重複覆蓋
   （若該 media 還有別的元件規則，只刪 stage-filter 相關行，勿整段砍光）
3. 檔案後段 959 若已有 stage-filter 一段：與上面合併成一處，不要兩段 959 各寫一半
4. 桌機基礎（@media 外的 .stage-filter-row 等）保留；只移「手機覆蓋」

════════════════════════════════════
自測（必做，回報要寫）
════════════════════════════════════
DevTools 裝置寬度（不要只拖拉視窗猜）：
[ ] 375px（SE）
[ ] 390px（一般）
[ ] 414px（較大）

每寬度確認：
- pills 可左右滑、不把下面兩顆擠爆
- 「只看我的」「最新在上」並排約各半、完整可點
- 無橫向整頁爆版

桌機 ≥960：篩選列仍是一列（pills 左、end 右），未被 column 化。

════════════════════════════════════
驗收
════════════════════════════════════
[ ] grep：stage-filter 系列的「手機覆蓋」只剩一處 @media（桌機 base 可另算）
[ ] 375／390／414 篩選列可讀、不擠爆
[ ] 無新增 !important
[ ] 只動 globals.css
[ ] typecheck 仍綠（CSS-only 通常無影響）

════════════════════════════════════
收工
════════════════════════════════════
commit 建議：
  fix(UX-B3-P03): consolidate stage-filter mobile CSS to single 959 block
不 push。
回報：刪了哪些舊區塊（行號或描述）、單一新區塊位置、三寬度自測結果。

開工先回 4 行：根因同意／定案 959 一處／預計刪哪幾段／有無 ambiguity。
```

---

## 總指揮備註

| 項 | 內容 |
|---|---|
| 工人 | **Grok** |
| 粗包 | Claude 3／6 |
| 範圍 | **只** stage-filter 篩選列，不是全檔 18 個 media 大掃除 |
| 斷點 | 粗包 640 → **定案 959**（對齊 shell） |
| 風險 | 工人誤刪 768 裡無關規則；核帳時 diff 要細看 |
| 推送 | 核帳後代 push |
