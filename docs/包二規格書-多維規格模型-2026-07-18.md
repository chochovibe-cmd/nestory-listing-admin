# 包二規格書：多維規格模型（2026-07-18，Fable 總指揮親寫）

> 依據：回饋 10（多維被壓單維，最重要）、55（Showmore 多款式未展開）、
> 84（款式值生成時未簡轉繁，2026-07-18 新增）、CAP 系列帶來的真實多維商品
> （Razer 皮卡丘滑鼠 16 款式已在庫）。
> 回饋 85（站①卡加規格分頁）＝ UI 歸 UIUX 指揮，本包只備資料面，見 §6。
> 狀態：**待老闆確認**；確認後 Fable 出工人開場指令。

## 1. 現況盤點（指揮已查證）

| 層 | 現況 | 缺口 |
|---|---|---|
| DB | 001 已有 option1/2/3_name/value；022（B7）已有 draft.variant_dimensions（≤3 軸） | 無 —— 模型夠用，**零/極少 migration** |
| 擷取 | CAP-1 mapCaptureFields 已寫 option1/2/3＋raw_capture.sku_table 原表留底 | 壓平 warning 後沒有「回放展開」 |
| 表單 | VariantEditor 單維列（option1='款式'） | 不能編第二/三軸；這是回饋 10 的主體 |
| Shopify payload | 需盤點：是否只送 option1 | 工人開工先查證 |
| Matrixify/Showmore | 主列單 SKU only（回饋 55 明載多款式未展開） | 多 variant 列展開 |
| 簡轉繁 | 標題/文案生成時已轉；**款式值不轉**（回饋 84：「皮卡丘鼠标SE」） | generate 時轉款式軸名與值 |

## 2. 目標

一句話：**淘寶的「顏色×尺寸」等多維商品，從擷取→表單→生成→發布/匯出全程
保持交叉結構，對齊 Shopify option1/2/3；款式文字入庫後生成時自動變繁體。**

## 3. 分包（建議兩刀，UI 依賴 UX 線收斂度）

### 包二A｜資料與管線（工人可立即做，不撞 UX）
1. **盤點與修正 Shopify payload / Matrixify / Showmore 的 option 對映**：
   - Shopify：productOptions 送滿 1–3 軸；variant 列帶 option1/2/3_value
   - Matrixify：多 variant → 多列（首列商品資訊、後續列留白＋各自 SKU/價/庫存），
     一併結清回饋 55 的最大缺口；74 的庫存對映沿用不回歸
   - Showmore：同 55 定案格式（首列有名後列留白）
2. **回饋 84｜款式簡轉繁**：generate 成功路徑對該草稿的 variant_dimensions
   軸名＋product_variants 的 option*_name/value 跑 localizeToTaiwanTraditionalText
   （既有轉換層＋用語表；「鼠标→滑鼠」這類走用語表）。只在生成時轉（表單
   手填當下不動，尊重輸入原文）；轉過標記避免重複轉。
3. **擷取回放升級**：CAP-1 落稿時 variants_flat 已含多軸——把「壓平 warning」
   改為「多維已入庫（N×M）」的正常資訊；raw_capture 完整交叉表若含價格，
   逐格對映到 variant 列（缺格才留空）。
4. verify-b7/matrixify/showmore 回歸＋新增多維案例（Razer 16 款式做 fixture）。

### 包二B｜表單多軸編輯（等 UX-D/I/J/K 收斂後，與 UIUX 指揮共管）
- VariantEditor 支援 1–3 軸定義（variant_dimensions）＋交叉列生成/編輯
- 版面遵 UIUX 計劃包；Fable 只驗資料行為
- 回饋 9/13/59（款式列快速移除、規格合併輸入、二手切換）屆時一起議

## 4. 裁決點預告（工人方案時要答）
- Shopify payload 現況實查結果（只送 option1？三軸怎麼補）
- 84 的「轉過標記」設計（欄位 or 冪等轉換即可不標記）
- Matrixify 多列展開與 P0-73 handle/74 庫存的互動（variant 列不重複主列欄位）
- 舊單維資料相容（option1='款式' 的既有草稿完全不受影響）

## 5. 驗收
- Razer 草稿（16 款式）：生成後款式全繁體；Matrixify 匯出 16+1 列結構正確；
  Shopify 測試建稿三軸可見
- 單維舊商品匯出/發布零回歸（verify 全綠）

## 6. 給 UIUX 指揮的資料面支援（回饋 85）
站①卡「規格」分頁：draft 的 variant_dimensions＋product_variants 已含全部
所需資料（軸名/值/成本/售價/庫存），無需新 API；編輯保存可走既有 draft
update 路徑。UI 何時做、放哪個 UX 包由 UIUX 指揮排程。
